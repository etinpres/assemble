# V4 Spike XI Plan — 3 standard bundles (idea-shaper / design-pack / guardian)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Each Task = fresh sub-agent dispatch + spec review + code-quality review.

**Date**: 2026-05-04
**Spec**: `docs/specs/2026-05-04-v4-spike-xi-design.md`
**Pattern**: subagent-driven-development — Task 단위 fresh sub-agent + 두 단계 review
**Baseline**: master `b55369a`, pytest 759 passed.

**Goal**: V4 결정 #1 라인업 마지막 3 stages (discover/design/safety)에 표준 등급 번들 3개 추가하여 10/10 채움.

**Architecture**: 표준 번들 3개 = SKILL.md + 1-2 prompt + templates. ★ 번들과 달리 Bash 권한 없음, dispatch 단순 (1-step 또는 메인 직접 IO), Codex retro 선택. wiring 단계는 7 ★ bundles이 쓰는 동일 패턴 재사용.

**Tech Stack**: Python 3 (server.py / harness.py / inventory.py 기존), pytest, Markdown templates, AskUserQuestion.

---

## Overview

10 tasks across 6 phases. 각 Task = 1 atomic commit. Phase boundary는 sub-agent context isolation checkpoint.

| Phase | Scope | Task count | Commit prefix |
|---|---|---|---|
| A | idea-shaper bundle (SKILL + prompt + template + tests) | 3 | `feat(v4-spike-xi-A{N})` |
| B | design-pack bundle (SKILL + prompt + 2 templates + tests) | 3 | `feat(v4-spike-xi-B{N})` |
| C | guardian bundle (SKILL + template + tests, dispatch X) | 2 | `feat(v4-spike-xi-C{N})` |
| D | wiring (harness + inventory + contracts.json — 단일 atomic) | 1 | `feat(v4-spike-xi-D)` |
| E | overall review (superpowers:code-reviewer) | 1 | `fix(v4-spike-xi-E)` (필요시) |
| F | B-16 dogfood + ship | 2 | mixed |

**Sequential ordering** (간단함):
- A → B → C → D → E → F (각 Phase 안 task는 같은 sub-agent context, Phase 경계마다 fresh)
- Phase A/B/C은 서로 독립이지만 plan 단순화 위해 순차 진행 (각 번들 ~5-10분)
- Phase D는 atomic — 3 wiring 변경 모두 같은 commit (Spike X D1+D2+D3+D4 precedent)

---

## Phase A — idea-shaper bundle (3 tasks)

### A1 — SKILL.md + 디렉토리 구조

**Goal**: bundled/idea-shaper/ 디렉토리 + SKILL.md 작성. 표준 번들 베이스라인.

**Files**:
- Create: `bundled/idea-shaper/SKILL.md`
- Create: `bundled/idea-shaper/prompts/subagent/.gitkeep` (Phase A2가 채움)
- Create: `bundled/idea-shaper/templates/.gitkeep` (Phase A2가 채움)

- [ ] **Step 1: SKILL.md 작성**

```markdown
---
name: idea-shaper
description: 모호한 아이디어를 사용자/문제/wedge 1페이지로 정리하는 표준 번들 (V4 discover stage)
stages: discover
grade: standard
---

# idea-shaper — discover stage 표준 번들

V4 결정 #1 라인업의 discover stage. 사용자가 "X 같은 거 만들고 싶어" 식 모호 아이디어를 가져오면 AskUserQuestion 4개로 본질을 추출하고 IDEA.md 1페이지로 정리.

## 사용 시점

- 새 프로젝트 brainstorm 직후, plan-pack ★ 호출 *전*
- 또는 plan-pack 단독 사용 가능 (사용자가 IDEA.md 직접 손으로 작성)

## 워크플로

### Step 1: 인터뷰 (메인 Claude 직접 — AskUserQuestion 묶음 ×2)

**1차 묶음 — 사용자 + 문제:**
- Q1: "이 아이디어가 누구를 위한 거야? (target user 1명만 골라)"
- Q2: "그 사람이 지금 *겪고 있는* 가장 구체적인 문제 1개?"

**2차 묶음 — wedge + non-goals:**
- Q3: "왜 *지금* 이 도구가 필요해? (timing edge)"
- Q4: "MVP에서 *명시적으로 제외* 할 항목 1개?"

옵션은 사용자 컨텍스트 기반으로 동적 생성. 4-옵션 강제 X — 의미 모호하면 더 길어도 풀어서 표기.

### Step 2: dispatch via `general-purpose` (preferred role: `text-summarize`)

dispatch_prompt('bundled/idea-shaper/prompts/subagent/idea_shape_step1.md', run_id) 호출.

번들 prompt가 자동으로 harness preamble v3 + Track B 학습 회수 fence를 prepend. 메인 Claude는 dispatch 결과만 받음.

sub-agent는 IDEA.md.template를 read하여 4 placeholder + {{TASK_SUMMARY}}를 substitute한 후 `{{RUN_DIR}}/IDEA.md`로 write.

### Step 3: 후속 알림

dispatch 완료 후 메인이 사용자에게 1줄: "IDEA.md 작성됨. 다음 단계 plan-pack ★로 PRD/ARCH/ADR/UI_GUIDE 만들래?"

## 서브에이전트 매핑 표

| 단계 | role | 폴백 보장 |
|---|---|---|
| 1. 인터뷰 | (메인 직접) | — |
| 2. IDEA.md 작성 | text-summarize | general-purpose + 컨텍스트 "한국어 1페이지 IDEA 문서 작성, 템플릿 placeholder 그대로 substitute" |

## 산출물

- `<run_dir>/IDEA.md` (5 sections: User / Problem / Wedge / Non-goals / Task summary)

## V4 정체성 합치

- ✅ V4 #9 orchestrator-only — 인터뷰 + dispatch만, 메인 무거운 작업 X
- ✅ V4 #13 역할 명시만 — text-summarize 사전 정의 / fallback general-purpose
- ✅ harness 4원칙 prepend — preamble v3 통해 자동 (Track B 학습도 자동)

## 비범위 (V4)

- ❌ 외부 검색 (web_search) — V5
- ❌ 시장 조사 / 경쟁자 분석 — V5
- ❌ wedge 자동 생성 — 사용자 인터뷰 필수
```

- [ ] **Step 2: 디렉토리 + .gitkeep**

```bash
cd ~/.claude/skills/assemble
mkdir -p bundled/idea-shaper/prompts/subagent bundled/idea-shaper/templates
touch bundled/idea-shaper/prompts/subagent/.gitkeep bundled/idea-shaper/templates/.gitkeep
```

- [ ] **Step 3: pytest 실행 (regression 없는지)**

Run: `cd ~/.claude/skills/assemble && pytest -x 2>&1 | tail -5`
Expected: 759 passed, no new failures (SKILL.md not yet wired).

- [ ] **Step 4: Commit**

```bash
git add bundled/idea-shaper/
git commit -m "feat(v4-spike-xi-A1): bundled/idea-shaper/ — SKILL.md + dir structure"
```

**Acceptance**:
- bundled/idea-shaper/SKILL.md ≤120 lines
- 4 sections: 사용 시점 / 워크플로 / 서브에이전트 매핑 / 산출물
- frontmatter `stages: discover`, `grade: standard`
- pytest baseline 759 그대로

**Sub-agent type**: `general-purpose`.

---

### A2 — prompt + IDEA.md template

**Goal**: idea_shape_step1.md prompt + IDEA.md.template (5 placeholder).

**Files**:
- Create: `bundled/idea-shaper/prompts/subagent/idea_shape_step1.md`
- Create: `bundled/idea-shaper/templates/IDEA.md.template`
- Delete: `bundled/idea-shaper/prompts/subagent/.gitkeep` (대체)
- Delete: `bundled/idea-shaper/templates/.gitkeep` (대체)

- [ ] **Step 1: idea_shape_step1.md 작성**

```markdown
You are filling out an IDEA.md template based on the main Claude's interview answers.

## Task

Read the template at:
  ~/.claude/skills/assemble/bundled/idea-shaper/templates/IDEA.md.template

Substitute these placeholders verbatim from the interview answers passed in {{TASK}} body
(do NOT paraphrase, do NOT expand, do NOT add commentary):

- `{{USER}}` — answer to "이 아이디어가 누구를 위한 거야?" (Q1)
- `{{PROBLEM}}` — answer to "지금 겪고 있는 가장 구체적인 문제?" (Q2)
- `{{WEDGE}}` — answer to "왜 지금 이 도구가 필요해?" (Q3)
- `{{NON_GOALS}}` — answer to "MVP에서 명시적으로 제외할 항목?" (Q4)
- `{{TASK_SUMMARY}}` — main Claude's free-text user prompt, ≤200 chars

Write the rendered file to:
  {{RUN_DIR}}/IDEA.md

## Constraints

1. NO commentary or recommendations beyond template fields.
2. NO scope expansion (V4 #6 — task scope은 seed가 아닌 contract).
3. Korean labels and section headers stay Korean. English placeholders translate to natural Korean if interview answer is in Korean.
4. {{TASK_SUMMARY}} ≤200 chars — truncate with `…` if longer.

## Output

Single file write to {{RUN_DIR}}/IDEA.md. NO other files modified.
```

- [ ] **Step 2: IDEA.md.template 작성**

```markdown
# IDEA — {{TASK_SUMMARY}}

## User
{{USER}}

## Problem
{{PROBLEM}}

## Wedge (왜 지금?)
{{WEDGE}}

## Non-goals (MVP 제외)
{{NON_GOALS}}

## Next step
plan-pack ★ 호출하여 PRD/ARCH/ADR/UI_GUIDE 작성 권장.
```

- [ ] **Step 3: .gitkeep 삭제**

```bash
rm bundled/idea-shaper/prompts/subagent/.gitkeep bundled/idea-shaper/templates/.gitkeep
```

- [ ] **Step 4: 파일 검증**

```bash
test -f bundled/idea-shaper/prompts/subagent/idea_shape_step1.md && echo OK
test -f bundled/idea-shaper/templates/IDEA.md.template && echo OK
grep -c "{{" bundled/idea-shaper/templates/IDEA.md.template
```

Expected: `OK` x2, placeholder count == 5.

- [ ] **Step 5: Commit**

```bash
git add bundled/idea-shaper/prompts bundled/idea-shaper/templates
git rm bundled/idea-shaper/prompts/subagent/.gitkeep bundled/idea-shaper/templates/.gitkeep 2>/dev/null
git commit -m "feat(v4-spike-xi-A2): idea-shaper prompt + IDEA.md template (5 placeholders)"
```

**Acceptance**:
- prompt 파일 ≤50 lines
- template 5 placeholder ({{USER}}/{{PROBLEM}}/{{WEDGE}}/{{NON_GOALS}}/{{TASK_SUMMARY}})
- HARNESS RULES preamble 자동 (dispatch 시 wrap_with_preamble으로 prepend됨, 여기 prompt 자체에는 박지 X)

**Sub-agent type**: `general-purpose`.

---

### A3 — idea-shaper template substitution test

**Goal**: pytest 신규 모듈 — IDEA.md.template 5 placeholder substitution + render correctness.

**Files**:
- Create: `tests/unit/test_idea_shaper_template.py`

- [ ] **Step 1: failing test 작성**

```python
"""Idea-shaper template substitution + render correctness tests."""
from __future__ import annotations

from pathlib import Path

ASSEMBLE_ROOT = Path.home() / ".claude" / "skills" / "assemble"
TEMPLATE_PATH = ASSEMBLE_ROOT / "bundled" / "idea-shaper" / "templates" / "IDEA.md.template"

EXPECTED_PLACEHOLDERS = {
    "{{USER}}",
    "{{PROBLEM}}",
    "{{WEDGE}}",
    "{{NON_GOALS}}",
    "{{TASK_SUMMARY}}",
}


def test_template_exists():
    assert TEMPLATE_PATH.is_file(), f"Missing template at {TEMPLATE_PATH}"


def test_template_has_all_5_placeholders():
    body = TEMPLATE_PATH.read_text(encoding="utf-8")
    found = {p for p in EXPECTED_PLACEHOLDERS if p in body}
    missing = EXPECTED_PLACEHOLDERS - found
    assert not missing, f"Missing placeholders: {missing}"


def test_template_has_5_sections():
    body = TEMPLATE_PATH.read_text(encoding="utf-8")
    section_headers = [line for line in body.splitlines() if line.startswith("## ")]
    assert len(section_headers) == 4, (
        f"Expected 4 H2 sections (User/Problem/Wedge/Non-goals/Next step), got {len(section_headers)}"
    )
    # H1 is "IDEA — {{TASK_SUMMARY}}", section 5 (Next step) is below content


def test_template_substitution_round_trip():
    body = TEMPLATE_PATH.read_text(encoding="utf-8")
    rendered = (
        body
        .replace("{{USER}}", "택시 기사")
        .replace("{{PROBLEM}}", "월말 정산이 손으로 한 시간")
        .replace("{{WEDGE}}", "기존 가계부 앱은 직장인용")
        .replace("{{NON_GOALS}}", "위젯 + 다국어")
        .replace("{{TASK_SUMMARY}}", "택시 기사 가계부")
    )
    assert "{{" not in rendered, f"Unsubstituted placeholder remains in rendered output:\n{rendered}"
    assert "택시 기사" in rendered
    assert "월말 정산" in rendered


def test_template_korean_section_headers():
    body = TEMPLATE_PATH.read_text(encoding="utf-8")
    for required in ("## User", "## Problem", "## Wedge", "## Non-goals", "## Next step"):
        assert required in body, f"Missing section header: {required}"
```

- [ ] **Step 2: 테스트 실행 — 통과 확인**

Run: `cd ~/.claude/skills/assemble && pytest tests/unit/test_idea_shaper_template.py -v`
Expected: 5 passed (template 이미 있으므로 즉시 통과 — 이는 regression-proof anchor).

- [ ] **Step 3: full pytest baseline 검증**

Run: `cd ~/.claude/skills/assemble && pytest 2>&1 | tail -3`
Expected: `764 passed` (759 + 5 new).

- [ ] **Step 4: Commit**

```bash
git add tests/unit/test_idea_shaper_template.py
git commit -m "test(v4-spike-xi-A3): idea-shaper template 5-placeholder anchor tests"
```

**Acceptance**:
- 5 신규 테스트 모두 green
- pytest 759 → 764
- placeholder substitution round-trip 깨끗 (`{{` 잔재 X)

**Sub-agent type**: `general-purpose`.

---

## Phase B — design-pack bundle (3 tasks)

### B1 — SKILL.md + 디렉토리 구조

**Goal**: bundled/design-pack/ + SKILL.md.

**Files**:
- Create: `bundled/design-pack/SKILL.md`
- Create: `bundled/design-pack/prompts/subagent/.gitkeep`
- Create: `bundled/design-pack/templates/.gitkeep`

- [ ] **Step 1: SKILL.md 작성** — Phase A1 패턴 따라 (4 sections, ≤120 lines, frontmatter `stages: design`, `grade: standard`)

본문 구조 (요약):
- 사용 시점 — PRD/IDEA 작성 후, 또는 독립 사용
- 워크플로 — Step 1 인터뷰 (4 Q: tone / color / components / typo) → Step 2 dispatch via general-purpose → DESIGN.md + ANTI_PATTERNS.md write
- 서브에이전트 매핑 — Step 2 = general-purpose (preferred 없음)
- 산출물 — DESIGN.md (디자인 시스템) + ANTI_PATTERNS.md (8 슬롭 표 고정)
- V4 정체성 합치 (V4 #9 / #13)
- 비범위 — Stitch / Figma 연동 / 자동 생성 (V5)

전체 SKILL.md는 A1 SKILL.md 동형. 자세한 본문은 spec § Bundle 2 참조.

- [ ] **Step 2: 디렉토리 + .gitkeep**

```bash
cd ~/.claude/skills/assemble
mkdir -p bundled/design-pack/prompts/subagent bundled/design-pack/templates
touch bundled/design-pack/prompts/subagent/.gitkeep bundled/design-pack/templates/.gitkeep
```

- [ ] **Step 3: pytest 실행**

Run: `cd ~/.claude/skills/assemble && pytest -x 2>&1 | tail -3`
Expected: 764 passed (regression 없음).

- [ ] **Step 4: Commit**

```bash
git add bundled/design-pack/
git commit -m "feat(v4-spike-xi-B1): bundled/design-pack/ — SKILL.md + dir structure"
```

**Acceptance**:
- SKILL.md ≤120 lines, 4 sections, frontmatter 정확
- pytest 764 그대로

**Sub-agent type**: `general-purpose`.

---

### B2 — prompt + 2 templates (DESIGN + ANTI_PATTERNS)

**Goal**: design_draft_step1.md + DESIGN.md.template (5 placeholder) + ANTI_PATTERNS.md.template (content-fixed 8 항목).

**Files**:
- Create: `bundled/design-pack/prompts/subagent/design_draft_step1.md`
- Create: `bundled/design-pack/templates/DESIGN.md.template`
- Create: `bundled/design-pack/templates/ANTI_PATTERNS.md.template`
- Delete: `bundled/design-pack/prompts/subagent/.gitkeep`, `bundled/design-pack/templates/.gitkeep`

- [ ] **Step 1: design_draft_step1.md 작성** (spec § Bundle 2 본문 그대로):

```markdown
You are filling DESIGN.md and ANTI_PATTERNS.md templates.

## Task

Read templates:
  ~/.claude/skills/assemble/bundled/design-pack/templates/DESIGN.md.template
  ~/.claude/skills/assemble/bundled/design-pack/templates/ANTI_PATTERNS.md.template

Substitute placeholders verbatim:
- `{{TONE}}` — answer to Q1 (디자인 톤)
- `{{COLOR_PRIMARY}}` — answer to Q2 (주 색상 hex)
- `{{COMPONENTS}}` — answer to Q3 (컴포넌트 라이브러리)
- `{{TYPO}}` — answer to Q4 (타이포)
- `{{IDEA_OR_PRD_SUMMARY}}` — 2-3 lines summary read from `{{RUN_DIR}}/IDEA.md` OR `{{RUN_DIR}}/PRD.md` (whichever exists; if both, prefer PRD). If neither exists, write `(no upstream design context)`.

Write rendered files:
- `{{RUN_DIR}}/DESIGN.md`
- `{{RUN_DIR}}/ANTI_PATTERNS.md` (template body content-fixed; only header `{{TONE}}` substitutes)

## Constraints

1. NO gradient-text / glass morphism / backdrop-blur / "혁신적" / "차세대" language in DESIGN.md.
2. ANTI_PATTERNS.md is the deny list — DESIGN.md must not violate it.
3. Korean labels stay Korean.
4. NO scope expansion beyond template fields.

## Output

Two file writes to {{RUN_DIR}}/. NO other files modified.
```

- [ ] **Step 2: DESIGN.md.template 작성** (spec § Bundle 2):

```markdown
# DESIGN — {{IDEA_OR_PRD_SUMMARY}}

## Tone
{{TONE}}

## Color
- Primary: {{COLOR_PRIMARY}}
- Neutral: 회색 #525252 / #f5f5f5 (light) / #171717 (dark)

## Typography
{{TYPO}}, 본문 16px / 헤더 24-32px / 라벨 14px.

## Components
{{COMPONENTS}}

## Layout
- 모바일 우선, max-width 600px on desktop
- Spacing scale: 4 / 8 / 16 / 24 / 32 / 48
- Radius: 8 (carded), 4 (input)

## Reference
ANTI_PATTERNS.md를 항상 참조 — AI 슬롭 8 항목 deny list.
```

- [ ] **Step 3: ANTI_PATTERNS.md.template 작성** (content-fixed 8 항목):

```markdown
# AI 슬롭 안티패턴 — {{TONE}} 디자인 deny list

이 8개 패턴은 모든 화면에서 *사용 금지*:

1. **gradient-text** (`background-clip: text`) — 모든 헤더가 무지개색
2. **glass morphism / backdrop-blur** — 모든 카드가 투명 유리
3. **보라색 일색** — primary가 보라/네온이면 즉시 의심
4. **의미 없는 emoji 폭격** — 모든 라벨에 ✨/🚀/💎
5. **Lorem ipsum / placeholder 잔재** — "Lorem ipsum" / "Sample text" / "placeholder@example.com"
6. **TODO 미완성** — "TODO: 채워주세요" / "// add later"
7. **광고 카피체** — "혁신적인" / "차세대" / "AI 기반"
8. **회색 그라데이션 박스** — 콘텐츠 자리 채우기용 빈 회색 카드

reviewer ★ 번들이 review 단계에서 이 표를 자동 검증할 수 있음 (현재는 수동 점검).
```

- [ ] **Step 4: .gitkeep 삭제 + 검증**

```bash
rm bundled/design-pack/prompts/subagent/.gitkeep bundled/design-pack/templates/.gitkeep
test -f bundled/design-pack/prompts/subagent/design_draft_step1.md && echo OK
test -f bundled/design-pack/templates/DESIGN.md.template && echo OK
test -f bundled/design-pack/templates/ANTI_PATTERNS.md.template && echo OK
grep -c "{{" bundled/design-pack/templates/DESIGN.md.template
grep -c "^[0-9]\." bundled/design-pack/templates/ANTI_PATTERNS.md.template
```

Expected: `OK` x3, DESIGN placeholder count == 5, ANTI_PATTERNS numbered list count == 8.

- [ ] **Step 5: Commit**

```bash
git add bundled/design-pack/prompts bundled/design-pack/templates
git rm bundled/design-pack/prompts/subagent/.gitkeep bundled/design-pack/templates/.gitkeep 2>/dev/null
git commit -m "feat(v4-spike-xi-B2): design-pack prompt + DESIGN.md + ANTI_PATTERNS.md templates"
```

**Acceptance**:
- prompt ≤60 lines
- DESIGN.md.template 5 placeholder
- ANTI_PATTERNS.md.template 8 numbered items + 1 header placeholder
- ANTI_PATTERNS body content-fixed (재현 시 8 entries 그대로)

**Sub-agent type**: `general-purpose`.

---

### B3 — design-pack template substitution test

**Goal**: pytest 신규 모듈 — DESIGN.md + ANTI_PATTERNS.md placeholder + 8 anti-pattern verbatim.

**Files**:
- Create: `tests/unit/test_design_pack_template.py`

- [ ] **Step 1: 테스트 작성**

```python
"""Design-pack template substitution + 8-entry anti-pattern verbatim tests."""
from __future__ import annotations

from pathlib import Path

ASSEMBLE_ROOT = Path.home() / ".claude" / "skills" / "assemble"
DESIGN_TEMPLATE = ASSEMBLE_ROOT / "bundled" / "design-pack" / "templates" / "DESIGN.md.template"
ANTI_PATTERNS_TEMPLATE = ASSEMBLE_ROOT / "bundled" / "design-pack" / "templates" / "ANTI_PATTERNS.md.template"

DESIGN_PLACEHOLDERS = {
    "{{TONE}}",
    "{{COLOR_PRIMARY}}",
    "{{COMPONENTS}}",
    "{{TYPO}}",
    "{{IDEA_OR_PRD_SUMMARY}}",
}

EXPECTED_ANTI_PATTERNS = (
    "gradient-text",
    "glass morphism",
    "보라색 일색",
    "emoji 폭격",
    "Lorem ipsum",
    "TODO 미완성",
    "광고 카피체",
    "회색 그라데이션 박스",
)


def test_design_template_exists():
    assert DESIGN_TEMPLATE.is_file()


def test_anti_patterns_template_exists():
    assert ANTI_PATTERNS_TEMPLATE.is_file()


def test_design_template_has_all_5_placeholders():
    body = DESIGN_TEMPLATE.read_text(encoding="utf-8")
    found = {p for p in DESIGN_PLACEHOLDERS if p in body}
    missing = DESIGN_PLACEHOLDERS - found
    assert not missing, f"Missing in DESIGN.md.template: {missing}"


def test_anti_patterns_has_tone_header_placeholder():
    body = ANTI_PATTERNS_TEMPLATE.read_text(encoding="utf-8")
    assert "{{TONE}}" in body, "ANTI_PATTERNS header must use {{TONE}}"


def test_anti_patterns_has_8_verbatim_entries():
    body = ANTI_PATTERNS_TEMPLATE.read_text(encoding="utf-8")
    for entry in EXPECTED_ANTI_PATTERNS:
        assert entry in body, f"Missing anti-pattern entry: {entry}"


def test_anti_patterns_numbered_1_through_8():
    body = ANTI_PATTERNS_TEMPLATE.read_text(encoding="utf-8")
    for n in range(1, 9):
        assert f"{n}. **" in body, f"Missing numbered entry {n}."


def test_design_template_substitution_round_trip():
    body = DESIGN_TEMPLATE.read_text(encoding="utf-8")
    rendered = (
        body
        .replace("{{TONE}}", "미니멀 모노")
        .replace("{{COLOR_PRIMARY}}", "#2563eb")
        .replace("{{COMPONENTS}}", "shadcn/ui")
        .replace("{{TYPO}}", "Pretendard")
        .replace("{{IDEA_OR_PRD_SUMMARY}}", "택시 기사 가계부 MVP")
    )
    assert "{{" not in rendered
    assert "미니멀 모노" in rendered
    assert "#2563eb" in rendered


def test_design_template_no_slop_phrases():
    body = DESIGN_TEMPLATE.read_text(encoding="utf-8")
    forbidden = ("gradient-text", "혁신적", "차세대")
    for word in forbidden:
        assert word not in body, f"DESIGN.md.template must not contain {word!r}"
```

- [ ] **Step 2: 테스트 실행**

Run: `cd ~/.claude/skills/assemble && pytest tests/unit/test_design_pack_template.py -v`
Expected: 8 passed.

- [ ] **Step 3: full baseline 검증**

Run: `cd ~/.claude/skills/assemble && pytest 2>&1 | tail -3`
Expected: 772 passed (764 + 8).

- [ ] **Step 4: Commit**

```bash
git add tests/unit/test_design_pack_template.py
git commit -m "test(v4-spike-xi-B3): design-pack template + 8-anti-pattern verbatim tests"
```

**Acceptance**:
- 8 신규 테스트 green
- 8 anti-pattern verbatim regression-proof
- DESIGN.md.template은 본문에 슬롭 단어 자체 포함 안 함 (의미상 안 됨)

**Sub-agent type**: `general-purpose`.

---

## Phase C — guardian bundle (2 tasks)

### C1 — SKILL.md + GUARDIAN.md template

**Goal**: bundled/guardian/ — dispatch X (메인 직접 IO, V4 #9 예외).

**Files**:
- Create: `bundled/guardian/SKILL.md`
- Create: `bundled/guardian/templates/GUARDIAN.md.template`

- [ ] **Step 1: SKILL.md 작성** (≤100 lines, dispatch X 명시)

```markdown
---
name: guardian
description: 위험 명령 차단 + 디렉토리 freeze 가이드 문서 작성하는 표준 번들 (V4 safety stage)
stages: safety
grade: standard
---

# guardian — safety stage 표준 번들

V4 결정 #1 라인업의 safety stage. 사용자가 destructive 작업 직전에 의식적으로 거치는 *참조 가이드 문서* GUARDIAN.md 작성. V4는 hook 차단 시스템이 아님 — 자동 거부 X, 사용자 명시 체크리스트 작성 후 다른 번들에서 read해서 경고 표시.

## V4 #9 예외 적용 (메인 직접 IO)

V4 결정 #9 — "메인 Claude는 직접 작업 X. 단순 IO·AskUserQuestion만 예외".
guardian 워크플로 = (a) AskUserQuestion + (b) 템플릿 substitution + Write.
dispatch overhead가 실 작업보다 큼 → ALLOWED_PROMPT_FILES에 entry 추가 X.

## 사용 시점

- 새 세션 시작 직후 (destructive 작업 *전*)
- 또는 builder / shipper / debugger 번들 작업 시작 직전 안전 가이드 셋업

## 워크플로

### Step 1: 인터뷰 (메인 직접 — AskUserQuestion 묶음 ×2)

**1차 묶음 — freeze + 위험 명령:**
- Q1: "freeze 할 디렉토리 (이번 작업 동안 *수정 금지*)?"
- Q2: "이번 작업에서 *명시적 deny* 명령?"

**2차 묶음 — 계획:**
- Q3: "계획된 destructive 작업 (이번 세션 안 일어날 일)?"

### Step 2: 메인이 GUARDIAN.md 작성 (Write 직접)

메인 Claude가 `bundled/guardian/templates/GUARDIAN.md.template` read하고 4 placeholder ({{TIMESTAMP}}/{{FROZEN_DIRS}}/{{DENY_COMMANDS}}/{{PLANNED_DESTRUCTIVE}}) substitute 후 `{{RUN_DIR}}/GUARDIAN.md`로 Write.

## 서브에이전트 매핑 표

| 단계 | role | 폴백 보장 |
|---|---|---|
| 1. 인터뷰 | (메인 직접) | — |
| 2. GUARDIAN.md 작성 | (메인 직접 — V4 #9 예외) | — |

## 산출물

- `<run_dir>/GUARDIAN.md` (활성 freeze + deny 명령 + 계획 작업 + 사용자 체크리스트)

## 다른 번들과의 통합

builder / shipper / debugger 번들이 destructive 작업 직전 GUARDIAN.md read하면 콘솔에 1줄 경고 표시 (V5에서 자동 차단 hook 추가 후보). V4는 가이드 문서 + 사용자 의식만.

## V4 정체성 합치

- ✅ V4 #9 — 단순 IO·AskUserQuestion 예외 적용 (dispatch X)
- ✅ orchestrator-only 정신 유지 — 메인이 무거운 분석 안 함, IO만

## 비범위 (V4)

- ❌ 자동 차단 hook (PreToolUse) — V5
- ❌ 명령 패턴 자동 학습 — V5
- ❌ 백업 자동 실행 — V5
```

- [ ] **Step 2: GUARDIAN.md.template 작성**

```markdown
# GUARDIAN — 안전 가이드 ({{TIMESTAMP}})

## Frozen directories (이 세션 *수정 금지*)
{{FROZEN_DIRS}}

## Deny commands (이 세션 *실행 금지*)
{{DENY_COMMANDS}}

## Planned destructive operations (사전 알림)
{{PLANNED_DESTRUCTIVE}}

## 사용자 체크리스트 (destructive 작업 직전 매번)

- [ ] 백업 (git commit / DB dump / file copy) 완료
- [ ] dry-run 실행 (`--dry-run` / `-n` 플래그)
- [ ] 영향 범위 확인 (touched files / affected rows count)
- [ ] 롤백 절차 1줄로 적을 수 있는가
- [ ] 사용자 (형) 본인 명시적 승인

## 다른 번들에 알림

builder / shipper / debugger 번들이 destructive 작업 직전 이 파일을 read하면 콘솔에 1줄 경고 표시 (V5에서 자동 차단 hook 추가 후보).
```

- [ ] **Step 3: 디렉토리 + 검증**

```bash
mkdir -p bundled/guardian/templates
test -f bundled/guardian/SKILL.md && echo OK
test -f bundled/guardian/templates/GUARDIAN.md.template && echo OK
grep -c "{{" bundled/guardian/templates/GUARDIAN.md.template
```

Expected: `OK` x2, placeholder count == 4.

- [ ] **Step 4: pytest baseline**

Run: `cd ~/.claude/skills/assemble && pytest -x 2>&1 | tail -3`
Expected: 772 passed (regression 없음 — 아직 wiring X).

- [ ] **Step 5: Commit**

```bash
git add bundled/guardian/
git commit -m "feat(v4-spike-xi-C1): bundled/guardian/ — SKILL.md + GUARDIAN.md template (V4 #9 exception)"
```

**Acceptance**:
- SKILL.md ≤100 lines (no dispatch, simpler)
- frontmatter `stages: safety`, `grade: standard`
- template 4 placeholder
- pytest 772 그대로

**Sub-agent type**: `general-purpose`.

---

### C2 — guardian template test

**Goal**: pytest 신규 모듈 — GUARDIAN.md placeholder + 사용자 체크리스트 5 항목.

**Files**:
- Create: `tests/unit/test_guardian_template.py`

- [ ] **Step 1: 테스트 작성**

```python
"""Guardian template substitution + checklist coverage tests."""
from __future__ import annotations

from pathlib import Path

ASSEMBLE_ROOT = Path.home() / ".claude" / "skills" / "assemble"
TEMPLATE_PATH = ASSEMBLE_ROOT / "bundled" / "guardian" / "templates" / "GUARDIAN.md.template"

EXPECTED_PLACEHOLDERS = {
    "{{TIMESTAMP}}",
    "{{FROZEN_DIRS}}",
    "{{DENY_COMMANDS}}",
    "{{PLANNED_DESTRUCTIVE}}",
}

EXPECTED_CHECKLIST_ITEMS = (
    "백업",
    "dry-run",
    "영향 범위",
    "롤백",
    "명시적 승인",
)


def test_template_exists():
    assert TEMPLATE_PATH.is_file()


def test_template_has_4_placeholders():
    body = TEMPLATE_PATH.read_text(encoding="utf-8")
    found = {p for p in EXPECTED_PLACEHOLDERS if p in body}
    missing = EXPECTED_PLACEHOLDERS - found
    assert not missing, f"Missing placeholders: {missing}"


def test_template_has_5_checklist_items():
    body = TEMPLATE_PATH.read_text(encoding="utf-8")
    for item in EXPECTED_CHECKLIST_ITEMS:
        assert item in body, f"Missing checklist item: {item}"


def test_template_has_5_checkbox_lines():
    body = TEMPLATE_PATH.read_text(encoding="utf-8")
    checkboxes = [line for line in body.splitlines() if line.strip().startswith("- [ ]")]
    assert len(checkboxes) == 5, f"Expected 5 checkbox items, got {len(checkboxes)}"


def test_template_substitution_round_trip():
    body = TEMPLATE_PATH.read_text(encoding="utf-8")
    rendered = (
        body
        .replace("{{TIMESTAMP}}", "2026-05-04T15:00:00Z")
        .replace("{{FROZEN_DIRS}}", "- ~/Documents\n- /etc")
        .replace("{{DENY_COMMANDS}}", "- git push --force\n- rm -rf")
        .replace("{{PLANNED_DESTRUCTIVE}}", "- DB 마이그레이션 (다음 화요일)")
    )
    assert "{{" not in rendered
    assert "~/Documents" in rendered
    assert "git push --force" in rendered
```

- [ ] **Step 2: 테스트 실행**

Run: `cd ~/.claude/skills/assemble && pytest tests/unit/test_guardian_template.py -v`
Expected: 5 passed.

- [ ] **Step 3: full baseline 검증**

Run: `cd ~/.claude/skills/assemble && pytest 2>&1 | tail -3`
Expected: 777 passed (772 + 5).

- [ ] **Step 4: Commit**

```bash
git add tests/unit/test_guardian_template.py
git commit -m "test(v4-spike-xi-C2): guardian template + checklist coverage tests"
```

**Acceptance**:
- 5 신규 테스트 green
- placeholder + 5 checklist 모두 verbatim regression-proof

**Sub-agent type**: `general-purpose`.

---

## Phase D — wiring (3 bundles 동시, atomic commit)

### D — harness + inventory + contracts.json (single atomic task)

**Goal**: ALLOWED_PROMPT_FILES + _BUNDLES + _BUNDLED_DIR_TO_STAGE (BOTH harness + inventory) + _PROMPT_TO_STAGE + contracts.json — 단일 commit (Spike X D1+D2+D3+D4 precedent).

**Files**:
- Modify: `server/harness.py` (ALLOWED_PROMPT_FILES + _BUNDLES + _BUNDLED_DIR_TO_STAGE + _PROMPT_TO_STAGE)
- Modify: `server/inventory.py` (_BUNDLED_DIR_TO_STAGE — universal-defense convention sync)
- Modify: `tests/contracts/contracts.json` (+3 entries)

- [ ] **Step 1: 현재 wiring 위치 파악**

Run: `cd ~/.claude/skills/assemble && grep -n "ALLOWED_PROMPT_FILES\|_BUNDLES\|_BUNDLED_DIR_TO_STAGE\|_PROMPT_TO_STAGE" server/harness.py | head -10`
Expected: ALLOWED_PROMPT_FILES (line ~50), _BUNDLES tuple (line ~150), _BUNDLED_DIR_TO_STAGE (line ~200), _PROMPT_TO_STAGE (line ~280).

Run: `grep -n "_BUNDLED_DIR_TO_STAGE" server/inventory.py`
Expected: 1 location.

- [ ] **Step 2: server/harness.py 수정 — ALLOWED_PROMPT_FILES**

idea-shaper + design-pack 2 entries 추가 (guardian 추가 X — dispatch 안 함):

```python
# 기존 ALLOWED_PROMPT_FILES 마지막에 추가
"bundled/idea-shaper/prompts/subagent/idea_shape_step1.md",
"bundled/design-pack/prompts/subagent/design_draft_step1.md",
```

- [ ] **Step 3: server/harness.py — _BUNDLES**

```python
_BUNDLES = (
    "plan-pack",
    "debugger",
    "builder",
    "reviewer",
    "verifier",
    "shipper",
    "keeper",
    "idea-shaper",
    "design-pack",
    "guardian",
)
```

- [ ] **Step 4: server/harness.py — _BUNDLED_DIR_TO_STAGE**

```python
_BUNDLED_DIR_TO_STAGE = {
    # ... 기존 7 entries unchanged ...
    "idea-shaper": "discover",
    "design-pack": "design",
    "guardian": "safety",
}
```

- [ ] **Step 5: server/harness.py — _PROMPT_TO_STAGE**

```python
_PROMPT_TO_STAGE = {
    # ... 기존 39 entries unchanged ...
    "bundled/idea-shaper/prompts/subagent/idea_shape_step1.md": "discover",
    "bundled/design-pack/prompts/subagent/design_draft_step1.md": "design",
    # guardian 없음
}
```

- [ ] **Step 6: server/inventory.py — _BUNDLED_DIR_TO_STAGE sync**

```python
_BUNDLED_DIR_TO_STAGE = {
    # ... 기존 entries unchanged (universal-defense convention) ...
    "idea-shaper": "discover",
    "design-pack": "design",
    "guardian": "safety",
}
```

- [ ] **Step 7: tests/contracts/contracts.json — +3 entries**

```json
{
  "spike-xi-idea-shaper": {
    "phrase": "spike XI: idea-shaper standard bundle",
    "checks": [
      "ALLOWED_PROMPT_FILES contains 'bundled/idea-shaper/prompts/subagent/idea_shape_step1.md'",
      "_BUNDLED_DIR_TO_STAGE['idea-shaper'] == 'discover' (BOTH harness.py + inventory.py)",
      "templates/IDEA.md.template exists with all 5 placeholders",
      "SKILL.md ≤120 lines"
    ]
  },
  "spike-xi-design-pack": {
    "phrase": "spike XI: design-pack standard bundle",
    "checks": [
      "ALLOWED_PROMPT_FILES contains 'bundled/design-pack/prompts/subagent/design_draft_step1.md'",
      "_BUNDLED_DIR_TO_STAGE['design-pack'] == 'design' (BOTH harness.py + inventory.py)",
      "templates/DESIGN.md.template + ANTI_PATTERNS.md.template both exist with placeholders + 8 verbatim entries",
      "SKILL.md ≤120 lines"
    ]
  },
  "spike-xi-guardian": {
    "phrase": "spike XI: guardian standard bundle",
    "checks": [
      "_BUNDLED_DIR_TO_STAGE['guardian'] == 'safety' (BOTH harness.py + inventory.py)",
      "templates/GUARDIAN.md.template exists with 4 placeholders",
      "guardian has NO entries in ALLOWED_PROMPT_FILES (V4 #9 exception)",
      "SKILL.md ≤100 lines"
    ]
  }
}
```

- [ ] **Step 8: pytest 실행 (bidirectional integrity 확인)**

Run: `cd ~/.claude/skills/assemble && pytest -x 2>&1 | tail -10`
Expected: 모두 green (777 baseline + 0 — wiring 자체는 신규 테스트 X). 만약 bidirectional integrity test가 broken하면:
- `set(_PROMPT_TO_STAGE) == set(ALLOWED_PROMPT_FILES)` 강제 — guardian은 양쪽 모두 없음, idea-shaper + design-pack은 양쪽 모두 있음 ✅

- [ ] **Step 9: contracts test 실행**

Run: `cd ~/.claude/skills/assemble && pytest tests/contracts/ -v 2>&1 | tail -10`
Expected: 신규 3 entries 통과 (또는 existing test_contracts.py가 contracts.json을 자동 enumerate).

- [ ] **Step 10: Commit (atomic)**

```bash
git add server/harness.py server/inventory.py tests/contracts/contracts.json
git commit -m "feat(v4-spike-xi-D): wire 3 standard bundles — allowlist + _BUNDLES + _BUNDLED_DIR_TO_STAGE + contracts.json"
```

**Acceptance**:
- ALLOWED_PROMPT_FILES +2 entries (idea-shaper + design-pack)
- _BUNDLES +3 entries
- _BUNDLED_DIR_TO_STAGE +3 entries (BOTH harness.py + inventory.py)
- _PROMPT_TO_STAGE +2 entries
- contracts.json +3 entries
- pytest baseline 그대로 (777, 신규 wiring 테스트는 contracts test가 자동 검증)
- bidirectional integrity invariant 보존: `set(_PROMPT_TO_STAGE) == set(ALLOWED_PROMPT_FILES)`
- canonical preamble v3 sha 변경 X

**Sub-agent type**: `general-purpose`.

---

## Phase E — overall code review (1 task)

### E — superpowers:code-reviewer overall review

**Goal**: 전체 spec + 모든 Phase A-D commit 대상 final overall review.

**Files**:
- Create: `docs/dogfood/spike-xi-overall-review.md`

- [ ] **Step 1: superpowers:code-reviewer 호출**

dispatch via Agent tool with `subagent_type=superpowers:code-reviewer`. 입력:
- spec: `docs/specs/2026-05-04-v4-spike-xi-design.md`
- plan: `docs/plans/2026-05-04-v4-spike-xi.md`
- 모든 Phase A-D commits (7 commits A1-A3 + B1-B3 + C1-C2 + D = 9 commits)
- 검증 항목:
  - 표준 등급 정체성 유지 (★ 안 됨, Bash 0)
  - V4 #1 라인업 10/10 채움 정확성
  - V4 #9 예외 (guardian) 정당화 충분
  - V4 정체성 보호 (preamble sha / ALLOW_LIST / 기존 7 ★ bundle prompts unchanged)
  - bidirectional integrity (`_PROMPT_TO_STAGE` ↔ `ALLOWED_PROMPT_FILES`)
  - universal-defense convention (harness ↔ inventory `_BUNDLED_DIR_TO_STAGE` sync)
  - SKILL.md ≤120 lines (idea/design) / ≤100 lines (guardian)
  - pytest +18 신규 테스트 모두 green (5+8+5)

- [ ] **Step 2: 출력 파일 작성**

`docs/dogfood/spike-xi-overall-review.md` — review verdict + carryforward 목록 (있으면).

- [ ] **Step 3: 만약 carryforward 발견 시**

Critical issue 발견되면 즉시 fix commit (`fix(v4-spike-xi-E): apply <issue>`). Important 이하는 carryforward로 메모리에 기록 후 ship.

- [ ] **Step 4: Codex retro 결정**

표준 번들이고 보안 surface 없음 (Bash 0). E1 review에서 critical issue 1+ 발견되면 그때 codex retro 호출 결정. 기본 SKIP.

- [ ] **Step 5: Commit**

```bash
git add docs/dogfood/spike-xi-overall-review.md
git commit -m "fix(v4-spike-xi-E): apply E1 overall review — <issues or 'no carryforward'>"
```

**Acceptance**:
- E1 review 문서 존재 + verdict 명시
- critical 0건 (또는 즉시 fix)
- carryforward는 메모리 ship 시점에 기록

**Sub-agent type**: `superpowers:code-reviewer`.

---

## Phase F — B-16 dogfood + ship (2 tasks)

### F1 — B-16 dogfood

**Goal**: 3 sub-runs self-execute로 12 AC 검증.

**Files**:
- Create: `docs/dogfood/spike-xi-b16.md`

- [ ] **Step 1: dogfood 시작**

dogfood 디렉토리: `/tmp/spike-xi-b16/` (각 sub-run 별 sub-dir).

```bash
mkdir -p /tmp/spike-xi-b16/{idea,design,guardian}
```

- [ ] **Step 2: Sub-run 1 — idea-shaper happy**

Python 인라인 스크립트로 검증 (Spike X B-15 self-execute 패턴):

```bash
cd ~/.claude/skills/assemble && python3 -c "
import sys
sys.path.insert(0, '.')
from server import harness, inventory

# (1) inventory bundled=true
skills = inventory.scan_skills()
idea = next(s for s in skills if s.path.name == 'idea-shaper')
assert idea.bundled is True, f'idea-shaper bundled={idea.bundled}'
print('AC1 idea-shaper bundled=true PASS')

# (2) menu ★ prefix (rendered)
from server.menu import render_menu_block
menu = render_menu_block(skills, stage='discover')
assert '★' in menu and 'idea-shaper' in menu
print('AC2 idea-shaper ★ prefix PASS')

# (3) allowlist gate
prompt_path = harness._resolve_prompt_path('bundled/idea-shaper/prompts/subagent/idea_shape_step1.md')
assert prompt_path is not None
print('AC3 idea-shaper allowlist gate PASS')

# (6) _BUNDLED_DIR_TO_STAGE harness
assert harness._BUNDLED_DIR_TO_STAGE['idea-shaper'] == 'discover'
print('AC6a idea-shaper harness map PASS')

# (7) _BUNDLED_DIR_TO_STAGE inventory
assert inventory._BUNDLED_DIR_TO_STAGE['idea-shaper'] == 'discover'
print('AC7a idea-shaper inventory map PASS')

# (8) _PROMPT_TO_STAGE
assert harness._PROMPT_TO_STAGE['bundled/idea-shaper/prompts/subagent/idea_shape_step1.md'] == 'discover'
print('AC8a idea-shaper prompt map PASS')
"
```

Expected: 모두 PASS.

- [ ] **Step 3: Sub-run 2 — design-pack happy**

동일 패턴, design-pack에 대해 AC1/2/4/6b/7b/8b 검증.

- [ ] **Step 4: Sub-run 3 — guardian happy**

동일 패턴, guardian에 대해 AC1/2/5/6c/7c 검증 (AC8c는 없음 — _PROMPT_TO_STAGE에 entry 없음).

```python
# (5) guardian no allowlist entry
prompt_files = list(harness.ALLOWED_PROMPT_FILES)
assert not any('bundled/guardian/' in p for p in prompt_files)
print('AC5 guardian no allowlist entry PASS')
```

- [ ] **Step 5: AC9 — preamble sha**

```bash
python3 -c "
import sys
sys.path.insert(0, '.')
from server.harness import _CANONICAL_PREAMBLE_SHA
assert _CANONICAL_PREAMBLE_SHA == '8d22a29c9712d2c0c05bc2145ca5ad56c7e19705087dde4dd625908f7ec089a9', f'Preamble sha changed: {_CANONICAL_PREAMBLE_SHA}'
print('AC9 preamble sha PASS')
"
```

- [ ] **Step 6: AC10 — bidirectional integrity**

```bash
python3 -c "
import sys
sys.path.insert(0, '.')
from server.harness import ALLOWED_PROMPT_FILES, _PROMPT_TO_STAGE
assert set(_PROMPT_TO_STAGE.keys()) == set(ALLOWED_PROMPT_FILES), f'mismatch: prompt_only={set(_PROMPT_TO_STAGE) - set(ALLOWED_PROMPT_FILES)}, allow_only={set(ALLOWED_PROMPT_FILES) - set(_PROMPT_TO_STAGE)}'
print('AC10 bidirectional integrity PASS')
"
```

- [ ] **Step 7: AC11 — contracts.json 3 entries**

```bash
pytest tests/contracts/ -v 2>&1 | grep -E "spike-xi|PASS"
```

Expected: 3 신규 entries 모두 PASS.

- [ ] **Step 8: AC12 — full baseline**

```bash
pytest 2>&1 | tail -3
```

Expected: 777+ green (759 baseline + 5 + 8 + 5 = 777, +0~ for wiring).

- [ ] **Step 9: docs/dogfood/spike-xi-b16.md 작성**

문서 형식 (Spike X b15 패턴):
- 12 AC 표 (PASS/FAIL/PARTIAL)
- 3 sub-run 결과 + wall-time
- 만약 carryforward 발견되면 명시

- [ ] **Step 10: Commit**

```bash
git add docs/dogfood/spike-xi-b16.md
git commit -m "test(v4-spike-xi-F1): B-16 dogfood — 12 AC, 3 sub-runs self-execute"
```

**Acceptance**:
- 12 AC PASS (또는 PARTIAL 1개 이내, FAIL 0)
- wall-time ≤30s
- 표준 번들 3개 모두 dispatchable (또는 메인 직접 IO)

**Sub-agent type**: `general-purpose`.

---

### F2 — Ship commit (CHANGELOG + memory + MEMORY.md index)

**Goal**: 최종 출하.

**Files**:
- Modify: `CHANGELOG.md`
- Create: `~/.claude/projects/-Users-yonghaekim-my-folder/memory/project_assemble_v4_spike_xi.md`
- Modify: `~/.claude/projects/-Users-yonghaekim-my-folder/memory/MEMORY.md`

- [ ] **Step 1: CHANGELOG.md 업데이트**

`[Unreleased]` 헤더에 `+ Spike XI` 추가, 새 섹션:

```markdown
### V4 Spike XI (2026-05-04, B-16 dogfood ship — 3 standard bundles, V4 결정 #1 라인업 10/10 완성)

**3 standard-grade bundles closing V4 결정 #1 lineup gap. Standard 등급 = 1-step single-dispatch (또는 메인 직접 IO), Bash 권한 없음, Codex retro 선택. 7 ★ bundle prompts UNCHANGED — only ALLOWED_PROMPT_FILES +2, _BUNDLES +3, _BUNDLED_DIR_TO_STAGE +3 (BOTH harness + inventory).**

### Added (Spike XI)

- `bundled/idea-shaper/` standard bundle: SKILL.md (≤120 lines, frontmatter `stages: discover`, `grade: standard`) + 1 sub-agent prompt (idea_shape_step1.md) + 1 template (IDEA.md.template, 5 placeholders {{USER}}/{{PROBLEM}}/{{WEDGE}}/{{NON_GOALS}}/{{TASK_SUMMARY}})
- `bundled/design-pack/` standard bundle: SKILL.md + 1 sub-agent prompt (design_draft_step1.md) + 2 templates (DESIGN.md.template 5 placeholders + ANTI_PATTERNS.md.template content-fixed 8 anti-pattern entries verbatim)
- `bundled/guardian/` standard bundle (V4 #9 exception — main-direct IO, NO dispatch): SKILL.md (≤100 lines) + 1 template (GUARDIAN.md.template 4 placeholders + 5-checkbox checklist)
- ALLOWED_PROMPT_FILES +2 (idea_shape_step1.md + design_draft_step1.md), _BUNDLES +3 (idea-shaper + design-pack + guardian), _BUNDLED_DIR_TO_STAGE +3 (discover/design/safety) BOTH harness.py + inventory.py (universal-defense convention sync)
- _PROMPT_TO_STAGE +2 (idea-shaper:discover + design-pack:design; guardian has no prompts)
- `tests/contracts/contracts.json` +3 entries (spike-xi-idea-shaper / spike-xi-design-pack / spike-xi-guardian)
- `tests/unit/test_idea_shaper_template.py` — 5 placeholder anchor tests
- `tests/unit/test_design_pack_template.py` — 8 placeholder + anti-pattern verbatim tests
- `tests/unit/test_guardian_template.py` — 5 placeholder + checklist coverage tests
- `docs/dogfood/spike-xi-overall-review.md` — Phase E1 superpowers:code-reviewer overall review
- `docs/dogfood/spike-xi-b16.md` — B-16 self-execute dogfood 12/12 PASS, ≤30s wall-time

### Test count

- Baseline (Spike X cleanup `b55369a`): 759 passed
- Spike XI final: **777+ passed, 0 failed** (+18 new tests)
```

- [ ] **Step 2: 메모리 작성**

`~/.claude/projects/-Users-yonghaekim-my-folder/memory/project_assemble_v4_spike_xi.md`:

```markdown
---
name: /assemble V4 Spike XI — 3 standard bundles (idea-shaper / design-pack / guardian)
description: Spike XI ship 완료 (2026-05-04). 표준 등급 번들 3개 — V4 결정 #1 라인업 10/10 완성. discover/design/safety stages 자급자족 보증. master HEAD `<ship>`, 10 commits, pytest 777+. B-16 dogfood 12/12 PASS.
type: project
parent: project_assemble_v4_spec.md
originSessionId: current
---

# /assemble V4 Spike XI — 3 standard bundles

## Status: SHIPPED (2026-05-04)

- master HEAD: ship commit
- 9 implementation commits + 1 ship commit (10 total)
- pytest 759 → **777+ passed** (+18: 5 idea + 8 design + 5 guardian)
- Spike XI = 표준 등급 번들 3개 추가, V4 결정 #1 라인업 10/10 완성
- ★ 번들 변경 X — 7 ★ bundle prompts (plan-pack/debugger/builder/reviewer/verifier/shipper/keeper) 모두 unchanged

## 표준 vs ★ 등급 차이 (operationally validated)

- 표준 = 1-step single-dispatch (또는 메인 직접 IO) + Bash 0 + Codex retro 선택 + 가벼운 dogfood
- ★ = 4-step pipeline + Bash sub-agent grant + Codex retro mandatory + 4-sub-run dogfood + verdict + REPORT

## 3 bundles 산출물

| stage | bundle | 산출물 | sub-agent matrix |
|---|---|---|---|
| discover | idea-shaper | IDEA.md (5 sections) | 인터뷰 + 1 dispatch (text-summarize) |
| design | design-pack | DESIGN.md + ANTI_PATTERNS.md (8 verbatim) | 인터뷰 + 1 dispatch (general-purpose) |
| safety | guardian | GUARDIAN.md (4 placeholder + 5 checklist) | 인터뷰 + 메인 직접 Write (V4 #9 예외) |

## V4 정체성 보호 (변경 X)

- ✅ Spike I~X core contracts 모두
- ✅ canonical preamble v3 sha unchanged (`8d22a29c9712d2c0c05bc2145ca5ad56c7e19705087dde4dd625908f7ec089a9`)
- ✅ 7 ★ bundle prompts unchanged
- ✅ ALLOW_LIST = {v1, v2, v3} unchanged
- ✅ orchestrator-only V4 #9 — main never executes Bash; guardian의 메인 Write는 V4 #9 명시 예외 ("단순 IO·AskUserQuestion만 예외")
- ✅ universal-defense convention: _BUNDLED_DIR_TO_STAGE BOTH maps sync
- ✅ bidirectional integrity: set(_PROMPT_TO_STAGE) == set(ALLOWED_PROMPT_FILES)

## V4 결정 #1 라인업 (10/10 완성)

| stage | 번들 | 등급 | spike |
|---|---|---|---|
| discover | idea-shaper | 표준 | XI |
| plan | plan-pack | ★ | B-1~5 + I~III |
| design | design-pack | 표준 | XI |
| execute | builder | ★ | V |
| debug | debugger | ★ | IV |
| review | reviewer | ★ | VI |
| verify | verifier | ★ | VIII |
| ship | shipper | ★ | IX |
| safety | guardian | 표준 | XI |
| meta | keeper | ★ | X |

V4 결정 #6 (빈손 컴 dogfood가 V4 출시 게이트) 직전 마지막 단계. Spike XII = /assemble eject 명령. Spike XIII = 빈손 컴 dogfood (V4 출시).

## Spike XII candidates

- `/assemble eject <bundle>` 명령 (번들 → 사용자 스킬 변환)
- `roles.json` 파일 도입 (메모리 spec → 실제 파일)
- F4 perf collapse (reviewer ★ deterministic shell)

## Source

- Spec: `docs/specs/2026-05-04-v4-spike-xi-design.md`
- Plan: `docs/plans/2026-05-04-v4-spike-xi.md`
- E1 review: `docs/dogfood/spike-xi-overall-review.md`
- B-16 dogfood: `docs/dogfood/spike-xi-b16.md`
- Parent: `project_assemble_v4_spec.md`
- Sibling: `project_assemble_v4_spike_x.md`
```

- [ ] **Step 3: MEMORY.md index 업데이트**

기존 Spike X entry 아래에 추가:

```markdown
- [V4 Spike XI — 3 standard bundles (idea-shaper / design-pack / guardian)](project_assemble_v4_spike_xi.md): SHIP 2026-05-04. V4 결정 #1 라인업 10/10 완성. 표준 등급 번들 3개 (discover/design/safety stages). 7 ★ bundles unchanged. pytest 777+. B-16 12/12 PASS. Spike XII = /assemble eject 후보.
```

- [ ] **Step 4: 최종 검증**

```bash
cd ~/.claude/skills/assemble && pytest 2>&1 | tail -3
git status
```

Expected: 777+ passed, working tree clean (commit 직전).

- [ ] **Step 5: Ship commit**

```bash
git add CHANGELOG.md
git commit -m "docs(v4-spike-xi): ship — V4 Spike XI released, 3 standard bundles (idea-shaper / design-pack / guardian) — V4 결정 #1 라인업 10/10 완성"
```

- [ ] **Step 6: 메모리 commit (별도)**

```bash
cd ~/.claude/projects/-Users-yonghaekim-my-folder/memory
git add project_assemble_v4_spike_xi.md MEMORY.md
git commit -m "memory: add Spike XI ship + index update"
```

(메모리 디렉토리가 별도 git repo가 아니면 스킵 가능 — 사용자 셋업 따라.)

**Acceptance**:
- CHANGELOG.md 새 섹션 + 모든 변경 enumerate
- 메모리 파일 + MEMORY.md index entry
- Ship commit master에 push
- Working tree clean

**Sub-agent type**: `general-purpose`.

---

## Self-Review (writing-plans skill 명시 — fresh eyes 점검)

### 1. Spec coverage

| spec § | task | 비고 |
|---|---|---|
| Bundle 1 (idea-shaper) | A1, A2, A3 | SKILL + prompt + template + tests |
| Bundle 2 (design-pack) | B1, B2, B3 | SKILL + prompt + 2 templates + tests |
| Bundle 3 (guardian) | C1, C2 | SKILL + 1 template + tests (dispatch X) |
| Phase D wiring | D | atomic commit (Spike X precedent) |
| Phase E review | E | superpowers:code-reviewer overall |
| B-16 dogfood (12 AC) | F1 | 3 sub-runs + 12 AC |
| Phase F ship | F2 | CHANGELOG + memory + MEMORY.md |

모든 spec 섹션이 task로 매핑됨.

### 2. Placeholder scan

- "TBD" / "TODO: fill in" / "Add appropriate" 류 — none ✅
- 모든 step에 실제 코드 또는 구체적 명령 포함 ✅
- "similar to Task N" 식 — none (각 task 독립) ✅

### 3. Type consistency

- `_BUNDLED_DIR_TO_STAGE` (harness + inventory 동일 이름) ✅
- `_PROMPT_TO_STAGE` (Spike X 패턴 그대로) ✅
- placeholder 이름 spec ↔ plan ↔ template 일치:
  - {{USER}} / {{PROBLEM}} / {{WEDGE}} / {{NON_GOALS}} / {{TASK_SUMMARY}} (idea)
  - {{TONE}} / {{COLOR_PRIMARY}} / {{COMPONENTS}} / {{TYPO}} / {{IDEA_OR_PRD_SUMMARY}} (design)
  - {{TIMESTAMP}} / {{FROZEN_DIRS}} / {{DENY_COMMANDS}} / {{PLANNED_DESTRUCTIVE}} (guardian)

### 4. 잠재 footgun

- bidirectional integrity test가 idea/design 추가 후 자동 통과해야 함 (양쪽 다 추가). guardian은 양쪽 다 없음. ✅
- inventory.py의 `_BUNDLED_DIR_TO_STAGE`는 fallback 매핑이지 production reader가 없음 (Spike VIII A1 carryforward I2). 하지만 universal-defense convention으로 sync는 유지.
- 만약 SKILL.md frontmatter에 `stages:` 필드를 명시하면 inventory의 `_BUNDLED_DIR_TO_STAGE` fallback이 호출되지 않을 수도 — Spike VIII pattern에서 verifier도 명시적으로 frontmatter를 줘서 fallback unreachable이었음. 이번 3 bundles도 frontmatter `stages:` 박혀있으므로 inventory map은 cosmetic. 그래도 sync 유지 (Spike VII fix 정신).

---

## Execution Handoff

Plan 저장 완료: `docs/plans/2026-05-04-v4-spike-xi.md`.

다음: spec + plan 단일 commit 후 `superpowers:subagent-driven-development`으로 task별 fresh subagent 진행.
