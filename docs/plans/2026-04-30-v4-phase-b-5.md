# /assemble V4 Phase B-5 — 4-doc integration spike (multi-iteration + true 4-way parallel + blank-Mac sim)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close Phase B by formalizing three contract-vs-reality gaps that B-1/B-2/B-3/B-4 dogfoods exposed — (a) multi-iteration loop with stop conditions replacing the fixed 1-iteration cap, (b) true 4-way parallel dispatch demonstrated and gated under empirically-validated platform headroom, (c) `wrap_with_preamble` byte-identity guarantee enforced via dogfood-time sha256 gate. Add the `ASSEMBLE_BUNDLED_ONLY=1` blank-Mac simulation flag and run the full 4-doc dogfood under it. Close gates B5.1–B5.7.

**Architecture:** Server infrastructure (`server/run_dir.py`, `server/harness.py`) stays unchanged — gate B5.4 enforces empty diff against master on those two files. `server/inventory.py` gains the `ASSEMBLE_BUNDLED_ONLY` env flag with menu filtering at scan time. `bundled/plan-pack/SKILL.md` Step 6 rewrites the iteration block from "exactly one iteration" to "iterate until `RESOLVED ≥ 80% AND NEW ≤ 0` two iterations in a row, capped at 7" with explicit state tracking via `runs/<rid>/iteration_state.json`. Steps 2/3 and Step 6 step 4 cite the platform-limit experiment captured in `docs/research/2026-04-29-platform-limit.md` to tighten orchestrator caution (the 5-way headroom is now part of the contract). Preamble integrity becomes a dogfood obligation, not a function-call obligation: orchestrators may invoke `wrap_with_preamble` *or* inline the preamble literal verbatim, but every dispatched prompt's preamble block must hash to the canonical `bundled/_shared/harness-preamble.md` sha256 (gate B5.7). The fixed-iteration prose in Step 6 is rewritten in place; iteration scope discipline (B-4 fix in `e61ab1a`) is preserved verbatim and applies to every iteration in the new loop. All changes land as incremental commits on branch `v4-phase-b-5`. **Review-before-merge gate**: Task 6 dispatches `superpowers:code-reviewer` against the branch diff and triages findings before any merge — same shape as B-3/B-4.

**Tech Stack:** Python 3 stdlib only (no new deps). pytest. Phase B-1/B-2/B-3/B-4 modules `server.run_dir` (`write_run_artifact`, `read_run_artifact`) and `server.harness` (`wrap_with_preamble`) reused as-is. `server.inventory.scan()` extended with one env-flag branch. Bash `sha256sum` used in dogfood gate.

**Lessons applied from B-4 dogfood + retro (`docs/dogfood/phase-b-4.md` § Findings, ledger `project_assemble_v4_phase_b_posttuning.md`):**

- **Item A — Multi-iteration loop with stop conditions (HIGH).** B-2 Finding #4 + B-3 Finding #5 + B-4 Finding #2 all show the same recurrence: iteration resolves prior findings (4/4 → 9/10 → 12/12) but introduces NEW findings that exit unresolved at the 1-iteration cap. Three corroborating phases. Stop condition `RESOLVED ≥ 80% AND NEW ≤ 0` for two consecutive iterations, capped at 7, formalized in this plan's Task 2.
- **Item B-1 — Parallel dispatch (HIGH, B-5 spike core).** B-3 + B-4 dogfoods both fell back to sequential out of orchestrator caution about a hypothetical platform Agent-call budget. The platform-limit experiment run on 2026-04-29 (Trials T2/T3/T4/T5 — single-message dispatch of 2/3/4/5 `general-purpose` Agent calls) returned all-pass with no reject/rate-limit/silent-degrade observed. **Conclusion: 4-way parallel is achievable; the orchestrator caution is no longer evidence-based.** Task 3 lands the experiment citation in SKILL.md Steps 2/3 + Step 6 step 4 prose and tightens the sequential-fallback caveat.
- **Item B-2 — `wrap_with_preamble` bypass (HIGH, B-5 spike core).** B-4 dogfood orchestrator used the function for the first dispatch (via `/tmp` file roundtrip) then bypassed for all subsequent dispatches because the multi-step roundtrip was awkward. Result: trace-byte-identical preamble guarantee broke. Task 3 reframes the contract: orchestrator may use `wrap_with_preamble` *or* inline the preamble literal — both are acceptable, **as long as every dispatched prompt's preamble block hashes to the canonical preamble file's sha256** (new gate B5.7). This decouples the byte-identity guarantee from the function-call discipline.
- **Item C — Test anchoring brittleness (Tier 2, applied as a constraint, not refactored here).** All new B-5 tests in `tests/unit/test_plan_pack_skill.py` MUST locate spec text via heading anchor (`body.index("### Step N")` or `awk '/^### Step N/{flag=1} flag'`) instead of fixed `body[:N]` window slices. Constraint applies to plan authoring; the broader pattern refactor lives on the post-B-5 quality pass track.
- **Item D — Test ↔ spec wording drift (Tier 2, applied as a constraint, not refactored here).** When this plan asserts both a test grep literal (in a Task's bullet) and the SKILL.md spec wording the test grep-matches, the plan author MUST verify the literal appears verbatim in the spec block before finalizing the task. The constraint is checked during plan review (Task 6 review-before-merge).

**Branching:**

```bash
cd ~/.claude/skills/assemble
git checkout master && git pull
git checkout -b v4-phase-b-5
# … Tasks 1–5 …
# (merge happens inside Task 5, after review-before-merge gate clears)
```

---

## File Structure

**Create:**
- `~/.claude/skills/assemble/docs/research/2026-04-29-platform-limit.md` — platform-limit experiment record (T2/T3/T4/T5 trace + verdict). Authoritative reference for SKILL.md citation in Task 3.
- `~/.claude/skills/assemble/docs/dogfood/phase-b-5.md` — Phase B-5 dogfood gate results (filled in Task 5)
- `~/.claude/skills/assemble/docs/plans/2026-04-30-v4-phase-b-5.md` — this file (staged in Task 6)

**Modify:**
- `~/.claude/skills/assemble/server/inventory.py` — Task 1 (ASSEMBLE_BUNDLED_ONLY env-flag branch)
- `~/.claude/skills/assemble/bundled/plan-pack/SKILL.md` — Tasks 2, 3
- `~/.claude/skills/assemble/tests/unit/test_inventory.py` — Task 1 (+2 tests)
- `~/.claude/skills/assemble/tests/unit/test_plan_pack_skill.py` — Tasks 2, 3 (+5 tests)
- `~/.claude/skills/assemble/CHANGELOG.md` — Task 6

**Unchanged (gate B5.4):**
- `server/run_dir.py`
- `server/harness.py`
- `server/__init__.py`
- All `bundled/plan-pack/templates/*.md.template` (PRD, ARCHITECTURE, ADR, UI_GUIDE templates carry over from B-4 unchanged)

**Step numbering convention.** B-4 finalized Steps 0–13 with execution order `0 → 1 → 2+3 → 4 → 5 → 7 → 8 → 10 → 11 → 12 → 13 → 9 → 6`. B-5 does NOT add new step numbers — it rewrites the body of Step 6 (multi-iteration loop) and tightens prose in Steps 2/3 + Step 6 step 4 (parallel-dispatch caveat). Execution order is preserved.

---

## Task 1: `ASSEMBLE_BUNDLED_ONLY=1` flag in `server/inventory.py`

**Goal:** when the env flag is set, `scan()` excludes every non-bundled skill/agent from the inventory output, so blank-Mac simulation can run without nuking the user's `~/.claude/skills/` install.

**Files:**
- `server/inventory.py` — gate `enumerate_skill_paths()` and `enumerate_agent_paths()` results behind the env flag inside `scan()`. The filter MUST run on path prefix (`/.claude/skills/assemble/bundled/` containment), not on classification source — bundled tools are identified structurally, not by a label.
- `tests/unit/test_inventory.py` — add 2 tests:
  - `test_bundled_only_excludes_user_skills` — write a fixture user skill outside the bundled tree, run `scan()` with `ASSEMBLE_BUNDLED_ONLY=1` set, assert it's absent. Test grep literal: `assert "test_user_skill" not in inv["skills"]` (note: `inv["skills"]` is a dict keyed by skill name).
  - `test_bundled_only_keeps_bundled` — same fixture but assert plan-pack survives. Test grep literal: `assert "plan-pack" in inv["skills"]`.

**Spec contract (verbatim — appears as `scan()` docstring/comment line after Task 1 lands; not a test grep target, so Item D check does not apply here):**

> When `ASSEMBLE_BUNDLED_ONLY=1` is set, `scan()` returns only entries whose path is contained under `bundled/`. The flag is a simulation aid for blank-Mac dogfood gates; it does not affect classification cache state.

- [ ] **Step 1.** Read `server/inventory.py`. Locate `scan()` and the `enumerate_skill_paths()`/`enumerate_agent_paths()` calls. Add an `_bundled_only_active()` helper that returns `os.environ.get("ASSEMBLE_BUNDLED_ONLY") == "1"`.
- [ ] **Step 2.** In `scan()`, after enumeration but before classification, filter `skill_paths` and `agent_paths` by `("/bundled/" in str(p))` when `_bundled_only_active()` returns True.
- [ ] **Step 3.** Add the docstring/comment line quoted above to `scan()`. (Item D pre-check passed: literal phrase identical to test assertion's reasoning trail.)
- [ ] **Step 4.** Write the 2 tests in `tests/unit/test_inventory.py`. Each test sets `monkeypatch.setenv("ASSEMBLE_BUNDLED_ONLY", "1")` (or `delenv` for the non-flag baseline) and runs against a tmp_path inventory.
- [ ] **Step 5.** `pytest tests/unit/test_inventory.py -v` — green. Full regression `pytest -q` — green (147+2=149 passes).
- [ ] **Step 6.** Commit on `v4-phase-b-5`: `feat(v4): ASSEMBLE_BUNDLED_ONLY=1 flag for blank-Mac sim (B-5)`.

**Gate B5.4 check:** `git diff master..HEAD -- server/run_dir.py server/harness.py server/__init__.py` MUST be empty after Task 1. Validate before commit.

---

## Task 2: SKILL.md Step 6 — multi-iteration loop with stop conditions

**Goal:** Replace the fixed-1 iteration cap in `bundled/plan-pack/SKILL.md` Step 6 with a stop-condition loop. Preserve the existing iteration scope discipline block (committed as `33b3056` in `e61ab1a` merge) verbatim — it applies to every iteration in the new loop.

**Files:**
- `bundled/plan-pack/SKILL.md` — rewrite Step 6 body (lines covering "Phase B-4 covers exactly **one iteration**" through the dogfood evidence blockquote)
- `tests/unit/test_plan_pack_skill.py` — add 3 tests anchored on `### Step 6` heading

**Stop condition contract (verbatim — Item D check: this exact wording appears in SKILL.md Step 6 after Task 2 lands and is grep-matched by the test in Step 5 below):**

> The orchestrator continues iterating while either condition holds: (a) Step 9 review reports `NEW ≥ 1` for the just-completed iteration, OR (b) the most recent two iterations have not both satisfied `RESOLVED ≥ 80% AND NEW ≤ 0`. Iteration stops when two consecutive iterations both satisfy `RESOLVED ≥ 80% AND NEW ≤ 0`, or when the iteration counter reaches 7, whichever comes first.

**Iteration state tracking contract (verbatim — Item D check):**

> The orchestrator maintains a per-run state file at `runs/<rid>/iteration_state.json` with shape `{"iterations": [{"index": N, "resolved_pct": F, "new_count": N, "stopped": bool, "reason": "..."}, ...]}`. The file is updated after each Step 9 cross-doc review. Termination reason is one of `stop-condition-met`, `cap-reached`, `user-requested-stop`.

- [ ] **Step 1.** Read `bundled/plan-pack/SKILL.md` lines 595–612 (the existing Step 6 closing prose and dogfood blockquote). Identify the cut points: from "Phase B-4 covers exactly **one iteration**." through the closing `>` blockquote.
- [ ] **Step 2.** Replace that block with the multi-iteration loop prose. The new block contains, in order:
  1. The verbatim stop condition contract above.
  2. The verbatim iteration state tracking contract above.
  3. A "User exit override" subsection — after each iteration, the orchestrator asks `AskUserQuestion` "Continue iterating?" with options `["yes — run another iteration", "no — stop here"]`. User no-answer overrides the stop condition (records `user-requested-stop`).
  4. A "Iteration cap exceeded" subsection — if the counter reaches 7 without satisfying the stop condition, the orchestrator emits a one-line warning to the user citing the cap and the unresolved finding count, then exits.
  5. **Preserve** (do not rewrite) the existing iteration scope discipline block (the paragraph starting "Iteration scope discipline (mandatory constraint…"). It now applies to *every* iteration in the loop, not only iteration 1. Add a one-line clarifier above the block: `> The constraint below applies to every iteration in the loop above, not only the first.`
  6. **Preserve** the "Iteration write order" enumerated list (Steps 2+3 → 8 → 11 → 13 → Step 5 overwrites → Step 8 overwrites → … → Step 9 again). Update only step 4's prose to remove the B-4-era hedging and replace with the platform-limit-experiment citation per Task 3.
- [ ] **Step 3.** Update the dogfood evidence blockquote to point to `docs/dogfood/phase-b-5.md` once dogfood runs. Use a `{{B5_RUN_ID}}` placeholder for now; substituted in Task 4 Step 7 with the actual `<rid>`.
- [ ] **Step 4.** Confirm `## [Unreleased]` heading shape in CHANGELOG.md is single (B-4 retro #3 lesson — no double-Unreleased). Defer the actual CHANGELOG update to Task 6.
- [ ] **Step 5.** Add 3 tests in `tests/unit/test_plan_pack_skill.py`:
  - `test_step6_has_stop_condition_contract` — locate Step 6 block via `_section(body, "### Step 6")` (heading-anchored helper inline; do NOT use `body[:N]` slice — Item C constraint). Assert the verbatim stop condition contract phrase appears as substring. Test grep literal: `"two consecutive iterations both satisfy `RESOLVED ≥ 80% AND NEW ≤ 0`"`.
  - `test_step6_has_iteration_state_contract` — same anchor. Assert `runs/<rid>/iteration_state.json` and the JSON shape phrase appear as substrings. Test grep literal: `'runs/<rid>/iteration_state.json'`.
  - `test_step6_has_iteration_scope_discipline_preserved` — same anchor. Assert the existing `33b3056` discipline block phrase still appears verbatim (regression guard against accidental delete during rewrite). Test grep literal: `"PRD `## Core features` is the authoritative scope"`.
- [ ] **Step 6.** `pytest tests/unit/test_plan_pack_skill.py -v` — green. Full regression — green (149+3=152 passes).
- [ ] **Step 7.** Commit on `v4-phase-b-5`: `feat(v4): plan-pack Step 6 multi-iteration loop with stop conditions (B-5 Item A)`.

**Inline helper for Item C constraint:** Add a top-of-file helper in `tests/unit/test_plan_pack_skill.py` if not already present:

```python
def _section(body: str, heading: str) -> str:
    """Return the body block from `heading` through the next sibling-or-shallower heading line.

    `heading` must include leading hash markers, e.g. "### Step 6". The function
    locates the heading, then scans forward and stops at the first heading line
    of equal-or-shallower depth (so a `### Step 6` slice ends on the next `### `,
    `## `, or `# ` heading — whichever comes first).
    """
    start = body.index(heading)
    depth = len(heading) - len(heading.lstrip("#"))
    nl = body.find("\n", start)
    if nl == -1:
        return body[start:]
    cursor = nl + 1
    while cursor < len(body):
        next_nl = body.find("\n", cursor)
        line_end = next_nl if next_nl != -1 else len(body)
        line = body[cursor:line_end]
        if line.startswith("#"):
            line_depth = len(line) - len(line.lstrip("#"))
            if line_depth <= depth and not line.startswith(heading):
                return body[start:cursor]
        if next_nl == -1:
            break
        cursor = next_nl + 1
    return body[start:]
```

This helper is the Item C compliance landing point — all new B-5 tests use it for slicing instead of `body[:N]` windows.

---

## Task 3: SKILL.md parallel-dispatch + preamble byte-identity contract

**Goal:** Tighten Steps 2/3 and Step 6 step 4 prose with the platform-limit experiment citation (Item B-1), and reframe the `wrap_with_preamble` contract as a byte-identity guarantee verifiable via dogfood-time sha256 (Item B-2).

**Files:**
- `bundled/plan-pack/SKILL.md` — modify Steps 2/3 prose + Step 6 step 4 prose + add a new dispatched-prompt preamble integrity subsection
- `docs/research/2026-04-29-platform-limit.md` — create (experiment record, source of truth for the SKILL.md citation)
- `tests/unit/test_plan_pack_skill.py` — add 2 tests

**Platform-limit citation contract (verbatim — Item D check):**

> Empirical evidence (`docs/research/2026-04-29-platform-limit.md`): a controlled platform-limit experiment dispatched 2 / 3 / 4 / 5 `general-purpose` Agent calls in a single message. All four trials returned successful — no reject, no rate-limit, no silent-degrade. The platform tolerates at least 5-way parallel dispatch in a single response. Sequential fallback is therefore an orchestrator timing choice, not a platform constraint, and MUST NOT be the default at the iteration write surface (Step 6 step 4).

**Preamble byte-identity contract (verbatim — Item D check):**

> The orchestrator may dispatch a sub-agent prompt either by (a) calling `wrap_with_preamble(prompt)` and passing the result, or (b) inlining the preamble block literally as the prompt prefix. Both forms are acceptable. The contract is byte-identity: every dispatched prompt's preamble block, when isolated and hashed, MUST match the sha256 of `bundled/_shared/harness-preamble.md`. Drift in either direction (rewording, missing newline, added text) is a contract violation. Dogfood gate B5.7 verifies this.

- [ ] **Step 1.** Create `docs/research/2026-04-29-platform-limit.md` with the experiment record:
  - Hypothesis (ledger Item B "How to apply" prep paragraph, verbatim)
  - Method (4 trials T2/T3/T4/T5, single message dispatch with N general-purpose agents emitting `SENTINEL_T<N>_<idx>`)
  - Results table (N, dispatch acceptance, return integrity, durations)
  - Verdict (5-way headroom confirmed; 4-way is the conservative B-5 spike target)
  - Decision (SKILL.md stays — no sequential canonical rewrite; orchestrator caution language tightened)
- [ ] **Step 2.** In SKILL.md Step 2/3 (line ~112: "fire both in a single message with two Agent calls (true parallel dispatch — Phase B-1 verification location *a*)"), append a citation sentence pointing to the research doc. Verbatim: `> Sequential fallback is permitted only if a documented orchestrator constraint blocks parallel dispatch (e.g. dependent inputs); the platform itself does not constrain to <2. See \`docs/research/2026-04-29-platform-limit.md\`.`
- [ ] **Step 3.** In SKILL.md Step 6 step 4 (the iteration write order step 4 — the "true 4-way parallel-dispatch surface" line), replace the existing caveat block ("sequential dispatch remains acceptable per the same B-3 dogfood Finding #4 caveat …") with the verbatim platform-limit citation contract above. Sequential fallback for iteration is now restricted to: input dependency exists OR orchestrator detects retry-after on a previous attempt — NOT general caution.
- [ ] **Step 4.** In SKILL.md Step 2/3, immediately after the canonical `from server.harness import wrap_with_preamble` example block (line ~107), insert a new `#### Preamble byte-identity` subsection with the verbatim preamble byte-identity contract above. Reword the existing "do **not** hand-write the 4-rule preamble inline; hand-writing risks wording drift" to: "Inlining the preamble literal is permitted as an alternative to the function call; both must produce byte-identical preamble bytes, verified at dogfood time via gate B5.7."
- [ ] **Step 5.** Add 2 tests in `tests/unit/test_plan_pack_skill.py`:
  - `test_step6_step4_cites_platform_limit_research` — `_section(body, "### Step 6")`, assert verbatim citation phrase. Test grep literal: `"docs/research/2026-04-29-platform-limit.md"`.
  - `test_steps_2_3_have_preamble_byte_identity_contract` — `_section(body, "### Step 2")`, assert preamble-byte-identity phrase. Test grep literal: `"every dispatched prompt's preamble block, when isolated and hashed, MUST match"`.
- [ ] **Step 6.** `pytest tests/unit/test_plan_pack_skill.py -v` — green. Full regression — green (152+2=154 passes).
- [ ] **Step 7.** Commit on `v4-phase-b-5`: `feat(v4): plan-pack parallel-dispatch caveat tightening + preamble byte-identity contract (B-5 Item B-1 + B-2)`.

---

## Task 4: Dogfood — blank-Mac simulation + true 4-way parallel + multi-iteration loop + sha256 preamble gate

**Goal:** Run the full plan-pack workflow under `ASSEMBLE_BUNDLED_ONLY=1`, dispatching the iteration write surface as a true 4-way parallel turn, exercising the multi-iteration loop until natural stop or cap, and verifying the preamble byte-identity gate via `sha256sum`.

**Files:**
- `docs/dogfood/phase-b-5.md` — phase B-5 dogfood report (gate results, trace excerpts, findings)
- `bundled/plan-pack/SKILL.md` — substitute `{{B5_RUN_ID}}` placeholder from Task 2 with the actual run id

**Dogfood task spec.** A throwaway product idea (concrete enough to elicit AC bash + design direction). Suggested: "단일 페이지 영수증 OCR 도우미 — 사진 1장 → 합계/날짜/품목 추출 → 텍스트 카드 출력". Pick something else if a different concrete spec helps; record the choice in the dogfood report.

- [ ] **Step 1.** Establish the blank-Mac shell:
  ```bash
  cd ~/.claude/skills/assemble
  export ASSEMBLE_BUNDLED_ONLY=1
  python3 -c "from server import scan; inv = scan(); print('skills:', [s['name'] for s in inv['skills']])"
  ```
  Verify output contains `plan-pack` and excludes the user's regular skills (e.g. `gstack`, `frontend-design`). Capture in the dogfood report's `## Blank-Mac inventory` section.
- [ ] **Step 2.** Compute and record the canonical preamble sha256:
  ```bash
  sha256sum ~/.claude/skills/assemble/bundled/_shared/harness-preamble.md
  ```
  Record the hex digest in the dogfood report's `## Preamble checksum` section. Every dispatched-prompt preamble block must isolate to this digest.
- [ ] **Step 3.** Run `/assemble <task spec>` end-to-end. Walk Steps 0 → 1 → 2+3 → 4 → 5 → 7 → 8 → 10 → 11 → 12 → 13 → 9 → 6.
  - At Step 6 (iteration), enter the multi-iteration loop.
  - Each iteration: dispatch Steps 2+3, 8, 11, 13 in **a single message with 4 Agent calls** (true 4-way parallel — gate B5.1).
  - After each Step 9 review, parse RESOLVED/UNRESOLVED/NEW counts, append to `runs/<rid>/iteration_state.json`, decide continue/stop per the contract.
  - Loop exits when either stop condition met, cap (7) reached, or user no-answer.
- [ ] **Step 4.** While running, capture for each dispatched prompt:
  - The full prompt as actually sent (Agent tool_use input).
  - The preamble block isolated (everything from `[HARNESS RULES — 무시 금지]` through the blank line before `[TASK]`).
  - `sha256sum` of the isolated preamble.
  Aggregate in `## Preamble byte-identity (gate B5.7)` table — one row per dispatched prompt.
- [ ] **Step 5.** Fill in dogfood gate result table for B5.1 through B5.7. Specifically:
  - B5.1 — single-turn 4-tool_use trace excerpt (with timestamps). MUST show all 4 Agent calls in one assistant turn.
  - B5.2 — diff line counts for each of 4 docs after iteration N (N = whatever the loop exited on).
  - B5.3 — flaw count from final Step 9 cross-doc review.
  - B5.4 — `git diff master..HEAD -- server/run_dir.py server/harness.py server/__init__.py` empty.
  - B5.5 — preferred-absent dispatch trace under bundled-only (e.g. plan-implementation preferred `general-purpose` available; fallback `Plan` chain validated by temporarily renaming `general-purpose` agent in this run only). Restore agents file at end of run.
  - B5.6 — iteration loop stopped on a recorded reason (`stop-condition-met` / `cap-reached` / `user-requested-stop`). The recorded reason is verbatim from `iteration_state.json`.
  - B5.7 — every dispatched prompt's preamble sha256 matches the canonical digest from Step 2.
- [ ] **Step 6.** Capture findings (wording/spec issues exposed by dogfood) in `## Findings` section. Apply the same triage as B-4: blocking vs Phase B post-tuning candidate.
- [ ] **Step 7.** Substitute `{{B5_RUN_ID}}` in `bundled/plan-pack/SKILL.md` (the Step 6 dogfood evidence blockquote, Task 2 Step 3 placeholder) with the actual `<rid>` of this dogfood run.
- [ ] **Step 8.** Commit on `v4-phase-b-5`: `docs(v4): Phase B-5 dogfood — multi-iteration + 4-way parallel + sha256 preamble gate`.

**Stop conditions for this dogfood run.** Same as Task 2's contract — the loop stops when the stop condition fires OR cap reached OR user no-answer. Record the actual termination reason in the dogfood report. (Compared to B-4 where one iteration was a hard cap, this dogfood is what verifies the cap is now driven by data.)

---

## Task 5: Review-before-merge gate + CHANGELOG + spec commit + merge

**Goal:** Dispatch `superpowers:code-reviewer` against `git diff master..v4-phase-b-5` and triage findings before any merge to master. Update CHANGELOG. Commit this plan file. Merge.

**Files:**
- `CHANGELOG.md` — add B-5 entry under the existing single `## [Unreleased]` heading
- `docs/plans/2026-04-30-v4-phase-b-5.md` — this file, committed in this task
- (review may surface a fix-up commit — prepared if so)

- [ ] **Step 1.** Acceptance criteria (encoded for the reviewer):
  - AC1: `bundled/plan-pack/SKILL.md` Step 6 contains the verbatim stop condition contract (Task 2).
  - AC2: `bundled/plan-pack/SKILL.md` Step 6 contains the verbatim iteration state contract (Task 2).
  - AC3: `bundled/plan-pack/SKILL.md` Step 6 preserves the `33b3056` iteration scope discipline block verbatim (Task 2 regression guard).
  - AC4: `bundled/plan-pack/SKILL.md` Steps 2/3 contain the verbatim platform-limit citation (Task 3).
  - AC5: `bundled/plan-pack/SKILL.md` Steps 2/3 contain the verbatim preamble byte-identity contract (Task 3).
  - AC6: `docs/research/2026-04-29-platform-limit.md` exists with results table covering N=2,3,4,5 (Task 3).
  - AC7: `server/inventory.py` honors `ASSEMBLE_BUNDLED_ONLY=1` (Task 1).
  - AC8: `docs/dogfood/phase-b-5.md` reports B5.1–B5.7 all green and B5.6 records the actual termination reason (Task 4).
  - AC9: `git diff master..HEAD -- server/run_dir.py server/harness.py server/__init__.py` is empty.
  - AC10: Diff scope = the files listed in `## File Structure` only.
- [ ] **Step 2.** Dispatch `superpowers:code-reviewer` with framing: "Review `git diff master..v4-phase-b-5` against the AC1–AC10 criteria above. For each AC, return PASS / FAIL with a one-line citation. Surface any IMPORTANT or NIT findings beyond AC compliance."
- [ ] **Step 3.** Triage. If reviewer returns FAIL on any AC or IMPORTANT findings:
  - Address inline on `v4-phase-b-5` as a fix-up commit. Do not amend prior commits.
  - Re-dispatch the reviewer if the fix surface is non-trivial (>10 lines).
  - Record the fix-up commit SHA in the dogfood report's `## Pre-merge review` section.
- [ ] **Step 4.** Update CHANGELOG.md under the existing `## [Unreleased]` heading. Add the B-5 entry summarizing: multi-iteration loop with stop conditions, true 4-way parallel demonstration, preamble byte-identity sha256 gate, blank-Mac simulation flag, dogfood run id, gate B5.1–B5.7 result. Confirm only one `## [Unreleased]` heading exists after edit (B-4 retro #3 regression guard).
- [ ] **Step 5.** Commit on `v4-phase-b-5`:
  - `docs(v4): Phase B-5 plan + changelog`
  - Stage: `docs/plans/2026-04-30-v4-phase-b-5.md` (this file), `CHANGELOG.md`
- [ ] **Step 6.** Merge `v4-phase-b-5` → `master` with a no-ff merge. Push origin master. Confirm `git status -sb` clean and `origin/master` synced.

---

## Verification (manual smoke after all tasks)

```bash
cd ~/.claude/skills/assemble
git checkout master && git pull
git log --oneline master -10  # expect Task 1–5 commits + merge commit
pytest -q                     # expect all green
python3 -c "
from server import scan
import os
os.environ['ASSEMBLE_BUNDLED_ONLY'] = '1'
inv = scan()
names = [s['name'] for s in inv['skills']]
assert 'plan-pack' in names, names
print('blank-Mac sim works:', names)
"
sha256sum bundled/_shared/harness-preamble.md
# Cross-check against the digest recorded in docs/dogfood/phase-b-5.md
grep -c "RESOLVED ≥ 80% AND NEW ≤ 0" bundled/plan-pack/SKILL.md
# expect ≥ 2 (one in stop-condition contract, one in stop-condition referenced elsewhere)
```

---

## Out of scope (post-B-5 quality / hygiene passes — do NOT do in this plan)

The following ledger items remain queued for separate passes after Phase B closes:

- **Item C — Test anchoring brittleness (full pattern refactor).** This plan applies the constraint locally (new B-5 tests use `_section()` helper) but does NOT refactor existing `body[:N]` window slices in tests merged before this plan. The bulk refactor + a contributor doc on the pattern lives on the post-B-5 quality pass.
- **Item D — Test ↔ spec wording drift (process check).** This plan applies the constraint locally (every contract has an Item D check note) but does NOT institute a CI step that grep-validates contract literals against spec literals. The CI step lives on the post-B-5 quality pass.
- **Item E — SKILL.md instruction ambiguity audit.** Run `superpowers:code-reviewer` over plan-pack SKILL.md with framing "find ambiguous imperative instructions". Hygiene pass.
- **Item F — Gate line-anchored regex (`^TBD$` / `^미정$`).** Tighten the dogfood gate regex shape. Hygiene pass.

These are deliberate deferrals tracked in `~/.claude/projects/-Users-yonghaekim-my-folder/memory/project_assemble_v4_phase_b_posttuning.md` Tier 2 + Tier 3.

---

## Hard prohibitions (V4 identity — do NOT violate)

- Do NOT introduce sub-agent self-dispatch. Orchestrator-only dispatch (main Claude → subagent) is the V4 identity rule.
- Do NOT modify `server/run_dir.py`, `server/harness.py`, or `server/__init__.py`. Gate B5.4 enforces empty diff.
- Do NOT remove or weaken the iteration scope discipline block (B-4 fix `33b3056`). It applies to every iteration in the new loop.
- Do NOT replace `wrap_with_preamble` function with the literal-inline pattern as canonical. Both are acceptable; the function call remains the recommended path. Only the *guarantee* is reframed (function-call discipline → byte-identity).

---

## Artifact map (state at end of Phase B-5)

```
~/.claude/skills/assemble/
  bundled/plan-pack/SKILL.md            # Step 6 multi-iteration + Steps 2/3 caveats updated
  bundled/plan-pack/templates/*.md.template  # unchanged from B-4
  bundled/_shared/harness-preamble.md   # unchanged (canonical preamble — sha256 source of truth)
  server/inventory.py                   # ASSEMBLE_BUNDLED_ONLY env-flag branch
  server/run_dir.py                     # unchanged
  server/harness.py                     # unchanged
  server/__init__.py                    # unchanged
  docs/research/2026-04-29-platform-limit.md  # NEW — experiment record
  docs/plans/2026-04-30-v4-phase-b-5.md       # this file
  docs/dogfood/phase-b-5.md             # NEW — phase B-5 dogfood
  tests/unit/test_inventory.py          # +2 tests (Task 1)
  tests/unit/test_plan_pack_skill.py    # +5 tests (Tasks 2 + 3)
  CHANGELOG.md                          # +1 entry under single [Unreleased]
```

Phase B closes when this plan merges. Post-B-5 work proceeds on the Tier 2 + Tier 3 ledger items (test pattern refactor + ambiguity audit + line-anchored regex).
