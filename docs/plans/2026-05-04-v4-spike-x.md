# V4 Spike X Plan — keeper ★ bundle (cross-cutting C closure)

**Date**: 2026-05-04
**Spec**: `docs/specs/2026-05-04-v4-spike-x-design.md`
**Pattern**: subagent-driven-development — each Task = fresh sub-agent dispatch + spec review + code-quality review
**Baseline**: master `d200a6f`, pytest 563 passed.

---

## Overview

26 tasks across 7 phases. Each task = 1 atomic commit. Phase boundaries are sub-agent
context-isolation checkpoints.

| Phase | Scope | Task count | Commit prefix |
|---|---|---|---|
| A | server/learnings.py + ledger helpers + parsed_scope schema doc | 3 | `feat(v4-spike-x-A{N})` |
| B | keeper bundle prompts + script + SKILL + templates | 7 | `feat(v4-spike-x-B{N})` |
| C | SECURITY + contracts.json + inventory sync + harness sync | 4 | `feat(v4-spike-x-C{N})` |
| D | dispatch chain wiring (allowlist + ORCHESTRATOR_ONLY + new wrapper + route) | 4 | `feat(v4-spike-x-D{N})` |
| E | overall review + Codex retro (MANDATORY) | 3 | `fix(v4-spike-x-E{N})` |
| F | integration tests + B-15 dogfood 4 sub-runs | 4 | mixed |
| G | ship (CHANGELOG + memory + MEMORY.md + master commit) | 1 | `docs(v4-spike-x)` |

Parallel ordering:
- Phase A1, A2, A3 are mutually independent — parallel-safe
- Phase B prompts (B2-B6) depend on A1 (STAGE_CATEGORY_PRIORITY) — sequential after A
- Phase C tasks depend on B (need full bundle on disk for sync)
- Phase D depends on Phase C2 (contracts.json structure)
- Phase E depends on Phase D (review needs full implementation)
- Phase F depends on Phase E (dogfood needs reviewed code)
- Phase G is final sequential

---

## Phase A — server module + ledger helpers (3 tasks, parallel-safe)

### A1 — `server/learnings.py` module (selection + render + priority map)

**Goal**: New module with deterministic top-K selection, fence rendering, and stage→
category priority map. Pure functions only (no I/O on `learnings.jsonl` — that's A3).

**Files touched**:
- `server/learnings.py` — NEW. Functions: `select_relevant(stage, k=5, ledger=None)`,
  `render_learnings_fence(entries)`, constants: `STAGE_CATEGORY_PRIORITY`,
  `MAX_K_DEFAULT = 5`, `MAX_SUMMARY_CHARS = 200`.
- `tests/unit/test_learnings_select.py` — 12+ tests (empty ledger / single entry /
  K cap / category priority order / recency tiebreaker / rule_id alpha tiebreaker /
  unknown stage fallback / explicit ledger param injection for testability).
- `tests/unit/test_learnings_fence.py` — 6+ tests (empty entries → empty fence /
  single entry render / multi-entry numbering / 200-char truncation guard /
  newline strip in summary / Korean + English mix).

**Acceptance**:
- `select_relevant("plan", k=5, ledger=[entry1, entry2, ...])` returns ≤5 sorted by:
  1. category match priority (`STAGE_CATEGORY_PRIORITY[stage]`)
  2. ts descending
  3. rule_id alpha ascending
- `render_learnings_fence([])` returns `""` (no fence).
- `render_learnings_fence(entries)` returns:
  ```
  [PRIOR LEARNINGS — 우선 회피]
  1. (R2) Edited src/auth.py despite deny pattern auth/* — ...
  2. (R3) ...
  [/PRIOR LEARNINGS]
  ```
- `STAGE_CATEGORY_PRIORITY` covers 7 stages (plan/execute/debug/review/verify/ship/meta).
- All tests green.

**Sub-agent type**: `general-purpose`.

**Dependencies**: none.

---

### A2 — `parsed_scope.json` schema doc + keeper consumer notes

**Goal**: pure documentation pass. Annotate `parsed_scope.py` docstring noting that
`allow` + `deny` lists are also consumed by keeper Rule R2; no schema change. Avoids
silent coupling.

**Files touched**:
- `server/scope_parser.py` — extend module-level docstring with "Cross-bundle consumers"
  section listing reviewer (existing) + keeper (Spike X).
- `tests/unit/test_scope_parser.py` — 1 new test asserting docstring contains both
  consumer names (regression-proof anchor).

**Acceptance**:
- Docstring change visible in `help(server.scope_parser)`.
- Existing parser tests unchanged (no behavior change).

**Sub-agent type**: `general-purpose`.

**Dependencies**: none.

---

### A3 — ledger helpers (`learnings_path`, `read_ledger`, `write_ledger`, `prune_ledger`)

**Goal**: All ledger I/O lives in `server/learnings.py` (extending A1's module). Atomic
overwrite via tempfile + rename. Skiplist parsing (one hash per line, `#` comments).

**Files touched**:
- `server/learnings.py` — extend with: `learnings_path()`, `learnings_skip_path()`,
  `read_ledger() -> list[dict]`, `read_skiplist() -> set[str]`, `write_ledger(entries)`,
  `prune_ledger(entries, *, now=None, ttl_days=30, cap=100, skiplist=None) -> list[dict]`.
- `tests/unit/test_learnings_ledger.py` — 16+ tests (empty ledger / 1-entry round trip /
  malformed line skip / TTL drop / skiplist drop / dedup keeps most-recent / FIFO cap /
  prune-rule order matters / atomic-overwrite-doesn't-clobber / `#` comments in
  skiplist / blank lines in skiplist).

**Acceptance**:
- `prune_ledger` rule order: TTL → skiplist → dedup → FIFO cap (deterministic).
- `prune_ledger` is pure (`now` injectable for tests).
- `write_ledger` writes to `<path>.tmp` then `os.replace` — concurrent crash-safe at
  process level (NOT cross-process — V5 scope, documented).
- `read_ledger` skips malformed JSON lines without raising (logs warning).
- All tests green.

**Sub-agent type**: `general-purpose`.

**Dependencies**: A1 (uses constants from same module).

---

## Phase B — keeper bundle assets (7 tasks)

### B1 — `bundled/keeper/SKILL.md`

**Goal**: 5-section SKILL.md following Spike IX shipper pattern: §Identity, §Inputs,
§Sub-agent matrix, §Verdict logic, §CRITICAL anti-bypass. References §Hand-off NOT
needed (keeper is terminal, no downstream).

**Files touched**:
- `bundled/keeper/SKILL.md` — NEW. ~150 lines. Verbatim sub-agent matrix + 4 prompt
  filenames matching `ALLOWED_PROMPT_FILES` (Phase D1).
- `tests/unit/test_keeper_skill_structure.py` — 6+ tests (sections present / matrix
  table 4 rows + 1 helper / verdict block contains 3 outcomes / CRITICAL anti-bypass
  enumerates 4 prompts).

**Acceptance**:
- §Sub-agent matrix lists 4 subagent + 1 orchestrator helper.
- §CRITICAL §Anti-bypass enumerates: `keeper_audit_step1.md`, `keeper_extract_step2.md`,
  `keeper_summarize_step3.md`, `keeper_ledger_step4.md`.
- Contains `## Bash tool access GRANTED` heading inside `prompts/subagent/` subset
  (matches existing F5 grep gate convention).
- ko-locale entries NOT required for Spike X (i18n debt).

**Sub-agent type**: `general-purpose`.

**Dependencies**: A1 (priority map referenced in SKILL.md examples).

---

### B2 — `keeper_audit_step1.md` (audit inventory)

**Goal**: Step 1 prompt. Reads run_dir, runs `git diff --name-only`, writes
`audit_inventory.json`. Bash GRANTED for read-only git probe.

**Files touched**:
- `bundled/keeper/prompts/subagent/keeper_audit_step1.md` — NEW.
- `tests/unit/test_keeper_audit_step1_prompt.py` — 4+ tests (file exists / Bash heading
  present / save block emits `WROTE: <run_dir>/audit_inventory.json` / Inputs section
  documents RUN_DIR token).

**Acceptance**:
- Prompt contains `^## Bash tool access GRANTED` heading.
- Save block uses `{{RUN_DIR}}` token (not `runs/{{RUN_ID}}`).
- Final WROTE format = `WROTE: <RUN_DIR>/audit_inventory.json` (single line).
- Bash usage scoped to `git status --porcelain`, `git rev-parse HEAD`,
  `git rev-parse --abbrev-ref HEAD`, `git diff --name-only HEAD~..HEAD`. NO
  `shell=True`, NO arg interpolation.

**Sub-agent type**: `general-purpose`.

**Dependencies**: A1.

---

### B3 — `keeper_extract_step2.md` + `bundled/keeper/scripts/extract_rules.py`

**Goal**: Step 2 prompt + canned Python script. Sub-agent runs the script via Bash;
script implements R1-R5 deterministic rules.

**Files touched**:
- `bundled/keeper/prompts/subagent/keeper_extract_step2.md` — NEW. Bash GRANTED for
  single canned `python3 <fixed_path> <run_dir>` invocation.
- `bundled/keeper/scripts/extract_rules.py` — NEW. ~150 lines. Reads
  `audit_inventory.json` + targeted artifacts (parsed_scope, dispatches.jsonl,
  verify_result, etc.). Applies R1-R5. Writes `learning_candidates.json`.
  No imports from `server.*` (keeper bundle is self-contained — script can be
  invoked from any cwd).
- `tests/unit/test_keeper_extract_rules.py` — 18+ tests (R1 dispatch-failure /
  R2 deny+diff overlap / R2 deny but no diff overlap / R3 verify pass / R3 verify
  fail / R4 added TODO / R4 unchanged TODO / R5 dispatch failed / no rules fire /
  evidence_hash deterministic / evidence_hash differs across runs).

**Acceptance**:
- Script runs standalone (`python3 extract_rules.py <run_dir>`) without server import.
- Output = `learning_candidates.json` with `candidates: [{rule_id, category,
  evidence, evidence_hash}, ...]`.
- `evidence_hash` = `sha256(json.dumps(evidence, sort_keys=True))` (canonical form).
- Script is idempotent (running twice on same run_dir produces identical output).
- Prompt's Bash invocation = exact literal command (no user-controlled args).

**Sub-agent type**: `general-purpose`.

**Dependencies**: B2 (audit_inventory.json schema).

---

### B4 — `keeper_summarize_step3.md` (bounded LLM summary)

**Goal**: Step 3 prompt. Reads `learning_candidates.json`, writes ≤200-char single-line
summary per entry. Includes deterministic fallback template if LLM step fails.

**Files touched**:
- `bundled/keeper/prompts/subagent/keeper_summarize_step3.md` — NEW. NO Bash.
- `tests/unit/test_keeper_summarize_step3_prompt.py` — 4+ tests (file exists / no
  Bash heading / save block emits `WROTE: <RUN_DIR>/learnings_to_emit.json` /
  fallback template strings present).

**Acceptance**:
- Summary constraints documented in prompt: ≤200 chars, single line, language match.
- Fallback templates per rule_id documented (R1-R5).
- Output schema preserves `evidence` + `evidence_hash` from input verbatim.

**Sub-agent type**: `general-purpose`.

**Dependencies**: B3 (consumes B3 output schema).

---

### B5 — `keeper_ledger_step4.md` (append + prune + report)

**Goal**: Step 4 prompt. Reads existing ledger, appends new entries, applies prune,
writes ledger + KEEPER_REPORT. Uses A3 helpers (sub-agent reads helper signatures
from prompt body — Python snippet inline OR canned via second script).

**Decision (sub-agent prompt complexity)**: keeper Step 4 ledger I/O is too complex
for inline `python3 -c` (~80 lines). Ship `bundled/keeper/scripts/ledger_update.py`
that imports from `server.learnings` (acceptable since orchestrator's PYTHONPATH
already covers `server/`). Sub-agent invokes via Bash. **This means Step 4 ALSO
needs Bash access** — revising spec sub-agent matrix from "Step 4 = pure Read/Write"
to "Step 4 = Read/Write/Bash". Updates spec § Sub-agent matrix table.

**Files touched**:
- `bundled/keeper/prompts/subagent/keeper_ledger_step4.md` — NEW. **Bash GRANTED**.
- `bundled/keeper/scripts/ledger_update.py` — NEW. Imports `from server.learnings
  import read_ledger, write_ledger, prune_ledger, read_skiplist`. Reads
  `learnings_to_emit.json` → appends → prunes → writes ledger + KEEPER_REPORT.md.
- `bundled/keeper/templates/KEEPER_REPORT.md.template` (happy variant, 7 sections).
- `bundled/keeper/templates/KEEPER_REPORT_ABORT.md.template` (abort variant, 4 sections).
- `tests/unit/test_keeper_ledger_step4_prompt.py` — 4+ tests.
- `tests/unit/test_keeper_ledger_update_script.py` — 12+ tests (append-prune end-to-end
  / KEEPER_REPORT 7-section render / abort variant render / placeholder substitution
  count / ledger byte-level idempotency).

**Acceptance**:
- Script imports `server.learnings` successfully when run from skill root cwd.
- KEEPER_REPORT happy = 7 H2 sections; abort = 4 H2 sections.
- All template placeholders substituted (no `{{...}}` left in output).
- Ledger byte count grows by exactly N entries on N candidate input (after prune).

**Sub-agent type**: `general-purpose`.

**Dependencies**: A3 (ledger helpers), B4 (consumes learnings_to_emit.json schema).

**Spec amendment recorded**: spec table updated to "Step 4 = Read, Write, **Bash**"
during this task. Documented in commit message + amended spec line.

---

### B6 — `keeper_iter_revisit.md` (orchestrator helper)

**Goal**: Orchestrator-only helper for iteration (e.g., re-running keeper after seeded
fix). Lives under `prompts/orchestrator/`. NOT in ALLOWED_PROMPT_FILES.

**Files touched**:
- `bundled/keeper/prompts/orchestrator/keeper_iter_revisit.md` — NEW. ~30 lines.
- `tests/unit/test_keeper_iter_revisit_orchestrator.py` — 3+ tests (file exists /
  not in ALLOWED_PROMPT_FILES / matches ORCHESTRATOR_ONLY_PROMPTS member after
  Phase D2).

**Acceptance**:
- File present.
- Test asserts `"keeper_iter_revisit.md" not in ALLOWED_PROMPT_FILES` AND
  `"keeper_iter_revisit.md" in ORCHESTRATOR_ONLY_PROMPTS` (after D2).

**Sub-agent type**: `general-purpose`.

**Dependencies**: B1 (SKILL.md references this helper).

---

### B7 — KEEPER_REPORT templates (happy + abort)

> **Reconciliation note (post-execution)**: B5 and B7 list the same template
> files in their `Files touched` blocks because B5's ledger script (`ledger_update.py`)
> physically depends on the templates to render KEEPER_REPORT. Per dependency
> chain (B7 `Dependencies: B5`), the two subtasks were consolidated into a
> single dispatch + commit `3f497e8` (`feat(v4-spike-x-B5+B7): keeper_ledger_step4
> + ledger_update.py + KEEPER_REPORT templates`). The split-into-two layout
> below is preserved as the original review framing, not as separate commits.

**Goal**: 2 markdown templates with `{{...}}` placeholders. Happy = 7 H2 sections,
abort = 4 H2 sections.

**Files touched**:
- `bundled/keeper/templates/KEEPER_REPORT.md.template` — moved from B5 (split out
  for cleaner review).
- `bundled/keeper/templates/KEEPER_REPORT_ABORT.md.template`.
- `tests/unit/test_keeper_templates.py` — 6+ tests (section count / placeholder
  enumeration / no leftover `TODO` markers in template body).

**Acceptance**:
- Happy template H2 count = 7: Run summary / Audit inventory / Rules fired /
  Learnings emitted / Ledger state delta / Prune summary / Next-run recall preview.
- Abort template H2 count = 4: Run summary / Audit inventory / Skip reason /
  Next steps.
- Placeholder set documented in tests for B5 substitution coverage.

**Sub-agent type**: `general-purpose`.

**Dependencies**: B5 (B5 may co-evolve placeholder names; finalize here).

---

## Phase C — safety + integrity layer (4 tasks)

### C1 — `bundled/keeper/SECURITY.md`

**Goal**: 7-threat model + 6 mitigations + 5 explicit non-goals (per spec § Security
model). Documents single-run-only assumption, evidence-trust model, ledger
non-redaction.

**Files touched**:
- `bundled/keeper/SECURITY.md` — NEW. Header structure: § Threats T1-T7 / § Mitigations
  M1-M6 / § Audit-evidence trust model / § Explicit non-goals / § Known limitations.
- `tests/unit/test_keeper_security_md.py` — 4+ tests (7 threats enumerated /
  6 mitigations / 5 non-goals / "single-run-only" string present).

**Acceptance**:
- 7 H2 threats enumerated.
- 6 mitigations cross-referenced to threats.
- Non-goals list matches spec § Out of scope.

**Sub-agent type**: `general-purpose`.

**Dependencies**: B1-B7 (full bundle structure).

---

### C2 — `contracts.json` 3 entries

**Goal**: Add 3 keeper contract entries to `~/.claude/skills/assemble/contracts.json`
matching Spike VIII pattern (phrase-based, not type-based).

**Files touched**:
- `contracts.json` — extend with `spike-x-keeper-allowlist`,
  `spike-x-keeper-verdict-invariant`, `spike-x-keeper-artifact-invariant`.
- `tests/unit/test_keeper_contracts.py` — 4+ tests (3 entries present /
  allowlist enumerates 4 prompts / verdict invariant 3 outcomes / artifact
  schema lock 7-or-4 sections).

**Acceptance**:
- 3 new entries in contracts.json.
- Each entry's invariant is verifiable by associated unit test.

**Sub-agent type**: `general-purpose`.

**Dependencies**: B1-B7.

---

### C3 — `inventory.py._BUNDLED_DIR_TO_STAGE` += `keeper:meta`

**Goal**: Universal-defense convention from Spike VIII — both inventory.py and
harness.py keep their `_BUNDLED_DIR_TO_STAGE` maps in sync.

**Files touched**:
- `server/inventory.py` — extend map with `"keeper": "meta"`.
- `tests/unit/test_inventory_bundled_stages.py` — 1 new test (keeper:meta entry).
- `tests/unit/test_inventory_e2e_v4.py` — extend e2e fixture if used.

**Acceptance**:
- inventory scan finds keeper bundle, returns `stage: meta`.

**Sub-agent type**: `general-purpose`.

**Dependencies**: B1.

---

### C4 — `harness.py._BUNDLED_DIR_TO_STAGE` sync (universal-defense)

**Goal**: Match C3 in harness.py copy. Cross-reference comments updated.

**Files touched**:
- `server/harness.py` — extend `_BUNDLED_DIR_TO_STAGE` with `"keeper": "meta"`.
- `tests/unit/test_harness_bundled_stages.py` — 1 new test mirroring C3.
- `tests/unit/test_universal_defense_symmetry.py` (if exists) — extend.

**Acceptance**:
- Both maps contain `"keeper": "meta"`.
- Symmetry test (if extant) passes.

**Sub-agent type**: `general-purpose`.

**Dependencies**: C3.

---

## Phase D — dispatch chain wiring (4 tasks)

### D1 — `ALLOWED_PROMPT_FILES` += 4 keeper subagent prompts

**Goal**: Extend allowlist (additive). Validates new prompts dispatchable via
`dispatch_prompt`.

**Files touched**:
- `server/harness.py` — append 4 entries: `keeper_audit_step1.md`,
  `keeper_extract_step2.md`, `keeper_summarize_step3.md`, `keeper_ledger_step4.md`.
- `tests/unit/test_allowlist_keeper.py` — 5+ tests (4 entries present / dispatch
  resolves each / unknown prompt still rejected).

**Acceptance**:
- ALLOWED_PROMPT_FILES count grows by exactly 4.
- All 4 keeper prompts dispatch successfully via `dispatch_prompt`.

**Sub-agent type**: `general-purpose`.

**Dependencies**: B2-B5.

---

### D2 — `ORCHESTRATOR_ONLY_PROMPTS` += `keeper_iter_revisit.md`

**Goal**: Single-line addition to orchestrator-only set.

**Files touched**:
- `server/harness.py` — extend `ORCHESTRATOR_ONLY_PROMPTS` frozenset.
- `tests/unit/test_orchestrator_only_keeper.py` — 2+ tests (entry present / NOT
  in ALLOWED_PROMPT_FILES / file resolvable on disk).

**Acceptance**:
- `keeper_iter_revisit.md` in ORCHESTRATOR_ONLY_PROMPTS.
- Allowlist roundtrip integrity test (existing) still green.

**Sub-agent type**: `general-purpose`.

**Dependencies**: B6.

---

### D3 — `wrap_with_preamble_and_learnings` + `_PROMPT_TO_STAGE` map

**Goal**: NEW function in `server/harness.py`. Splice learnings fence into body region
after `[TASK]\n` delimiter (preamble bytes UNCHANGED). Module-level `_PROMPT_TO_STAGE`
maps each ALLOWED prompt to a stage label.

**Files touched**:
- `server/harness.py` — `wrap_with_preamble_and_learnings(prompt, run_id, stage, k=5)`
  + `_PROMPT_TO_STAGE` dict.
- `tests/unit/test_wrap_with_preamble_and_learnings.py` — 14+ tests:
  - empty ledger → byte-identical to `wrap_with_preamble`
  - missing run_id OR missing stage → byte-identical
  - non-empty ledger → fence inserted between `[TASK]\n` and body
  - preamble portion of result hash → unchanged from `canonical_preamble_sha256()`
  - splice handles `[TASK]\n` literal in user content (only first occurrence)
  - top-K limit honored
  - stage→category priority drives fence ordering
- `tests/unit/test_prompt_to_stage_map.py` — 3+ tests (every ALLOWED prompt has stage /
  STAGE values are valid keys of STAGE_CATEGORY_PRIORITY).

**Acceptance**:
- `wrap_with_preamble_and_learnings(p, None, None) == wrap_with_preamble(p)` byte-equal.
- With non-empty ledger: result = `<preamble>\n[TASK]\n<fence>\n\n<body>`.
- preamble_sha256 of result (via `_split_preamble_body`) ∈ ALLOW_LIST always.
- `_PROMPT_TO_STAGE` covers every entry in `ALLOWED_PROMPT_FILES`.

**Sub-agent type**: `general-purpose`.

**Dependencies**: A1 (select_relevant), A3 (read_ledger), D1 (allowlist final).

---

### D4 — Route `dispatch_and_record` through new wrapper

**Goal**: Single-call-site swap inside `dispatch_and_record`. Auto-derive stage from
prompt_file via `_PROMPT_TO_STAGE`. Zero call-site changes for existing bundles.

**Files touched**:
- `server/harness.py` — modify `dispatch_and_record`:
  ```python
  # OLD:
  prompt_text = dispatch_prompt(prompt_file)
  # NEW:
  text = _resolve_prompt_path(prompt_file).read_text(encoding="utf-8")
  stage = _PROMPT_TO_STAGE.get(prompt_file)
  prompt_text = wrap_with_preamble_and_learnings(text, run_id=run_id, stage=stage)
  ```
  (Allowlist check moved to a guard inline matching `dispatch_prompt` semantics.)
- `tests/unit/test_dispatch_and_record_learnings.py` — 8+ tests (empty ledger →
  byte-equal to old behavior / non-empty ledger → preamble_sha unchanged + body
  has fence / record_dispatch records same preamble_sha regardless of ledger
  state / verify_dispatches.ok=True regardless / status=skipped/failed paths
  unchanged).

**Acceptance**:
- All existing tests in tests/unit/test_dispatch_*.py STILL GREEN (regression-
  proof empty-ledger byte-equivalence).
- New tests verify learnings injection path.
- `verify_dispatches(rid)` returns ok=True for runs with non-empty ledger.

**Sub-agent type**: `general-purpose`.

**Dependencies**: D3.

---

## Phase E — review + Codex retro (3 tasks)

### E1 — `superpowers:code-reviewer` overall review

**Goal**: Single review pass invoking `superpowers:code-reviewer` on the full Spike X
diff (`d200a6f^..HEAD-after-D4`). Captures findings into `docs/dogfood/spike-x-overall-
review.md`. NOT mandatory amendments — informational + drives E3.

**Files touched**:
- `docs/dogfood/spike-x-overall-review.md` — NEW. Findings + severity classification.
- (No code changes in this task — amendments deferred to E3.)

**Acceptance**:
- review doc has explicit severity classification per finding (Critical / Important /
  Minor / Non-finding).
- Cross-references each finding to a Spike VIII/IX retro pattern when applicable.

**Sub-agent type**: `superpowers:code-reviewer`.

**Dependencies**: D4.

---

### E2 — Codex retro (MANDATORY per cross-cutting C complexity)

**Goal**: Adversarial second-opinion via `codex:codex-rescue` on:
- Step 2 deterministic extraction logic (`extract_rules.py`) — false-positive /
  false-negative analysis
- Step 4 prune order (TTL → skiplist → dedup → FIFO cap) — race / correctness
  edge cases
- Track B body-prefix fence — splice edge cases (multiple `[TASK]\n` literals,
  user content prefixed with `[TASK]\n`)
- Ledger schema durability — V5 forward-compat

**Files touched**:
- `docs/dogfood/spike-x-codex-retro.md` — NEW. Findings + amendments.
- (No code changes in this task — amendments deferred to E3.)

**Acceptance**:
- ≥3 findings (Critical or Important) recorded.
- Each finding has remediation recommendation.
- Confirmed non-findings explicitly listed.

**Sub-agent type**: `codex:codex-rescue`.

**Dependencies**: D4.

---

### E3 — Apply E1 + E2 amendments

**Goal**: Single fix-up commit applying review findings. Amendment set finalized after
E1 + E2 reports merged.

**Files touched**: variable — depends on findings.

**Acceptance**:
- All Critical findings closed (code change OR documentation amendment with
  rationale recorded).
- All Important findings either closed or carryforwarded with explicit Spike XI
  defer note.
- pytest still green; no test count regression.

**Sub-agent type**: `general-purpose`.

**Dependencies**: E1, E2.

---

## Phase F — integration tests + B-15 dogfood (4 tasks)

### F1 — integration tests (`tests/integration/test_keeper_e2e.py`)

**Goal**: End-to-end tests exercising real `dispatch_and_record` chain. Validates that
the full pipeline (Step 1 → 2 → 3 → 4) writes expected artifacts to a tempfile run_dir.

**Files touched**:
- `tests/integration/test_keeper_e2e.py` — NEW. 6+ tests (clean-path / scope-deviation
  seeded / verify-fail seeded / dispatch-fail seeded / TODO-leakage seeded /
  multiple-rule fire combo).

**Acceptance**:
- Each seeded scenario produces deterministic ledger byte delta.
- KEEPER_REPORT.md schema lock holds across all 6 cases.
- pytest green.

**Sub-agent type**: `general-purpose`.

**Dependencies**: E3.

---

### F2 — B-15 dogfood: 4 sub-runs (happy / abort / build-fail / iter-N recall)

**Goal**: Self-execute B-15 covering verdict matrix + recall path. Each sub-run is a
fresh tempdir; F2 wires them.

**Files touched**:
- `docs/dogfood/spike-x-b15.md` — NEW. 12 AC matrix + per-run logs + ledger byte
  diffs + recall fence excerpt.
- `/tmp/spike-x-b15/{happy,abort,build-fail,recall}/...` — runtime tempdirs (NOT
  committed; commands captured in dogfood doc).

**Acceptance**:
- 12 AC PASS target (PARTIAL acceptable on AC10 wall-time edge cases — record).
- Sub-run 4 (recall) wrapped prompt visibly contains `[PRIOR LEARNINGS]` fence
  with entries from sub-runs 2+3.
- Ledger byte counts match prediction (clean=0 delta, abort=+1, build-fail=+1).

**Sub-agent type**: `general-purpose`.

**Dependencies**: F1.

---

### F3 — `verify_dispatches` regression test (preamble sha invariant)

**Goal**: Single targeted test that runs through Track B's body-prefix splice path and
re-asserts `verify_dispatches(rid).ok == True`. Catches any future regression where
fence splicing accidentally lands inside preamble bytes.

**Files touched**:
- `tests/integration/test_verify_dispatches_with_learnings.py` — NEW. 3+ tests (empty
  ledger / non-empty ledger / multiple top-K configurations).

**Acceptance**:
- All cases return ok=True.
- preamble_sha256 in dispatches.jsonl equals canonical hash regardless of ledger
  state.

**Sub-agent type**: `general-purpose`.

**Dependencies**: F2.

---

### F4 — Spike X carryforward sweep (anything F1-F3 surfaced)

**Goal**: Pre-ship cleanup. Apply any tiny fix surfaced by F1-F3 that didn't warrant
a full revisit. NOT a major code change — single-commit polish.

**Files touched**: variable.

**Acceptance**:
- pytest green.
- No "TODO" markers added in this commit (Rule R4 self-discipline).

**Sub-agent type**: `general-purpose`.

**Dependencies**: F3.

---

## Phase G — ship (1 task)

### G1 — CHANGELOG flip + memory + MEMORY.md + ship commit

**Goal**: Final ship gate. Atomic commit flipping `[Unreleased]` → `[Spike X]` with
commit hash, writing memory file, indexing in MEMORY.md.

**Files touched**:
- `CHANGELOG.md` — `[Unreleased]` block → `[Spike X — keeper ★ + cross-cutting C]`.
- `~/.claude/projects/-Users-yonghaekim-my-folder/memory/project_assemble_v4_spike_x.md`
  — NEW. Mirror Spike IX memory structure.
- `~/.claude/projects/-Users-yonghaekim-my-folder/memory/MEMORY.md` — index entry.

**Acceptance**:
- Ship commit on master.
- pytest green at HEAD.
- Memory file written.
- MEMORY.md index entry one line, ≤150 chars.

**Sub-agent type**: `general-purpose` (or main directly if simple).

**Dependencies**: F4.

---

## Risk register

| Risk | Probability | Mitigation |
|---|---|---|
| Step 4 sub-agent script can't import `server.learnings` (PYTHONPATH) | Medium | B5 ships `bundled/keeper/scripts/ledger_update.py` testing import path explicitly; integration test F1 exercises real subprocess |
| Track B fence splice breaks existing dispatch tests | Low-Med | D3 byte-equality regression suite mandated; D4 hand-holds existing tests |
| Codex retro surfaces a Critical that requires V5-style refactor | Low | E2 scope explicit: V4-bounded amendments only; V5 carryforwards documented |
| Ledger jsonl race in B-15 sub-run 4 (concurrent recall) | Very Low | B-15 sub-runs are sequential by design; documented |
| 200-char summary truncation loses essential detail | Low | A1 fence rendering already guards; Step 3 prompt explicitly tells LLM to compress without losing rule_id evidence |

---

## Out of scope reminders (do NOT add tasks for these)

- ❌ Multi-run concurrency (V5)
- ❌ False-positive feedback loop (V5)
- ❌ Cross-machine ledger sync (V5)
- ❌ PII redaction (V5)
- ❌ Build-command sandboxing (V5)
- ❌ /assemble eject command (Spike XII)
- ❌ Phase G empty-mac dogfood (Spike XIII)
- ❌ F4 perf collapse (separate spike)
- ❌ roles.json file (separate spike)

---

## Source

- Spec: `docs/specs/2026-05-04-v4-spike-x-design.md`
- Plan: this file
- Parent project memory: `project_assemble_v4_spec.md`
- Sibling plans: `2026-05-04-v4-spike-vii.md`, `2026-05-04-v4-spike-viii.md`,
  `2026-05-04-v4-spike-ix.md`
