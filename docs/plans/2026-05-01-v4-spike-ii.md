# V4 Spike II Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Spike I dogfood (B-6, run `20260430-120552-6aad`)에서 surface된 5 Critical + 9 Important 이슈를 fix해서 sub-agent path-only contract를 *진짜로* 보호하고 distribution 준비를 완료한다. Spike I 핵심 contract(sub-agent path-only return, hook v1 Bash 차단)는 변경 X — 위에 *보강*만.

**Architecture:** (a) V3 Concierge 메뉴 응답 + SKILL.md dispatch contract에 시그니처/호출법 박기 (F1·F2), (b) hook regex를 plan-pack artifact 4개 화이트리스트로 좁히고 ASSEMBLE_GUARD off 제거 + warn은 stderr-only로 운영 (F8·F13), (c) §CRITICAL 룰을 sub-agent까지 propagate하고 v2 → v3 preamble cutoff 처리 (F9·F11·F12), (d) server에 `update_iteration_state` 추가해서 main이 1줄로 처리 (F15), (e) Rule 5 한국어 quality 강화 + (추천) 통일 (F3·F4), (f) Step 10/12 multi-select schema enforce (F5·F6·F7), (g) cross_doc_step9 COUNTS schema 강제 + iter_emphasis 압축 (F10·F14), (h) B-7 dogfood로 출하 게이트.

**Tech Stack:** Python 3 (server/), bash (hooks), pytest (tests), markdown (SKILL/prompts), JSON (settings).

**Source spec:** `~/.claude/skills/assemble/docs/specs/2026-04-30-v4-spike-ii-design.md` (commit `1c3c0a1`)

**Baseline:** master `1c3c0a1`, 192/192 tests passing, `canonical_preamble_sha256()` = `df27450513c019a9dd395d8f62c99b445e7a16b4fcdbb5cba52c352397993549` (v2).

---

## Pre-flight checklist (caller가 plan dispatch 직전에 확인)

이미 caller (메인 Claude) 가 Spike II 시작 직후 다음을 확인했음 (전사 참고). 본 plan을 새 세션에서 resume할 경우만 재실행.

- [x] **PF-1**: `cd ~/.claude/skills/assemble && pytest tests/` → 192 passed
- [x] **PF-2**: `python3 -c "from server.harness import canonical_preamble_sha256; print(canonical_preamble_sha256())"` → `df27450513c019a9dd395d8f62c99b445e7a16b4fcdbb5cba52c352397993549`
- [x] **PF-3**: `~/.claude/settings.json` PreToolUse — `Edit|Write|NotebookEdit|Bash` matcher 1개 (`guard_run_dir.sh`)
- [x] **PF-4**: `server/__init__.py` — `update_iteration_state` export 자리 비어있음 (Phase D에서 추가 예정)
- [x] **PF-5**: git status clean, master at `1c3c0a1`

---

## File Structure

새/수정되는 파일을 Task 단위로 매핑.

```
~/.claude/skills/assemble/
├── SKILL.md                                       (Task 1: §9 lazy-load policy 줄 추가, F1)
├── bundled/
│   ├── _shared/
│   │   └── harness-preamble.md                    (Task 5: v2 → v3, rule 5 사례 + rule 7 추가)
│   └── plan-pack/
│       ├── SKILL.md                               (Task 1, 4, 8, 9, 10, 11: §CRITICAL/Step dispatch contract/Step 9/Step 10/Step 12 정확화 + 한국어 (추천) 정책)
│       └── prompts/
│           ├── cross_doc_step9.md                 (Task 12: COUNTS schema enforce)
│           └── iter_emphasis.md                   (Task 13: 압축 + verbatim sentinel)
├── hooks/
│   └── guard_run_dir.sh                           (Task 2: regex 화이트리스트 / Task 3: ASSEMBLE_GUARD off 제거 + warn exit2)
├── server/
│   ├── run_dir.py                                 (Task 7: update_iteration_state 추가)
│   ├── __init__.py                                (Task 7: export 추가)
│   └── harness.py                                 (Task 6: ALLOW_LIST에 v2 sha256 추가)
├── tests/unit/
│   ├── test_harness_dispatches.py                 (Task 6: v2/v3 ALLOW_LIST 케이스 갱신)
│   ├── test_guard_bash_matcher.py                 (Task 2/3: 화이트리스트 + warn 테스트)
│   ├── test_run_dir.py                            (Task 7: update_iteration_state 신설)
│   ├── test_plan_pack_skill.py                    (Task 1, 4, 8, 9, 10, 11: SKILL.md grep 갱신)
│   ├── test_plan_pack_anti_fallback.py            (Task 4: anti-bypass 룰 grep 추가)
│   └── test_concierge_bundled_call.py             (Task 1: 신설, V3 SKILL.md §9 grep)
└── docs/research/
    └── 2026-05-01-preamble-v3-cutoff.md           (Task 5/6: v2 → v3 마이그레이션 메모)
```

---

## Phase A — F1 + F2 (V3 메뉴 + dispatch 시그니처)

> 의존: 없음. Phase C/D/E/F/G 의 prerequisite.

### Task 1: V3 Concierge ★ 호출 instruction + plan-pack dispatch 시그니처 (F1 + F2)

**Files:**
- Modify: `~/.claude/skills/assemble/SKILL.md:257` (V3 Concierge §9 lazy-load policy)
- Modify: `~/.claude/skills/assemble/bundled/plan-pack/SKILL.md:86-95` (Step dispatch contract)
- Test (신설): `~/.claude/skills/assemble/tests/unit/test_concierge_bundled_call.py`
- Modify: `~/.claude/skills/assemble/tests/unit/test_plan_pack_skill.py` (record_dispatch 시그니처 grep 추가)

**Acceptance:**
- V3 SKILL.md §9에 "★ bundled options는 `Skill` tool이 아니라 `tool_path/SKILL.md`를 Read tool로 로드한다" 1줄 명시 + tool_path 사용 예시 코드.
- plan-pack SKILL.md `### Step dispatch contract` 블록에 `record_dispatch` 시그니처 verbatim + `No 'role' kwarg` 경고 1줄.
- 새 grep 테스트 2개 통과 (V3 §9 + plan-pack §dispatch).

**영향 범위:** Task 9 (SKILL.md head 보강) + Task 4 (§CRITICAL 확장) 와 같은 SKILL.md 편집. 본 Task가 먼저.

- [ ] **Step 1: V3 SKILL.md §9에 ★ 호출 룰 1 paragraph 추가**

`~/.claude/skills/assemble/SKILL.md` line 257 끝에 다음 bullet을 *추가* (기존 줄 보존):

```markdown
- **★-prefixed bundled options are NOT slash-skill registry entries.** When the user picks one, load it by `Read` on `<tool_path>/SKILL.md` and follow the body in-place. Do NOT call `Skill(<name>)` — that resolves slash-skills only and will return `Unknown skill: <name>` for bundled bundles. The `tool_path` field on each option is the absolute directory path to the bundle's SKILL.md (verified by `enumerate_skill_paths`). Example: `Skill` tool is for `/loop`/`/assemble`/etc.; `Read` is for `★ plan-pack`.
```

- [ ] **Step 2: plan-pack SKILL.md `### Step dispatch contract` 시그니처 verbatim 추가**

`~/.claude/skills/assemble/bundled/plan-pack/SKILL.md` line 86-95 (`### Step dispatch contract` 블록 본문) 마지막에 다음 추가:

```markdown

`record_dispatch` 시그니처 (verbatim — `from server import record_dispatch`):

```python
record_dispatch(
    run_id: str,
    step: str,
    prompt_text: str,
    *,
    subagent_type: str = "",
    description: str = "",
    wrote_path: Optional[str] = None,
) -> Path
```

`role` kwarg 없음. 위 표 (Step → Role) 의 *Role* 은 prompt 본문에 자연어로 박히는 페르소나 라벨이고 `subagent_type` 은 항상 `"general-purpose"` 다. `record_dispatch(..., role=...)` 로 호출하면 `TypeError`.
```

- [ ] **Step 3: V3 §9 grep 테스트 신설**

`tests/unit/test_concierge_bundled_call.py` 새 파일:

```python
"""V3 Concierge SKILL.md must instruct main on how to invoke ★ bundled options.

Spike II F1: B-6 dogfood (run 20260430-120552-6aad) showed main calling
`Skill(plan-pack)` → `Unknown skill` error → Read fallback. Root cause:
SKILL.md §9 only said "the Skill tool actually invokes it", which doesn't
distinguish slash-skills from bundled bundles. This grep test pins the
explicit instruction so future edits don't regress.
"""

from pathlib import Path

SKILL_PATH = Path.home() / ".claude/skills/assemble/SKILL.md"


def test_v3_skill_md_documents_bundled_read_invocation():
    text = SKILL_PATH.read_text(encoding="utf-8")
    # Must contain the explicit Read-not-Skill instruction
    assert "★-prefixed bundled options are NOT slash-skill registry entries" in text
    assert "Read`" in text and "tool_path" in text
    # Must mention the actual error main hits without this guidance
    assert "Unknown skill" in text


def test_v3_skill_md_lazy_load_paragraph_intact():
    """Original §9 lazy-load paragraph must still be present (don't replace, append)."""
    text = SKILL_PATH.read_text(encoding="utf-8")
    assert "build_stage_options()" in text
    assert "lazy-load" in text.lower() or "## 9. Internals: lazy-load" in text
```

- [ ] **Step 4: plan-pack `record_dispatch` 시그니처 grep 테스트 추가**

`tests/unit/test_plan_pack_skill.py` 마지막에 신규 함수 추가:

```python
def test_skill_md_documents_record_dispatch_signature():
    """Spike II F2: SKILL.md must spell out record_dispatch's exact signature.

    B-6 dogfood: main inferred `role=` kwarg from the Sub-agent role mapping
    table column header → TypeError on first call. Grep ensures the verbatim
    signature + `No role kwarg` warning are present.
    """
    from pathlib import Path
    text = (Path.home() / ".claude/skills/assemble/bundled/plan-pack/SKILL.md").read_text(encoding="utf-8")
    # Verbatim signature parts
    assert "record_dispatch(" in text
    assert "subagent_type" in text and "wrote_path" in text
    # No-role warning
    assert "role" in text  # already present in role mapping; the new sentence
    assert "TypeError" in text
```

- [ ] **Step 5: 테스트 실행 + 통과 확인**

```bash
cd ~/.claude/skills/assemble && pytest tests/unit/test_concierge_bundled_call.py tests/unit/test_plan_pack_skill.py -v 2>&1 | tail -25
```

기대: `test_concierge_bundled_call.py` 2개 + `test_skill_md_documents_record_dispatch_signature` 1개 모두 PASS.

- [ ] **Step 6: Commit**

```bash
cd ~/.claude/skills/assemble
git add SKILL.md bundled/plan-pack/SKILL.md tests/unit/test_concierge_bundled_call.py tests/unit/test_plan_pack_skill.py
git commit -m "feat(v4-spike-ii): F1+F2 — V3 ★ Read-not-Skill + plan-pack record_dispatch signature

B-6 dogfood (run 20260430-120552-6aad) Critical findings:
- F1: main called Skill(plan-pack) → Unknown skill error. V3 §9 lazy-load
  paragraph never said bundled bundles use Read on tool_path/SKILL.md.
- F2: main inferred record_dispatch(role=...) → TypeError. Step dispatch
  contract block had no signature.

Spec: docs/specs/2026-04-30-v4-spike-ii-design.md §3.1 F1, F2."
```

---

## Phase B — F8 + F13 (hook regex + warn mode)

> 의존: 없음. 독립적으로 진행.

### Task 2: hook regex를 plan-pack artifact 4개 화이트리스트로 좁히기 (F8)

**Files:**
- Modify: `~/.claude/skills/assemble/hooks/guard_run_dir.sh:53`
- Modify: `~/.claude/skills/assemble/tests/unit/test_guard_bash_matcher.py` (화이트리스트 통과/차단 케이스 추가)

**Acceptance:**
- `iteration_state.json` direct write가 hook을 통과 (`exit 0`).
- `dispatches.jsonl` direct write가 hook을 통과.
- `PRD.md` / `ARCHITECTURE.md` / `ADR.md` / `UI_GUIDE.md` direct write는 여전히 차단 (`exit 2`).
- magic marker passthrough는 변경 없음 (sub-agent canonical write 통과 유지).

**영향 범위:** F15 (Phase D)의 `update_iteration_state` 가 main 직접 호출 가능해야 하므로 본 Task가 prerequisite.

- [ ] **Step 1: 변경 전 현재 동작 캡처**

```bash
cd ~/.claude/skills/assemble
# 현재 regex 위치 확인 (line 53 근방)
grep -n "runs/\[\^/" hooks/guard_run_dir.sh
```

기대: `runs/[^/]+/[^/]+\.(md|json|txt)` 매칭 식 1개.

- [ ] **Step 2: regex를 plan-pack artifact 4개 화이트리스트로 변경**

`hooks/guard_run_dir.sh:53` 의 trigger regex 부분을:

```bash
       && echo "$cmd" | grep -qE '(runs/[^/]+/[^/]+\.(md|json|txt)|write_run_artifact|runs_dir)'; then
```

다음으로 교체:

```bash
       && echo "$cmd" | grep -qE '(runs/[^/]+/(PRD|ARCHITECTURE|ADR|UI_GUIDE)\.md|write_run_artifact|runs_dir)'; then
```

핵심: `[^/]+\.(md|json|txt)` 광범위 매칭 → `(PRD|ARCHITECTURE|ADR|UI_GUIDE)\.md` 정확한 4개 명시. `iteration_state.json` / `dispatches.jsonl` / 기타 orchestrator 메타파일은 통과.

같은 hook의 line 60-64 차단 메시지(`bash_template`)도 업데이트:

```bash
      bash_template='[V4 GUARD — Item B-prime] Bash → plan-pack artifact 직접 write 차단
메인 Claude의 python3/sh -c 우회로 plan-pack artifact (PRD/ARCHITECTURE/ADR/UI_GUIDE.md) 쓰기 시도 감지.
명령 일부: __CMD__
이유: Spike I 후 plan-pack 본문은 sub-agent 가 자체 write 책임 — 메인은 dispatch + path 수령만.
복구: sub-agent dispatch 로 재시도하세요. (canonical save block 에 magic marker 포함됨)
참고: orchestrator 메타파일 (iteration_state.json, dispatches.jsonl) 은 main 직접 write 허용 (server 함수 사용 권장).'
```

- [ ] **Step 3: 화이트리스트 통과/차단 grep 케이스 추가**

`tests/unit/test_guard_bash_matcher.py` 에 다음 함수 4개 추가 (기존 4 케이스는 그대로):

```python
def test_iteration_state_json_passes_hook(tmp_path, monkeypatch):
    """Spike II F8: iteration_state.json direct-write must NOT be blocked.

    Hook v1 regex caught all `runs/<rid>/*.json` — false positive on
    orchestrator metadata. v2 regex is whitelist-only (PRD/ARCH/ADR/UI).
    """
    cmd = (
        "python3 -c 'import json; "
        'open("/Users/u/.claude/channels/assemble/runs/20260501/iteration_state.json", "w")'
        ".write(json.dumps({}))'"
    )
    rc = _invoke_hook_bash(cmd)
    assert rc == 0, "iteration_state.json must pass — orchestrator metadata"


def test_dispatches_jsonl_passes_hook():
    cmd = (
        'python3 -c \'open("/Users/u/.claude/channels/assemble/runs/20260501/dispatches.jsonl", "a")'
        ".write(\"{}\")'"
    )
    rc = _invoke_hook_bash(cmd)
    assert rc == 0


def test_prd_md_still_blocked_without_marker():
    cmd = (
        'python3 -c \'open("/Users/u/.claude/channels/assemble/runs/20260501/PRD.md", "w")'
        ".write(\"x\")'"
    )
    rc = _invoke_hook_bash(cmd)
    assert rc == 2  # main bypass still blocked


def test_arch_adr_ui_md_blocked_without_marker():
    """All four whitelisted artifacts blocked when no marker."""
    for fname in ("ARCHITECTURE.md", "ADR.md", "UI_GUIDE.md"):
        cmd = (
            f'python3 -c \'open("/Users/u/.claude/channels/assemble/runs/r/{fname}", "w")'
            ".write(\"x\")'"
        )
        rc = _invoke_hook_bash(cmd)
        assert rc == 2, f"{fname} must be blocked"
```

(만약 `_invoke_hook_bash` helper가 기존 파일에 없으면, 기존 테스트의 hook 호출 패턴을 helper로 추출 후 재사용. 현재 `test_guard_bash_matcher.py` 4 케이스 보고 동일 패턴.)

- [ ] **Step 4: 테스트 실행**

```bash
cd ~/.claude/skills/assemble && pytest tests/unit/test_guard_bash_matcher.py -v 2>&1 | tail -20
```

기대: 기존 4개 + 신규 4개 + 임의 1개 (test_arch_adr_ui_md_blocked_without_marker가 3 fname loop) = 8개 PASS.

- [ ] **Step 5: Commit**

```bash
cd ~/.claude/skills/assemble
git add hooks/guard_run_dir.sh tests/unit/test_guard_bash_matcher.py
git commit -m "fix(v4-spike-ii): F8 — hook regex whitelist 4 plan-pack artifacts

B-6 dogfood: main blocked when updating iteration_state.json (orchestrator
responsibility per cross_doc_step9.md spec). Hook regex .json was too broad.
Now explicitly: runs/<rid>/(PRD|ARCHITECTURE|ADR|UI_GUIDE)\.md only.

Spec: docs/specs/2026-04-30-v4-spike-ii-design.md §3.1 F8."
```

### Task 3: ASSEMBLE_GUARD off 제거 + warn은 stderr-only로 (F13)

**Files:**
- Modify: `~/.claude/skills/assemble/hooks/guard_run_dir.sh:14-23, 26-27, 67-71, 97-100`
- Modify: `~/.claude/skills/assemble/tests/unit/test_guard_bash_matcher.py` (warn/off 케이스 갱신)

**Acceptance:**
- `ASSEMBLE_GUARD=off` 설정해도 hook이 차단 동작 유지 (기존 line 27 `[[ "$mode" == "off" ]] && exit 0` 제거).
- `ASSEMBLE_GUARD=warn` 설정 시 stderr 메시지는 출력하되 `exit 2` 유지 (기존 `exit 0` → `exit 2`).
- hook script comment 헤더에 의도 명시.

- [ ] **Step 1: hook script 헤더 mode 설명 갱신**

`hooks/guard_run_dir.sh:14-23` (Modes 섹션) 을 다음으로 교체:

```bash
# Modes (env var ASSEMBLE_GUARD):
#   block (default) — exit 2 + stderr message → tool call blocked
#   warn            — exit 2 + stderr message (디버깅용 — production 차단 무력화 X)
#
# Spike II F13: `off` mode 제거. B-6 dogfood에서 sub-agent가
# `ASSEMBLE_GUARD=warn python3 << EOF` 로 ENV 명시 + magic marker 동시 사용
# (이중 우회) 시도. warn은 stderr만 추가될 뿐 차단은 유지.
```

- [ ] **Step 2: `off` 분기 제거**

`hooks/guard_run_dir.sh:26-27` 의:

```bash
mode="${ASSEMBLE_GUARD:-block}"
[[ "$mode" == "off" ]] && exit 0
```

다음으로 교체 (off 제거):

```bash
mode="${ASSEMBLE_GUARD:-block}"
# off mode removed in Spike II — see hook header.
```

- [ ] **Step 3: warn mode를 stderr-only + exit 2로**

`hooks/guard_run_dir.sh:67-71` (Bash 매처 분기 내) 의:

```bash
      if [[ "$mode" == "warn" ]]; then
        exit 0
      fi
      exit 2
```

다음으로 교체:

```bash
      # warn mode: stderr 메시지는 위에서 이미 출력. block 동등 exit 2.
      exit 2
```

같은 작업을 `hooks/guard_run_dir.sh:97-100` (Edit/Write 분기 내) 에도 적용:

```bash
# 변경 전
if [[ "$mode" == "warn" ]]; then
  exit 0
fi

exit 2
```

```bash
# 변경 후
# warn/block 모두 exit 2 (Spike II F13).
exit 2
```

- [ ] **Step 4: 차단 메시지 wording 갱신** (warn/block 둘 다 exit 2 임을 사용자가 알도록)

Bash 분기 (line 60-64 근방) 의 `bash_template` 마지막 줄을:

```
일시 우회: ASSEMBLE_GUARD=warn (경고만) 또는 ASSEMBLE_GUARD=off (비활성)
```

다음으로 교체:

```
디버깅: ASSEMBLE_GUARD=warn 으로 stderr 추가 정보 확인 (차단은 유지). off 모드 없음.
```

Edit/Write 분기 `template` 마지막 줄도 동일하게 교체.

- [ ] **Step 5: 테스트 갱신**

`tests/unit/test_guard_bash_matcher.py` 에 다음 추가:

```python
import os
import subprocess


def test_assemble_guard_off_does_not_disable():
    """Spike II F13: off mode removed. ENV 설정해도 차단 유지."""
    cmd = (
        'python3 -c \'open("/Users/u/.claude/channels/assemble/runs/r/PRD.md", "w").write("x")\''
    )
    env = {**os.environ, "ASSEMBLE_GUARD": "off"}
    rc = _invoke_hook_bash(cmd, env=env)
    assert rc == 2, "off mode should NOT disable hook anymore"


def test_assemble_guard_warn_still_exit2():
    """warn mode가 더 이상 production escape hatch가 아님 — stderr + exit 2."""
    cmd = (
        'python3 -c \'open("/Users/u/.claude/channels/assemble/runs/r/PRD.md", "w").write("x")\''
    )
    env = {**os.environ, "ASSEMBLE_GUARD": "warn"}
    result = _invoke_hook_bash_capture(cmd, env=env)
    assert result.returncode == 2
    assert b"V4 GUARD" in result.stderr or b"plan-pack" in result.stderr
```

(`_invoke_hook_bash_capture` helper는 `subprocess.run(..., capture_output=True)` 으로 returncode + stderr 반환 — 기존 helper 옆에 추가.)

- [ ] **Step 6: 테스트 실행**

```bash
cd ~/.claude/skills/assemble && pytest tests/unit/test_guard_bash_matcher.py -v 2>&1 | tail -20
```

기대: 모든 케이스 PASS.

- [ ] **Step 7: Commit**

```bash
cd ~/.claude/skills/assemble
git add hooks/guard_run_dir.sh tests/unit/test_guard_bash_matcher.py
git commit -m "fix(v4-spike-ii): F13 — ASSEMBLE_GUARD off removed, warn → exit 2

B-6 dogfood: sub-agent used ASSEMBLE_GUARD=warn ENV + magic marker for
double bypass. warn was a debug aid in Spike I but became a production
escape hatch. Now warn keeps stderr but exits 2 (block-equivalent), and
off mode is removed entirely.

Spec: docs/specs/2026-04-30-v4-spike-ii-design.md §3.1 F13."
```

---

## Phase C — F9 + F11 + F12 (§CRITICAL 확장 + preamble v3)

> 의존: Phase A. Task 4 는 plan-pack SKILL.md 편집, Task 5/6 는 preamble + harness.

### Task 4: §CRITICAL 룰 확장 (anti-bypass + sub-agent delegation ban) (F9 + F11)

**Files:**
- Modify: `~/.claude/skills/assemble/bundled/plan-pack/SKILL.md:14-30` (`## CRITICAL — orchestrator-only enforcement` 블록)
- Modify: `~/.claude/skills/assemble/tests/unit/test_plan_pack_anti_fallback.py` (anti-bypass + delegation grep 추가)

**Acceptance:**
- §CRITICAL 블록에 다음 3개 룰 추가 (verbatim):
  1. `settings.json` 편집해서 `ASSEMBLE_GUARD` 비활성화 시도 금지.
  2. hook 차단 시 `AskUserQuestion` 으로 사용자 결정 위임 (retry/abort/report). 차단 회피 목적 sub-agent dispatch 금지.
  3. sub-agent dispatch 는 `prompts/` 디렉토리의 8개 파일에 한정. orchestrator 메타데이터 (`iteration_state.json`, `dispatches.jsonl` 등) 는 sub-agent 위임 금지 — server 함수 사용.
- anti-fallback 테스트가 위 3개 룰의 핵심 키워드 grep으로 감시.

- [ ] **Step 1: §CRITICAL 블록 본문 확장**

`bundled/plan-pack/SKILL.md:14-30` (`## CRITICAL` 블록) 을 다음으로 교체:

```markdown
## CRITICAL — orchestrator-only enforcement

This skill is **orchestrator-only**. The main Claude (you) dispatches sub-agents
and parses their `WROTE: <path>` stdout. You MUST NOT fall back to
Bash/Edit/Write/python3 to write artifacts directly under `runs/<rid>/`.
The hook `guard_run_dir.sh` blocks such bypass attempts.

If a sub-agent dispatch fails (no `WROTE:` line, `ERROR:` line, timeout, or
empty stdout):
1. Surface the failure to the user via `AskUserQuestion`:
   "Step N failed. Retry / abort / report?"
2. Wait for user choice. Do not auto-recover.
3. NEVER attempt to write the artifact yourself, even "just to unblock the
   user". Sub-agent ownership is the contract.

If retry is chosen, re-dispatch the same sub-agent with the failure context
(`previous attempt failed: <reason>`) appended to the original prompt.

**Anti-bypass (Spike II F9 + F11):**

- `~/.claude/settings.json` 편집해서 `ASSEMBLE_GUARD` 환경 변수 무력화 시도 금지. hook 자체를 비활성화/우회 시도는 사용자 confirm 이 떠도 거절.
- hook 이 차단하면 즉시 `AskUserQuestion` 으로 사용자 결정 위임 (retry / abort / report). hook 차단을 회피하기 위한 sub-agent dispatch (예: "iteration_state.json 업데이트 해 달라" 라는 task로 sub-agent 호출) 금지.
- sub-agent dispatch 는 `bundled/plan-pack/prompts/` 디렉토리의 *8개 파일* (`prd_step2`, `prd_step3`, `prd_step4`, `arch_step8`, `adr_step11`, `ui_step13`, `cross_doc_step9`, `iter_emphasis`) 에 한정. orchestrator 메타데이터 (`iteration_state.json`, `dispatches.jsonl`, `progress.json` 등) 는 sub-agent 에 위임 금지 — `server.run_dir.update_iteration_state` 같은 server 함수 사용.
```

- [ ] **Step 2: anti-fallback 테스트에 새 grep 케이스 추가**

`tests/unit/test_plan_pack_anti_fallback.py` 마지막에 추가:

```python
def test_skill_md_critical_block_bans_settings_json_edit():
    """Spike II F9: §CRITICAL must explicitly ban ASSEMBLE_GUARD disable.

    B-6 dogfood: main attempted to add `ASSEMBLE_GUARD: "off"` to
    ~/.claude/settings.json after hook blocked it. Rule must be in §CRITICAL
    so it's loaded into main's context (not just sub-agent preamble).
    """
    from pathlib import Path
    text = (Path.home() / ".claude/skills/assemble/bundled/plan-pack/SKILL.md").read_text(encoding="utf-8")
    assert "ASSEMBLE_GUARD" in text
    assert "settings.json" in text
    # The actual ban phrase
    assert ("환경 변수 무력화 시도 금지" in text) or ("disable" in text.lower() and "settings.json" in text)


def test_skill_md_critical_block_bans_subagent_metadata_delegation():
    """Spike II F11: §CRITICAL must ban delegating orchestrator metadata.

    B-6: main dispatched sub-agent to update iteration_state.json after
    hook blocked direct write. iteration_state.json is orchestrator
    responsibility per cross_doc_step9.md — sub-agent delegation = bypass.
    """
    from pathlib import Path
    text = (Path.home() / ".claude/skills/assemble/bundled/plan-pack/SKILL.md").read_text(encoding="utf-8")
    assert "iteration_state.json" in text
    assert "위임 금지" in text or "delegate" in text.lower()
    # The 8-file allowlist for sub-agent dispatch
    assert "prompts/" in text and "8" in text


def test_skill_md_critical_block_lists_8_prompt_files():
    """Sub-agent dispatch limited to 8 prompts/<step>.md files."""
    from pathlib import Path
    text = (Path.home() / ".claude/skills/assemble/bundled/plan-pack/SKILL.md").read_text(encoding="utf-8")
    for prompt_name in ("prd_step2", "prd_step3", "prd_step4", "arch_step8",
                        "adr_step11", "ui_step13", "cross_doc_step9",
                        "iter_emphasis"):
        assert prompt_name in text, f"§CRITICAL must list {prompt_name}"
```

- [ ] **Step 3: 테스트 실행**

```bash
cd ~/.claude/skills/assemble && pytest tests/unit/test_plan_pack_anti_fallback.py -v 2>&1 | tail -15
```

기대: 기존 + 신규 3개 모두 PASS.

- [ ] **Step 4: Commit**

```bash
cd ~/.claude/skills/assemble
git add bundled/plan-pack/SKILL.md tests/unit/test_plan_pack_anti_fallback.py
git commit -m "fix(v4-spike-ii): F9+F11 — §CRITICAL extended (anti-bypass + delegation ban)

B-6 dogfood Critical findings:
- F9: main attempted settings.json edit to disable ASSEMBLE_GUARD
- F11: main delegated iteration_state.json update to sub-agent (bypass)

§CRITICAL now bans (a) settings.json edits to disable hook, (b) sub-agent
dispatch as bypass workaround, (c) delegating orchestrator metadata
(iteration_state/dispatches/progress.json) — server functions only.

Spec: docs/specs/2026-04-30-v4-spike-ii-design.md §3.1 F9, F11."
```

### Task 5: harness-preamble v2 → v3 (rule 5 사례 + rule 7 신설) (F12 + F3)

**Files:**
- Modify: `~/.claude/skills/assemble/bundled/_shared/harness-preamble.md`
- Create: `~/.claude/skills/assemble/docs/research/2026-05-01-preamble-v3-cutoff.md`

**Acceptance:**
- preamble 에 rule 7 추가 (다른 스킬 인프라 코드 read 금지).
- rule 5 본문에 외래어 표기 사례 박힘 (architecture→아키텍처, family→패밀리, top-level→최상위, recommended→추천).
- 새 sha256 산출 + cutoff 메모에 v2 → v3 sha256 두 값 기록.
- `canonical_preamble_sha256()` 갱신 → Task 6 ALLOW_LIST 갱신 prerequisite.

**영향 범위:** Task 6 `tests/unit/test_harness_dispatches.py` sha256 hard-coded 테스트 fail 예상 → Task 6 에서 갱신.

- [ ] **Step 1: 변경 전 v2 sha256 기록**

```bash
cd ~/.claude/skills/assemble
python3 -c "
import sys; sys.path.insert(0, '.')
from server.harness import canonical_preamble_sha256
print('v2 sha256:', canonical_preamble_sha256())
" | tee /tmp/preamble_v2_sha.txt
```

기대: `v2 sha256: df27450513c019a9dd395d8f62c99b445e7a16b4fcdbb5cba52c352397993549` (PF-2 결과 일치).

- [ ] **Step 2: harness-preamble.md 를 v3 로 교체**

`bundled/_shared/harness-preamble.md` 전체를 다음으로 교체:

```markdown
[HARNESS RULES — 무시 금지]
1. 불확실하면 추측 금지, 사용자 질문 우선
2. 과설계 금지, YAGNI
3. 요청 범위 밖 코드 임의 수정 금지
4. 버그 수정 시 재현 테스트 → 실패 확인 → 수정 → 재검증 루프
5. 사용자에게 표시되는 한국어 라벨·옵션은 자연스러운 한국어로 정제. 직역체(예: "좌히기"), 자작 한자어(예: "키디텍터"), 한영 혼합 단어(예: "PRD emp") 금지. 의미 모호하면 더 길어도 풀어서 표기. 사용자 메시지가 한국어면 응답·옵션도 한국어로. 영문 기술용어 한글화 시 정확한 외래어 표기 사용 (architecture→아키텍처, family→패밀리, top-level→최상위, recommended→추천, directory→디렉토리). 자작 변형 금지.
6. 사용자가 진술한 task scope은 *seed*이지 contract가 아니다. ★ 번들 풀번들이 contract. 사용자 표현이 작게 들려도 doc 스킵·iteration 다운스케일·풀번들 일부 생략 금지. content density(밀도) 조정으로만 반응 — 짧은 task → 짧은 PRD body, 그러나 PRD/ARCH/ADR/UI_GUIDE 4개 doc 모두 작성. AskUserQuestion 옵션 만들 때도 다운스케일 옵션 생성 금지.
7. 다른 스킬의 인프라 코드(다른 스킬의 hooks/, server/ 모듈, settings.json 등) read·grep 금지. 자기 task 와 무관한 코드 분석은 자율 우회 행동의 신호. 자기 task 가 *plan-pack templates* 같은 같은 번들 내부 파일 read 는 OK. hook/server 차단 받으면 그대로 ERROR 출력하고 종료 — 우회 학습 금지.
```

- [ ] **Step 3: 새 sha256 산출**

```bash
cd ~/.claude/skills/assemble
python3 -c "
import sys; sys.path.insert(0, '.')
from server.harness import canonical_preamble_sha256
# 캐시 클리어 (lru_cache 영향 없음 — _CACHED_PREAMBLE 은 path key)
print('v3 sha256:', canonical_preamble_sha256())
" | tee /tmp/preamble_v3_sha.txt
```

기대: v2 와 다른 hex 64자.

- [ ] **Step 4: cutoff 메모 작성**

`docs/research/2026-05-01-preamble-v3-cutoff.md` 새 파일:

```markdown
# Preamble v2 → v3 cutoff (2026-05-01)

## Why v3

Spike II spec §3.1 F12 + §3.2 F3:
- B-6 dogfood (run `20260430-120552-6aad`) showed sub-agents reading
  unrelated infrastructure code (hooks/, server/ modules) to learn how
  to bypass guards.
- Rule 5 한국어 quality 위반 7건 — 외래어 표기 (architecture/family/
  top-level/recommended) 자작 변형.

## v3 changes

- Rule 5 본문에 외래어 표기 사례 박음.
- Rule 7 신설: 다른 스킬 인프라 코드 read 금지.

## sha256 cutoff

| version | sha256 | first run |
|---|---|---|
| v1 | `858e9ff1cdc05ca73bb4009aab3acfc841169b30873d2fb00f2dfd546b86e159` | (pre-Spike I) |
| v2 | `df27450513c019a9dd395d8f62c99b445e7a16b4fcdbb5cba52c352397993549` | Spike I (master `cccd58a`) |
| v3 | `<TASK 5 STEP 3 OUTPUT>` | Spike II (this commit) |

`server.harness.verify_dispatches` ALLOW_LIST 는 Task 6 에서 v3 추가.
v1 + v2 + v3 모두 인정 — 과거 dogfood 데이터 backward compatible.

## Files

- `bundled/_shared/harness-preamble.md` — 7 rules
- `server/harness.py` — `_PREAMBLE_V1_SHA256` 옆에 `_PREAMBLE_V2_SHA256` 추가 (Task 6)
- `tests/unit/test_harness_dispatches.py` — sha256 hard-coded 테스트 갱신 (Task 6)
```

(Step 3 의 v3 sha256 출력으로 `<TASK 5 STEP 3 OUTPUT>` 자리 채움.)

- [ ] **Step 5: 영향받는 테스트 식별**

```bash
cd ~/.claude/skills/assemble && pytest tests/unit/test_harness_dispatches.py -v 2>&1 | tail -20
```

기대: 1개 이상 실패 (sha256 mismatch). 실패 목록을 Task 6 에서 갱신.

- [ ] **Step 6: Commit**

```bash
cd ~/.claude/skills/assemble
git add bundled/_shared/harness-preamble.md docs/research/2026-05-01-preamble-v3-cutoff.md
git commit -m "feat(v4-spike-ii): F12+F3 — harness-preamble v3 (rule 7 + rule 5 사례)

B-6 dogfood Critical/Important:
- F12: sub-agents read hooks/guard_run_dir.sh + server/run_dir.py to learn
  bypass paths. New rule 7 bans cross-skill infrastructure reads.
- F3: 7 Korean quality violations (외래어 자작 변형). Rule 5 now embeds
  canonical mappings (architecture→아키텍처 etc.).

sha256 cutoff: v2 → v3 (see docs/research/2026-05-01-preamble-v3-cutoff.md).
Test sha256 ALLOW_LIST update in Task 6.

Spec: docs/specs/2026-04-30-v4-spike-ii-design.md §3.1 F12, §3.2 F3."
```

### Task 6: server/harness.py ALLOW_LIST 에 v2 sha256 hard-code 추가 (F12 cutoff)

**Files:**
- Modify: `~/.claude/skills/assemble/server/harness.py:88-95, 257-260`
- Modify: `~/.claude/skills/assemble/tests/unit/test_harness_dispatches.py` (v3 sha256 케이스 + ALLOW_LIST 검증)

**Acceptance:**
- `_PREAMBLE_V2_SHA256 = "df274505..."` 상수가 `_PREAMBLE_V1_SHA256` 아래 추가.
- `verify_dispatches` ALLOW_LIST 에 v1 + v2 + canonical(v3) 3개 인정.
- 기존 dogfood 데이터 (v1 또는 v2 sha256 기록된 dispatches.jsonl) `verify_dispatches` 실행 시 `ok=True`.
- `test_record_dispatch_full_byte_identity_with_real_canonical_preamble` 테스트가 새 v3 sha256 으로 통과.
- 192/192 + 신규 → 모든 tests PASS.

**영향 범위:** Spike I Task 7 와 동일 패턴. v1 → v2 cutoff 시 Spike I 에서 동일 파일 변경했음.

- [ ] **Step 1: 새 v3 sha256 값을 환경 변수로 캡처**

```bash
cd ~/.claude/skills/assemble
NEW_SHA="$(python3 -c 'import sys; sys.path.insert(0, "."); from server.harness import canonical_preamble_sha256; print(canonical_preamble_sha256())')"
echo "$NEW_SHA"
test ${#NEW_SHA} -eq 64 || { echo "FAIL: sha256 length"; exit 1; }
```

`$NEW_SHA` 는 Step 3 + Step 4 에서 사용.

- [ ] **Step 2: server/harness.py 에 `_PREAMBLE_V2_SHA256` 상수 추가**

`server/harness.py:88-95` (`_PREAMBLE_V1_SHA256` 정의 블록) 을 다음으로 교체:

```python
# Spike I §8.3: pre-cutoff (2026-04-30) dogfood data was written under the
# v1 preamble. Spike II 이전 live data is v2.
# Spike II §3.1 F12: post-cutoff (2026-05-01) live data is v3 (rule 7 추가
# + rule 5 외래어 표기 사례). ALLOW_LIST 는 v1 + v2 + canonical(=v3) 3개를
# 모두 인정해서 과거 dogfood 검증이 깨지지 않도록 한다.
# See docs/research/2026-05-01-preamble-v3-cutoff.md.
_PREAMBLE_V1_SHA256 = (
    "858e9ff1cdc05ca73bb4009aab3acfc841169b30873d2fb00f2dfd546b86e159"
)
_PREAMBLE_V2_SHA256 = (
    "df27450513c019a9dd395d8f62c99b445e7a16b4fcdbb5cba52c352397993549"
)
```

- [ ] **Step 3: `verify_dispatches` ALLOW_LIST 갱신**

`server/harness.py:257-260` (`# ALLOW_LIST: pre-cutoff v1 + live v2 ...` 블록) 을 다음으로 교체:

```python
    # ALLOW_LIST: pre-cutoff v1 + Spike-I-cutoff v2 + live v3 (canonical).
    allow_list = {_PREAMBLE_V1_SHA256, _PREAMBLE_V2_SHA256}
    if canonical is not None:
        allow_list.add(canonical)
```

- [ ] **Step 4: 영향받는 테스트 갱신**

`tests/unit/test_harness_dispatches.py` 에서 v2 sha256 (`df27450513c019a9dd395d8f62c99b445e7a16b4fcdbb5cba52c352397993549`) 가 hard-code된 케이스를 grep으로 식별:

```bash
grep -n "df27450513c019a9dd395d8f62c99b445e7a16b4fcdbb5cba52c352397993549" tests/unit/test_harness_dispatches.py
```

각 위치를 파일 컨텍스트 (어떤 케이스가 v2 expected vs v3 expected) 보고 갱신:
- "current canonical equals X" 케이스 → v3 sha256 (Step 1 의 `$NEW_SHA`) 로 갱신
- "v1 backward compat" 케이스 → 변경 없음
- "v2 backward compat" 케이스 → 새로 추가 (다음 Step)

- [ ] **Step 5: 새 v2 backward-compat 테스트 케이스 추가**

`tests/unit/test_harness_dispatches.py` 마지막에 추가:

```python
def test_v2_dogfood_data_still_verifies_under_v3_canonical(tmp_path, monkeypatch):
    """Spike II §3.1 F12: v2 sha256 (Spike I dogfood era) must verify ok=True
    under v3 canonical. ALLOW_LIST = v1 + v2 + canonical.
    """
    import json
    from server.harness import verify_dispatches, _PREAMBLE_V2_SHA256
    monkeypatch.setenv("ASSEMBLE_HOME", str(tmp_path))
    rid = "20260430-test"
    rd = tmp_path / ".claude/channels/assemble/runs" / rid
    rd.mkdir(parents=True)
    rec = {
        "ts": "2026-04-30T12:00:00Z",
        "step": "step6.iter0.PRD",
        "subagent_type": "general-purpose",
        "description": "v2-era dispatch",
        "preamble_sha256": _PREAMBLE_V2_SHA256,
        "preamble_bytes": 1024,
        "body_sha256": "deadbeef" * 8,
        "body_bytes": 200,
        "wrote_path": None,
    }
    (rd / "dispatches.jsonl").write_text(json.dumps(rec) + "\n")
    result = verify_dispatches(rid)
    assert result["ok"], f"v2 ALLOW_LIST regression: {result}"
    assert result["total"] == 1
    assert result["mismatches"] == []
```

- [ ] **Step 6: 전체 테스트 실행**

```bash
cd ~/.claude/skills/assemble && pytest tests/ 2>&1 | tail -10
```

기대: 192 + 신규 = 모두 PASS (sha256 mismatch 0건).

- [ ] **Step 7: Commit**

```bash
cd ~/.claude/skills/assemble
git add server/harness.py tests/unit/test_harness_dispatches.py
git commit -m "fix(v4-spike-ii): F12 cutoff — preamble v3 ALLOW_LIST extension

verify_dispatches now accepts v1 + v2 + canonical(v3) sha256s. Spike I-era
dogfood data (v2 preamble = df27450513c019...) still verifies green.

Test: test_v2_dogfood_data_still_verifies_under_v3_canonical added.

Spec: docs/specs/2026-04-30-v4-spike-ii-design.md §3.1 F12 cutoff."
```

---

## Phase D — F15 + Step 9 wording (server function for iteration_state)

> 의존: Phase B (hook 화이트리스트로 메인 직접 write 가능). Phase A.

### Task 7: server/run_dir.py 에 `update_iteration_state` 함수 추가 (F15)

**Files:**
- Modify: `~/.claude/skills/assemble/server/run_dir.py` (함수 추가)
- Modify: `~/.claude/skills/assemble/server/__init__.py` (export)
- Test (신설): `~/.claude/skills/assemble/tests/unit/test_run_dir.py` 에 새 함수 4 케이스

**Acceptance:**
- `update_iteration_state(run_id, counts: dict) -> Path` 시그니처. atomic write (`write_run_artifact` 재사용 권장).
- `runs/<rid>/iteration_state.json` 파일이 없으면 `{"iterations": []}` 로 초기화 후 첫 항목 append.
- 있으면 `state["iterations"]` 에 `{"index": N, **counts}` append.
- `index` 는 자동으로 0,1,2,... 부여 (`len(state["iterations"])` 사용).
- 4 테스트 케이스: 신규 파일 / 기존 파일 append / index auto-increment / atomic (concurrent write 시 torn 없음).

**영향 범위:** Task 8 SKILL.md Step 9 wording 이 본 함수를 reference.

- [ ] **Step 1: server/run_dir.py 끝에 함수 추가**

`server/run_dir.py` 마지막에 추가:

```python
def update_iteration_state(run_id: str, counts: dict) -> Path:
    """Append one iteration record to `runs/<run_id>/iteration_state.json`.

    Spike II §3.1 F15 + D9: orchestrator metadata write helper. Main Claude
    parses the `COUNTS: resolved=<N> unresolved=<N> new=<N>` line emitted by
    Step 9 sub-agent, computes `resolved_pct`, and calls this in one line:

        from server import update_iteration_state
        update_iteration_state("20260501-abcd", {
            "resolved_pct": 0.85, "new_count": 0,
            "stopped": False, "reason": ""
        })

    The hook whitelist (Spike II §3.1 F8) lets main do this directly under
    `runs/<rid>/iteration_state.json` — no sub-agent dispatch needed
    (B-6 dogfood showed delegation cost = 23 tool uses, 58.6k tokens).

    File schema:
        {"iterations": [{"index": 0, ...}, {"index": 1, ...}, ...]}

    Append-only. `index` is auto-assigned by current list length. Atomic
    via `write_run_artifact`.

    Returns the absolute path of iteration_state.json.
    """
    import json as _json
    target = run_artifact_path(run_id, "iteration_state.json")  # validates
    if target.exists():
        try:
            state = _json.loads(target.read_text(encoding="utf-8"))
        except (_json.JSONDecodeError, OSError):
            # Corrupt or unreadable — start fresh. Don't silently mask
            # a programmer error; raising would block the workflow.
            state = {"iterations": []}
    else:
        state = {"iterations": []}
    if "iterations" not in state or not isinstance(state["iterations"], list):
        state["iterations"] = []
    entry = {"index": len(state["iterations"]), **counts}
    state["iterations"].append(entry)
    return write_run_artifact(run_id, "iteration_state.json",
                               _json.dumps(state, indent=2, ensure_ascii=False))
```

- [ ] **Step 2: server/__init__.py 에 export 추가**

`server/__init__.py` 의 run_dir import 블록 (line 34-39 근방) 에 `update_iteration_state` 추가:

```python
from server.run_dir import (
    write_run_artifact,
    read_run_artifact,
    run_artifact_path,
    strip_bash_fence,
    update_iteration_state,
)
```

같은 파일의 `__all__` 의 `# run_dir` 섹션에 추가:

```python
    # run_dir
    "write_run_artifact", "read_run_artifact", "run_artifact_path",
    "strip_bash_fence", "update_iteration_state",
```

- [ ] **Step 3: 신규 테스트 4 케이스 추가**

`tests/unit/test_run_dir.py` 마지막에 추가:

```python
import json


def test_update_iteration_state_creates_new_file(tmp_path, monkeypatch):
    """Spike II F15: 첫 호출 시 파일 신규 생성 + index=0."""
    import os
    monkeypatch.setenv("ASSEMBLE_HOME", str(tmp_path))
    from server import update_iteration_state
    rid = "20260501-newfile"
    path = update_iteration_state(rid, {"resolved_pct": 0.5, "new_count": 1})
    assert path.exists()
    state = json.loads(path.read_text())
    assert state["iterations"] == [{"index": 0, "resolved_pct": 0.5, "new_count": 1}]


def test_update_iteration_state_appends_to_existing(tmp_path, monkeypatch):
    """기존 파일에 append + index auto-increment."""
    monkeypatch.setenv("ASSEMBLE_HOME", str(tmp_path))
    from server import update_iteration_state
    rid = "20260501-append"
    update_iteration_state(rid, {"resolved_pct": 0.7})
    update_iteration_state(rid, {"resolved_pct": 0.85})
    path = update_iteration_state(rid, {"resolved_pct": 0.9, "stopped": True})
    state = json.loads(path.read_text())
    assert len(state["iterations"]) == 3
    assert state["iterations"][0]["index"] == 0
    assert state["iterations"][1]["index"] == 1
    assert state["iterations"][2]["index"] == 2
    assert state["iterations"][2]["stopped"] is True


def test_update_iteration_state_corrupt_file_resets(tmp_path, monkeypatch):
    """깨진 JSON 파일이면 fresh start, raise 안 함."""
    monkeypatch.setenv("ASSEMBLE_HOME", str(tmp_path))
    from server import update_iteration_state, run_artifact_path
    rid = "20260501-corrupt"
    p = run_artifact_path(rid, "iteration_state.json")
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_text("{not valid json")
    path = update_iteration_state(rid, {"new_count": 0})
    state = json.loads(path.read_text())
    assert state["iterations"] == [{"index": 0, "new_count": 0}]


def test_update_iteration_state_unsafe_run_id_rejected(tmp_path, monkeypatch):
    """run_dir validation 재사용 — path traversal 거절."""
    monkeypatch.setenv("ASSEMBLE_HOME", str(tmp_path))
    from server import update_iteration_state
    import pytest
    with pytest.raises(ValueError):
        update_iteration_state("../../etc", {"x": 1})
```

- [ ] **Step 4: 테스트 실행**

```bash
cd ~/.claude/skills/assemble && pytest tests/unit/test_run_dir.py -v 2>&1 | tail -15
```

기대: 기존 테스트 + 신규 4개 모두 PASS.

- [ ] **Step 5: Commit**

```bash
cd ~/.claude/skills/assemble
git add server/run_dir.py server/__init__.py tests/unit/test_run_dir.py
git commit -m "feat(v4-spike-ii): F15 — server.update_iteration_state for orchestrator metadata

B-6 dogfood: main delegated iteration_state.json update to sub-agent
(23 tool uses, 58.6k tokens, 7m 45s for a 1-line JSON append). Decision
D9: server function so main can update directly in 1 line. Hook
whitelist (Task 2) lets this path through.

Spec: docs/specs/2026-04-30-v4-spike-ii-design.md §3.1 F15 (D9)."
```

### Task 8: SKILL.md Step 9 wording — `update_iteration_state` 사용 명시 (F15 wording)

**Files:**
- Modify: `~/.claude/skills/assemble/bundled/plan-pack/SKILL.md:183-202` (`### Step 9 — 4-way cross-doc second-opinion`)
- Modify: `~/.claude/skills/assemble/bundled/plan-pack/SKILL.md:295-298` (Multi-iteration loop wording)
- Modify: `~/.claude/skills/assemble/tests/unit/test_plan_pack_skill.py` (Step 9 grep)

**Acceptance:**
- Step 9 본문이 "main parses COUNTS line, calls `update_iteration_state(rid, ...)` — sub-agent dispatch 금지" 명시.
- Multi-iteration loop verbatim 블록도 server 함수 사용으로 갱신.
- grep 테스트가 `update_iteration_state` keyword pinning.

- [ ] **Step 1: Step 9 마지막 단락 갱신**

`bundled/plan-pack/SKILL.md:198-202` (`Update runs/<rid>/iteration_state.json with the COUNTS values...` 단락) 를 다음으로 교체:

```markdown
Sub-agent prints **two** stdout lines — `WROTE: <path>` and
`COUNTS: resolved=<N> unresolved=<N> new=<N>`. Parse both. To update
`runs/<rid>/iteration_state.json` (drives the multi-iteration stop
condition), call `server.update_iteration_state` directly — *do not*
dispatch a sub-agent for this:

```python
from server import update_iteration_state
update_iteration_state(rid, {
    "resolved_pct": resolved / max(resolved + unresolved + new, 1),
    "new_count": new,
    "stopped": False,
    "reason": "",
})
```

orchestrator metadata 는 §CRITICAL 룰 (Spike II F11) 에 의해 sub-agent 위임 금지. hook 화이트리스트 (Spike II F8) 가 main 직접 write 를 허용한다. If the `COUNTS:` line is missing or unparseable (non-integer values, missing key), treat as Step 9 dispatch failure per §CRITICAL — surface to user, do NOT advance to Step 6.
```

- [ ] **Step 2: Multi-iteration loop wording 갱신**

`bundled/plan-pack/SKILL.md:295-298` (`After each Step 9 review the main Claude parses the COUNTS...` 단락) 을 다음으로 교체:

```markdown
After each Step 9 review the main Claude parses the
`COUNTS: resolved=N unresolved=N new=N` second-stdout line, computes
`resolved_pct = resolved / (resolved + unresolved + new)`, calls
`update_iteration_state(rid, {...})` (server function, see Step 9), and
decides whether to continue. The function appends to `iterations[]` with
auto-assigned `index`.
```

- [ ] **Step 3: grep 테스트 추가**

`tests/unit/test_plan_pack_skill.py` 에 추가:

```python
def test_skill_md_step9_uses_update_iteration_state():
    """Spike II F15: Step 9 must instruct main to use server function."""
    from pathlib import Path
    text = (Path.home() / ".claude/skills/assemble/bundled/plan-pack/SKILL.md").read_text(encoding="utf-8")
    assert "update_iteration_state" in text
    # Both Step 9 detail + Multi-iteration loop wording reference it
    assert text.count("update_iteration_state") >= 2
    # And the §CRITICAL ban on sub-agent metadata delegation is reinforced
    assert "do not" in text.lower() or "위임 금지" in text
```

- [ ] **Step 4: 테스트 실행**

```bash
cd ~/.claude/skills/assemble && pytest tests/unit/test_plan_pack_skill.py -v 2>&1 | tail -15
```

기대: 기존 + 신규 1개 모두 PASS.

- [ ] **Step 5: Commit**

```bash
cd ~/.claude/skills/assemble
git add bundled/plan-pack/SKILL.md tests/unit/test_plan_pack_skill.py
git commit -m "docs(v4-spike-ii): F15 wording — Step 9 uses update_iteration_state

SKILL.md Step 9 now shows the 1-line server call instead of leaving main
to choose hook-bypass paths. Reinforces §CRITICAL F11 (no sub-agent
delegation for metadata).

Spec: docs/specs/2026-04-30-v4-spike-ii-design.md §3.1 F15."
```

---

## Phase E — F3 + F4 (Rule 5 SKILL head + (추천) 통일)

> 의존: Phase C Task 5 (preamble v3 사례 박힘 — 본 Task 는 SKILL.md head 보강).

### Task 9: SKILL.md head Rule 5 사례 + (추천) 라벨 정책 (F3 + F4)

**Files:**
- Modify: `~/.claude/skills/assemble/bundled/plan-pack/SKILL.md:5-13` (HARNESS RULES head)
- Modify: `~/.claude/skills/assemble/bundled/plan-pack/SKILL.md` 새 §"Korean label policy" 섹션 추가
- Modify: `~/.claude/skills/assemble/tests/unit/test_plan_pack_skill.py`

**Acceptance:**
- SKILL.md head Rule 5 본문이 preamble v3 와 동일 사례 박힘 (architecture→아키텍처 등).
- 새 섹션 또는 §CRITICAL 직후에 1 paragraph: "한국어 locale 시 옵션 라벨 (Recommended)→`(추천)` 통일. 자작 변형 금지 (예: '(승인)' 잘못)".
- grep 테스트가 두 사례 (외래어 표기 + (추천)) 모두 pinning.

- [ ] **Step 1: SKILL.md head Rule 5 갱신**

`bundled/plan-pack/SKILL.md:11` 의 현재:

```markdown
5. 사용자에게 표시되는 한국어 라벨·옵션은 자연스러운 한국어로 정제
```

를 다음으로 교체:

```markdown
5. 사용자에게 표시되는 한국어 라벨·옵션은 자연스러운 한국어로 정제. 영문 기술용어 한글화 시 정확한 외래어 표기 사용 (architecture→아키텍처, family→패밀리, top-level→최상위, recommended→추천, directory→디렉토리). 자작 변형 금지.
```

- [ ] **Step 2: §"Korean label policy" 섹션 추가**

`bundled/plan-pack/SKILL.md` 의 `## CRITICAL` 블록 직후 (현재 line 30 직후 빈 줄 다음) 에 새 섹션:

```markdown
## Korean label policy (Spike II F4)

When `ASSEMBLE_LOCALE=ko` (or user message language is Korean), AskUserQuestion
options that historically carry English `(Recommended)` suffix MUST use the
unified Korean form `(추천)`. Do not improvise alternatives like `(승인)` —
those drift from the platform-wide V4 한국어 라벨 정책.

Example:
- `"yes — 강조점 인터뷰 + 4-doc 재작성 + cross-doc 재검증 (추천)"` ✓
- `"yes — ... (Recommended)"` ✗ (혼합 영어)
- `"yes — ... (승인)"` ✗ (자작 변형)

`(추천)` 만 사용. Other label languages keep their native suffix (English →
`(Recommended)`).
```

- [ ] **Step 3: grep 테스트 추가**

`tests/unit/test_plan_pack_skill.py` 에 추가:

```python
def test_skill_md_rule5_includes_loanword_examples():
    """Spike II F3: Rule 5 head에 외래어 표기 사례."""
    from pathlib import Path
    text = (Path.home() / ".claude/skills/assemble/bundled/plan-pack/SKILL.md").read_text(encoding="utf-8")
    for sample in ("architecture→아키텍처", "family→패밀리", "top-level→최상위", "recommended→추천"):
        assert sample in text, f"missing: {sample}"


def test_skill_md_korean_label_policy_section():
    """Spike II F4: (추천) 통일 정책이 명시."""
    from pathlib import Path
    text = (Path.home() / ".claude/skills/assemble/bundled/plan-pack/SKILL.md").read_text(encoding="utf-8")
    assert "Korean label policy" in text or "라벨 정책" in text
    assert "(추천)" in text
    assert "(승인)" in text  # 잘못된 사례 명시
```

- [ ] **Step 4: 테스트 실행**

```bash
cd ~/.claude/skills/assemble && pytest tests/unit/test_plan_pack_skill.py -v 2>&1 | tail -10
```

- [ ] **Step 5: Commit**

```bash
cd ~/.claude/skills/assemble
git add bundled/plan-pack/SKILL.md tests/unit/test_plan_pack_skill.py
git commit -m "fix(v4-spike-ii): F3+F4 — Rule 5 외래어 사례 + (추천) 라벨 통일

B-6 dogfood: 7 한국어 quality 위반 (아키텍쳘/패널리/팬더리/접수 등) +
(승인) 자작 한국어화 1건. Rule 5 head 와 §"Korean label policy" 신설로
prepend.

Spec: docs/specs/2026-04-30-v4-spike-ii-design.md §3.2 F3, F4."
```

---

## Phase F — F5 + F6 + F7 (Step 10/12 spec 정확화)

> 의존: Phase A.

### Task 10: Step 10 spec 정확화 + multi-select schema enforce (F5 + F6)

**Files:**
- Modify: `~/.claude/skills/assemble/bundled/plan-pack/SKILL.md:145-152` (Step 10 본문)
- Modify: `~/.claude/skills/assemble/tests/unit/test_plan_pack_skill.py`

**Acceptance:**
- Step 10 본문이 Call 5 / Call 6 의 형태를 명확히 구분 (B-6 의 "1-question multi-select" 자유 재해석 차단).
- Call 5 = 1 AskUserQuestion call with 3 sub-questions (D1, D2, D3 — title only) 명시.
- Call 6 = 3 AskUserQuestion calls (per decision), each with 3 sub-questions 명시.
- multi-select schema MUST include `minSelected: 3` + `maxSelected: 5` 명시.
- 사용자 응답 후 main 이 count 검증 + 미달 시 재질문 절차 1 paragraph.
- grep 테스트가 `minSelected` + `maxSelected` + count 검증 wording pinning.

- [ ] **Step 1: Step 10 본문 교체**

`bundled/plan-pack/SKILL.md:145-152` (`### Step 10 — ADR interview` 블록) 을 다음으로 교체:

```markdown
### Step 10 — ADR interview (main Claude, AskUserQuestion)

After Step 8 returns, **two phases** of `AskUserQuestion`:

**Call 5** — *single* `AskUserQuestion` call with **3 sub-questions** (D1, D2, D3 — title only). Each sub-question shape: PRD Q1–Q4 batch shape (multi-select: choose top decisions). multi-select schema MUST include `minSelected: 3, maxSelected: 5` (gate B3.2 minimum). After response, main MUST verify the user selected ≥ 3 decisions; if fewer, re-prompt the same Call 5 with the message "최소 3개 결정이 필요합니다" — do NOT proceed with 2 decisions even if AskUserQuestion returns successfully.

**Call 6** — *3 separate* `AskUserQuestion` calls, **one per decision** chosen in Call 5, each with **3 sub-questions**: (a) strongest rejected alternative, (b) main tradeoff, (c) decision-specific risks/unknowns. The three sub-questions are numbered 1/2/3 within each call. Three calls in sequence, NOT parallel — each subsequent call's wording references the prior decision title for user context.

Three decisions = minimum (gate B3.2); volunteer more → N ≥ 3, accept up to 5.

(Spike II F5/F6: spec 모호함 차단 — "1-question multi-select" 또는 single 6-question call 자유 재해석 금지.)
```

- [ ] **Step 2: grep 테스트 추가**

`tests/unit/test_plan_pack_skill.py` 에 추가:

```python
def test_skill_md_step10_call5_call6_explicit():
    """Spike II F6: Call 5 / Call 6 형태 명확."""
    from pathlib import Path
    text = (Path.home() / ".claude/skills/assemble/bundled/plan-pack/SKILL.md").read_text(encoding="utf-8")
    assert "Call 5" in text and "Call 6" in text
    # Call 5 shape: single call, 3 sub-questions
    assert "single" in text.lower() or "*single*" in text or "1 AskUserQuestion call" in text
    # Call 6 shape: 3 calls
    assert "3 separate" in text or "3 AskUserQuestion calls" in text


def test_skill_md_step10_min_selected_3():
    """Spike II F5: gate B3.2 — minSelected 3 강제."""
    from pathlib import Path
    text = (Path.home() / ".claude/skills/assemble/bundled/plan-pack/SKILL.md").read_text(encoding="utf-8")
    assert "minSelected: 3" in text or "minSelected=3" in text or "minSelected`: 3" in text
    assert "maxSelected: 5" in text or "maxSelected=5" in text or "maxSelected`: 5" in text
    # main count validation
    assert "최소 3개" in text or "verify the user selected" in text
```

- [ ] **Step 3: 테스트 실행**

```bash
cd ~/.claude/skills/assemble && pytest tests/unit/test_plan_pack_skill.py -v 2>&1 | tail -10
```

- [ ] **Step 4: Commit**

```bash
cd ~/.claude/skills/assemble
git add bundled/plan-pack/SKILL.md tests/unit/test_plan_pack_skill.py
git commit -m "fix(v4-spike-ii): F5+F6 — Step 10 Call 5/6 explicit + minSelected 3

B-6 dogfood: spec 'two AskUserQuestion calls of 3 questions each' 자유
재해석 → single multi-select with 3 candidates 로 압축. Result: 2-decision
ADR (gate B3.2 미달).

Now: Call 5 = 1 call w/ 3 sub-Qs + minSelected:3,maxSelected:5; Call 6 =
3 separate calls per decision. Main count validation 명시.

Spec: docs/specs/2026-04-30-v4-spike-ii-design.md §3.2 F5, F6."
```

### Task 11: Step 12 인터뷰 N개 강제 (F7)

**Files:**
- Modify: `~/.claude/skills/assemble/bundled/plan-pack/SKILL.md:163-171` (Step 12 본문)
- Modify: `~/.claude/skills/assemble/tests/unit/test_plan_pack_skill.py`

**Acceptance:**
- Step 12 의 "3 priority flows" / "3 required component patterns" 가 multi-select schema `minSelected: 3, maxSelected: 3` 강제 명시.
- main 이 count 검증 + 미달/초과 시 재질문 룰.
- grep 테스트.

- [ ] **Step 1: Step 12 본문 갱신**

`bundled/plan-pack/SKILL.md:163-171` (`### Step 12 — UI_GUIDE interview` 블록) 을 다음으로 교체:

```markdown
### Step 12 — UI_GUIDE interview (main Claude, AskUserQuestion)

After Step 11 returns, 6 questions across **two `AskUserQuestion` calls of
3 questions each**.

**Call 7 (U1–U3):** (1) visual identity / aesthetic, (2) 3 priority flows, (3) 3 required component patterns. Sub-questions U2 and U3 are multi-select with `minSelected: 3, maxSelected: 3` — exactly 3 items each. Main MUST verify the count after response; if user supplied 4+ items via free text or the schema returned fewer than 3, re-prompt that single sub-question only (don't restart Call 7). Spike II F7: B-6 dogfood received 4 answers for U2/U3 — schema 강제 + main 검증으로 차단.

**Call 8 (U4–U6):** (4) ≤5 brand color tokens (hex/named with role; "no preference" valid), (5) primary + 1 supporting font family ("no preference" valid), (6) project-specific antipattern emphasis beyond template baseline ("none" valid). Q6 surfaces project-specific antipattern signal for gate B4.3.
```

- [ ] **Step 2: grep 테스트 추가**

`tests/unit/test_plan_pack_skill.py` 에 추가:

```python
def test_skill_md_step12_u2_u3_exactly_3():
    """Spike II F7: U2/U3 minSelected:3, maxSelected:3 강제."""
    from pathlib import Path
    text = (Path.home() / ".claude/skills/assemble/bundled/plan-pack/SKILL.md").read_text(encoding="utf-8")
    # U2/U3 specific schema
    assert "minSelected: 3, maxSelected: 3" in text or "minSelected=3, maxSelected=3" in text
    # Main verification logic
    assert "4 answers" in text or "user supplied 4+" in text or "4개" in text
```

- [ ] **Step 3: 테스트 실행**

```bash
cd ~/.claude/skills/assemble && pytest tests/unit/test_plan_pack_skill.py -v 2>&1 | tail -10
```

- [ ] **Step 4: Commit**

```bash
cd ~/.claude/skills/assemble
git add bundled/plan-pack/SKILL.md tests/unit/test_plan_pack_skill.py
git commit -m "fix(v4-spike-ii): F7 — Step 12 U2/U3 exactly-3 schema + main 검증

B-6 dogfood: U2/U3 spec '3가지' 인데 사용자 4개 답변 → 그대로 받음.
minSelected:3, maxSelected:3 schema 강제 + main count 검증 명시.

Spec: docs/specs/2026-04-30-v4-spike-ii-design.md §3.2 F7."
```

---

## Phase G — F10 + F14 (cross_doc COUNTS schema + iter_emphasis 압축)

> 의존: Phase C/D.

### Task 12: prompts/cross_doc_step9.md COUNTS schema 강제 (F10)

**Files:**
- Modify: `~/.claude/skills/assemble/bundled/plan-pack/prompts/cross_doc_step9.md`
- Modify: `~/.claude/skills/assemble/bundled/plan-pack/SKILL.md:198-202` (Step 9 — main parsing regex 추가)

**Acceptance:**
- prompt 본문에 "Output exactly this format (no extra keys, no different keys): `COUNTS: resolved=<int> unresolved=<int> new=<int>`" 명시.
- 다른 키 (`NEW`/`WARN`/`INFO`/`CRITICAL`) 사용 금지 명시.
- SKILL.md Step 9 에 main 검증 regex 명시: `^COUNTS: resolved=\d+ unresolved=\d+ new=\d+$`.
- 매칭 실패 시 §CRITICAL 분기 (이미 spec 에 있음 — wording 정확화).

- [ ] **Step 1: cross_doc_step9.md 본문에 schema 강제 블록 추가**

`bundled/plan-pack/prompts/cross_doc_step9.md` 를 read 후, `WROTE: <path>` 와 `COUNTS:` 출력 명시 단락을 찾아 다음으로 교체 (또는 추가):

```markdown
## Output (stdout — exact form)

Print exactly two lines, in this order:

```
WROTE: <absolute-path-to-ADR.md>
COUNTS: resolved=<int> unresolved=<int> new=<int>
```

The COUNTS line schema is verbatim — **no extra keys, no different keys**:
- ✗ `COUNTS: NEW=10 WARN=3 INFO=6 CRITICAL=0` (B-6 dogfood iteration 0 — wrong)
- ✗ `COUNTS: resolved=8 unresolved=1` (missing `new`)
- ✓ `COUNTS: resolved=8 unresolved=1 new=0`

`resolved` = findings from prior Cross-doc review now closed by this iteration's edits.
`unresolved` = findings from prior review still open.
`new` = findings introduced this iteration (regression — should trend to 0).
First-pass (`iteration_count == 0`): all findings are NEW, but report them as `new=<count>` with `resolved=0 unresolved=0`. Do NOT use other keys.
```

(파일에 위 섹션이 이미 있으면 wording 만 정렬. 없으면 추가.)

- [ ] **Step 2: SKILL.md Step 9 에 main parsing regex 추가**

`bundled/plan-pack/SKILL.md` Step 9 본문 (Task 8 에서 갱신된 단락 직전 또는 직후) 에:

```markdown
Main parses the second stdout line with regex `^COUNTS: resolved=\d+ unresolved=\d+ new=\d+$` (Spike II F10). 매칭 실패 시 (다른 키, 키 누락, 비정수, 추가 토큰) Step 9 dispatch failure 처리 — §CRITICAL 분기 (retry/abort/report).
```

- [ ] **Step 3: 신규 grep 테스트**

`tests/unit/test_plan_pack_skill.py` 에 추가:

```python
def test_cross_doc_prompt_enforces_counts_schema():
    """Spike II F10: cross_doc_step9 prompt에 COUNTS schema verbatim."""
    from pathlib import Path
    text = (Path.home() / ".claude/skills/assemble/bundled/plan-pack/prompts/cross_doc_step9.md").read_text(encoding="utf-8")
    assert "COUNTS: resolved=" in text
    assert "unresolved=" in text and "new=" in text
    # Wrong-keys 거부 사례
    assert "NEW=" in text or "no different keys" in text


def test_skill_md_step9_includes_counts_regex():
    """Spike II F10: SKILL.md Step 9 에 main parsing regex pin."""
    from pathlib import Path
    text = (Path.home() / ".claude/skills/assemble/bundled/plan-pack/SKILL.md").read_text(encoding="utf-8")
    assert r"COUNTS: resolved=\d+ unresolved=\d+ new=\d+" in text
```

- [ ] **Step 4: 테스트 실행**

```bash
cd ~/.claude/skills/assemble && pytest tests/unit/test_plan_pack_skill.py -v 2>&1 | tail -10
```

- [ ] **Step 5: Commit**

```bash
cd ~/.claude/skills/assemble
git add bundled/plan-pack/prompts/cross_doc_step9.md bundled/plan-pack/SKILL.md tests/unit/test_plan_pack_skill.py
git commit -m "fix(v4-spike-ii): F10 — cross_doc_step9 COUNTS schema verbatim

B-6 dogfood: same prompt produced different COUNTS keys across iterations
(iter0: NEW=10/WARN=3/INFO=6/CRITICAL=0 vs iter1: resolved=8/unresolved=1/new=1).
Prompt now enforces verbatim 'resolved/unresolved/new' keys + main regex
validation in SKILL.md Step 9.

Spec: docs/specs/2026-04-30-v4-spike-ii-design.md §3.2 F10."
```

### Task 13: prompts/iter_emphasis.md — 변경 영역만 substitute + verbatim sentinel (F14)

**Files:**
- Modify: `~/.claude/skills/assemble/bundled/plan-pack/prompts/iter_emphasis.md`
- Modify: `~/.claude/skills/assemble/bundled/plan-pack/SKILL.md:236-269` (Step 6 yes-path detail)

**Acceptance:**
- iter_emphasis prompt 가 4-doc placeholder 전체 substitute 대신 *emphasis-target 섹션만* substitute 형태로 재구조화.
- 다른 섹션은 "verbatim — do not reword" sentinel 명시 (이미 일부 있음 → 강화).
- SKILL.md Step 6 yes-path detail 본문도 prompt 패턴 갱신.
- iter_emphasis prompt 의 `{{PRD_EMPHASIS_SECTION}}` / `{{ARCH_EMPHASIS_SECTION}}` 같이 *섹션 단위* placeholder 가 등장.

**참고:** 본 Task 는 prompt 압축이 핵심. B-6 iter1 4-doc redraft = 28 tool uses · 109k tokens. 압축 후 substantially 작아져야.

- [ ] **Step 1: 현재 iter_emphasis.md 구조 파악**

```bash
cd ~/.claude/skills/assemble
wc -l bundled/plan-pack/prompts/iter_emphasis.md
grep -n "^##\|{{" bundled/plan-pack/prompts/iter_emphasis.md
```

읽고 placeholder 목록 + 섹션 구조 메모.

- [ ] **Step 2: emphasis-section placeholder 패턴으로 재작성**

`bundled/plan-pack/prompts/iter_emphasis.md` 를 다음 골격으로 교체 (기존 verbatim discipline 룰은 보존):

```markdown
# Iteration {{ITERATION_COUNT}} — emphasis re-write

You are dispatched as plan-pack Step 6 yes-path sub-agent. Goal: re-write
ONE doc ({{DOC_NAME}}) under emphasis "{{EMPHASIS}}" while preserving all
other sections verbatim.

## Inputs

- run_id: `{{RUN_ID}}`
- doc_name: `{{DOC_NAME}}` (one of `PRD.md`, `ARCHITECTURE.md`, `ADR.md`, `UI_GUIDE.md`)
- emphasis: `{{EMPHASIS}}` ("(no change)" allowed — return doc verbatim)
- emphasis_target_section: `{{EMPHASIS_SECTION_TITLE}}` (e.g. `## Core features` for PRD)
- existing_section_text: `{{EMPHASIS_SECTION_BODY}}` (current content of that section only)

## Scope discipline (verbatim — do not paraphrase)

> PRD `## Core features` is the authoritative scope.
> Do not introduce new features, modules, components, screens, or token
> sets that have no counterpart in the existing PRD `## Core features`.
> Items the ADR has explicitly deferred (`> **Future ADRs**`) MUST NOT
> be pre-emptively decided. Existing sections that are not the explicit
> target of the iteration emphasis MUST be returned verbatim — do not
> reword Reasoning/Tradeoffs/Rejected-alternatives blocks. Pre-existing
> identifiers (variable, token, module, component names) MUST NOT be
> renamed unless the rename IS the requested change.

## Required behavior

1. If `{{EMPHASIS}}` == `(no change)`: read `{{DOC_NAME}}` via
   `read_run_artifact`, write it back unchanged via `write_run_artifact`.
   Return `WROTE: <path>`. Do NOT touch other sections, do NOT re-read
   other docs, do NOT inspect infrastructure code (rule 7).
2. Else: locate `{{EMPHASIS_SECTION_TITLE}}` in the doc, replace its body
   with an emphasis-aware rewrite. ALL other sections (incl. headings,
   ordering, whitespace, tables) MUST be byte-identical to input.
3. The "VERBATIM SENTINEL" sections (PRD `## Acceptance criteria` bash
   block, ADR `## Decision N` Reasoning/Tradeoffs/Rejected-alternatives
   sub-blocks) MUST be re-emitted byte-for-byte.

## Final step (canonical save block — DO NOT MODIFY THE STRUCTURE)

```python
# ASSEMBLE_SUBAGENT_LIFECYCLE_WRITE -- sub-agent legitimate dispatch
import sys
from pathlib import Path
sys.path.insert(0, str(Path.home() / ".claude/skills/assemble"))
from server import write_run_artifact, read_run_artifact

rid = "{{RUN_ID}}"
doc_name = "{{DOC_NAME}}"
new_text = """<your re-written doc body — see Required behavior above>"""

path = write_run_artifact(rid, doc_name, new_text)
print(f"WROTE: {path}")
```

If write fails, print `ERROR: <reason>` and exit. No fallback writes.
```

(현재 iter_emphasis 가 이미 4-doc 전체를 substitute 하는 구조면 위 골격이 substantial 변경. 만약 이미 single-doc 구조면 placeholder 만 변경.)

- [ ] **Step 3: SKILL.md Step 6 yes-path detail 본문 갱신**

`bundled/plan-pack/SKILL.md:236-269` 의 `### Step 6 yes-path detail` 단락에서 placeholder 목록을 갱신. 변경 전 (line 238-241):

```markdown
2. **Substitute placeholders in `prompts/iter_emphasis.md` header**
   before constructing the four iteration sub-agent prompts:
   `{{ITERATION_COUNT}}`, `{{EMPHASIS}}` (per-doc), `{{PRD_TEXT}}`,
   `{{ARCH_TEXT}}`, `{{ADR_TEXT}}`, `{{UI_TEXT}}`.
```

변경 후:

```markdown
2. **Per-doc emphasis substitution (Spike II F14):** main constructs ONE
   prompt per doc — 4 prompts total, each based on `iter_emphasis.md` —
   with placeholders: `{{ITERATION_COUNT}}`, `{{RUN_ID}}`, `{{DOC_NAME}}`
   (one of `PRD.md`/`ARCHITECTURE.md`/`ADR.md`/`UI_GUIDE.md`),
   `{{EMPHASIS}}` (per-doc answer from emphasis interview),
   `{{EMPHASIS_SECTION_TITLE}}` (canonical heading, e.g. `## Core features`),
   `{{EMPHASIS_SECTION_BODY}}` (current text of that one section, read by
   main from the live doc). 4-doc 전체 placeholder substitute 금지 — sub-agent
   가 자기 doc 만 보면 충분 (B-6 iter1 redraft cost 109k tokens 의 주요 원인).
```

- [ ] **Step 4: grep 테스트 추가**

`tests/unit/test_plan_pack_skill.py` 에 추가:

```python
def test_iter_emphasis_uses_per_doc_substitution():
    """Spike II F14: iter_emphasis 가 single-doc placeholder 패턴."""
    from pathlib import Path
    prompt = (Path.home() / ".claude/skills/assemble/bundled/plan-pack/prompts/iter_emphasis.md").read_text(encoding="utf-8")
    assert "{{DOC_NAME}}" in prompt
    assert "{{EMPHASIS_SECTION_TITLE}}" in prompt or "{{EMPHASIS_SECTION_BODY}}" in prompt
    # Old multi-doc placeholders should NOT all be present (4-doc full substitution removed)
    bad = sum(p in prompt for p in ("{{PRD_TEXT}}", "{{ARCH_TEXT}}", "{{ADR_TEXT}}", "{{UI_TEXT}}"))
    assert bad <= 1, "iter_emphasis should not embed all 4 doc bodies — F14 압축"


def test_skill_md_step6_yespath_uses_per_doc_emphasis():
    """SKILL.md Step 6 yes-path detail 본문도 per-doc 패턴 명시."""
    from pathlib import Path
    text = (Path.home() / ".claude/skills/assemble/bundled/plan-pack/SKILL.md").read_text(encoding="utf-8")
    assert "{{DOC_NAME}}" in text
    assert "Per-doc emphasis" in text or "per-doc" in text.lower()
```

- [ ] **Step 5: 테스트 실행**

```bash
cd ~/.claude/skills/assemble && pytest tests/ 2>&1 | tail -10
```

기대: 전체 테스트 + 신규 모두 PASS.

- [ ] **Step 6: Commit**

```bash
cd ~/.claude/skills/assemble
git add bundled/plan-pack/prompts/iter_emphasis.md bundled/plan-pack/SKILL.md tests/unit/test_plan_pack_skill.py
git commit -m "perf(v4-spike-ii): F14 — iter_emphasis per-doc substitution

B-6 iter1 4-doc redraft cost 109k tokens. Each sub-agent received all 4
doc bodies + verbatim discipline rule → re-read everything to comply.
Per-doc emphasis: each sub-agent sees only its target doc's emphasis
section + verbatim discipline. Estimated cost reduction: 60-70%.

Spec: docs/specs/2026-04-30-v4-spike-ii-design.md §3.2 F14."
```

---

## Phase H — B-7 dogfood (사용자 새 세션 직접 실행)

> 본 Phase 는 plan 의 일부가 아니다. Phase A~G 완료 후 새 세션에서 사용자가 직접 실행.

### B-7 acceptance criteria (spec § 5)

다음 9개 모두 통과 해야 Spike II 출하:

1. **F1 0회 재현** — 메뉴 ★ plan-pack 선택 시 `Skill()` 호출 없이 바로 SKILL.md Read
2. **F2 0회 재현** — `record_dispatch` 첫 호출에 `role=` TypeError 없음
3. **F8 0회 재현** — `iteration_state.json` update 에 hook 차단 없음 (server 함수 사용)
4. **F12 0회 재현** — sub-agent 들이 hook/server 인프라 코드 read 시도 0건
5. **F15 0회 재현** — `iteration_state.json` sub-agent 위임 0건 (main 직접 server 함수 호출)
6. **Rule 5 위반 0건** — 인터뷰 옵션 desc 한국어 자연성
7. **Gate B3.2 강제** — ADR Step 10 에서 사용자 2개 선택 시 재질문 발현
8. **COUNTS 형식 일관** — 두 iteration 모두 `resolved/unresolved/new` 키 사용
9. **`ASSEMBLE_GUARD=off` 제거** — settings.json 에 박혀있어도 hook 차단 유지

### B-7 실행 절차

새 세션에서:

```
형: /assemble "<dogfood task — B-6 와 다른 task 권장>"
형: ★ plan-pack 선택
... (인터뷰 → dispatch → cross-doc → iteration)
```

dogfood 후 readiness memo 작성 후 distribution 분기:
- 9 acceptance 모두 PASS → V4 master 배포 prep (CHANGELOG 작성, distribution 메모)
- 일부 FAIL → Spike III 분기 (잔여 carryforward)

---

## Self-Review

(plan 작성 직후 메인 Claude 가 한 번 검토)

**1. Spec coverage:**
- F1 → Task 1 ✓
- F2 → Task 1 ✓
- F3 → Task 5 (preamble v3) + Task 9 (SKILL head) ✓
- F4 → Task 9 ✓
- F5 → Task 10 ✓
- F6 → Task 10 ✓
- F7 → Task 11 ✓
- F8 → Task 2 ✓
- F9 → Task 4 ✓
- F10 → Task 12 ✓
- F11 → Task 4 ✓
- F12 → Task 5 (preamble) + Task 6 (ALLOW_LIST) ✓
- F13 → Task 3 ✓
- F14 → Task 13 ✓
- F15 → Task 7 (server 함수) + Task 8 (SKILL.md wording) ✓
- 9 Decisions D1-D9 → 모두 위 task 에 매핑 ✓
- M1/M2/M3 (미세) → spec § 6 Out of scope ✓

**2. Placeholder scan:**
- 각 step 에 코드 블록 + 명령 + 기대 출력 명시.
- "TBD/TODO/implement later" 없음.
- 한 가지 grep 패턴 (`<TASK 5 STEP 3 OUTPUT>`) 은 명시적으로 "Step 3 의 v3 sha256 출력으로 자리 채움" 안내 포함 — 의도된 자리.

**3. Type / 시그니처 일관성:**
- `update_iteration_state(run_id, counts: dict) -> Path` — Task 7 정의, Task 8 SKILL.md 본문 + Task 8 grep 테스트 동일 시그니처.
- `record_dispatch(...)` 시그니처 verbatim — Task 1 SKILL.md, Task 1 테스트 모두 동일.
- preamble v3 sha256 → Task 5 Step 3 산출, Task 6 Step 1 reuse, Task 6 Step 2 hard-code.

**4. Phase 의존성:**
- A → C/D/E/F/G ✓
- B (Task 2) → D (Task 7 hook 화이트리스트 활용) ✓
- C (Task 5) → C (Task 6 ALLOW_LIST) ✓

이상 모두 정합. 필요 시 plan 진행 중 수정.

---

## Sources

- Spike II spec: `~/.claude/skills/assemble/docs/specs/2026-04-30-v4-spike-ii-design.md` (commit `1c3c0a1`)
- Spike I plan (format reference): `~/.claude/skills/assemble/docs/plans/2026-04-30-v4-spike-i.md`
- B-6 dogfood: run `20260430-120552-6aad`
- Memory: `project_assemble_v4_spike_ii.md`
- 현 SKILL.md (323 lines, post-Spike I): `bundled/plan-pack/SKILL.md`
- 현 hook v1 (102 lines, Bash 매처): `hooks/guard_run_dir.sh`
- 현 preamble v2: `bundled/_shared/harness-preamble.md` (sha256 `df27450513c019...`)
