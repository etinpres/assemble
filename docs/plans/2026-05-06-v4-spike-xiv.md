# V4 Spike XIV Plan — Spike XIII NEEDS-FIX cleanup

**Date**: 2026-05-06
**Spec**: `docs/specs/2026-05-06-v4-spike-xiv-design.md`
**Method**: subagent-driven-development (각 task = fresh sub-agent + spec review + code-quality review)
**Baseline**: master `6b989a2`, pytest 813 passed (17.16s)

---

## Task list (atomic commits)

각 task = 1 atomic commit. subagent-driven-development 패턴:
- spec section read → fresh general-purpose sub-agent dispatch with explicit ACs
- spec review (compliance check) → code-quality review (style/test coverage)
- 모두 PASS 후 commit

| # | Phase | Task | 시간 | Commit message prefix |
|---|---|---|---|---|
| T1 | A | wrap_with_preamble ASSEMBLE_HOME body injection (preamble v3 sha 보존) | ~1h | `feat(v4-spike-xiv-A): ASSEMBLE_HOME auto-prepend in dispatch body` |
| T2 | B | 7 ★ 번들 SKILL.md mode-gate + Quick mode flow + dispatch contract 강화 | ~2h | `feat(v4-spike-xiv-B): paradigm hybrid — mode-gate + opt-in quick mode` |
| T3 | B | KEEPER_REPORT.md.template Mode usage section | ~15m | `feat(v4-spike-xiv-B): keeper template — Mode usage section + counts` |
| T4 | C | plan-pack SKILL.md Step 6 Recommendation policy | ~30m | `fix(v4-spike-xiv-C): plan-pack Step 6 default = yes when iter≤3 + counts` |
| T5 | D | mark_orthogonal_stage API + progress.json schema 확장 | ~30m | `feat(v4-spike-xiv-D): mark_orthogonal_stage API + auto-route from mark_stage` |
| T6 | E | SKILL.md dispatch_prompt 시그니처 drift sweep | ~30m | `fix(v4-spike-xiv-E): SKILL.md dispatch_prompt 1-arg basename — design-pack/idea-shaper` |
| T7 | E | enforcement test — dispatch_prompt signature drift 사전 차단 | ~15m | `test(v4-spike-xiv-E): enforcement — SKILL.md dispatch_prompt 1-arg basename` |
| T8 | F | B-20a 자동 sanity probe (18 AC) | ~1h | `test(v4-spike-xiv-F): B-20a automated sanity probe — 18 AC PASS` |
| T9 | F | B-20b lived dogfood capture guide | ~30m | `docs(v4-spike-xiv-F): B-20b lived dogfood guide — 6 new capture points` |
| T10 | F | B-20b 분석 + verdict (사용자 운전 후) | (사용자 ~90m + 분석 ~1h) | `docs(v4-spike-xiv-F): B-20b lived dogfood verdict + carryforwards` |
| T11 | ship | CHANGELOG flip + push | ~15m | `chore(v4-spike-xiv): ship — Spike XIV cleanup verdict <SHIP-READY \| ...>` |

총 11 commits + spec/plan 시작 commit (T0) = 12 commits.

---

## T0 — spec + plan single starting commit

```
docs(v4-spike-xiv): spec + plan — Spike XIII NEEDS-FIX cleanup (paradigm hybrid)
```

본 commit 에 두 파일:
- `docs/specs/2026-05-06-v4-spike-xiv-design.md`
- `docs/plans/2026-05-06-v4-spike-xiv.md`

---

## T1 — Phase A: wrap_with_preamble ASSEMBLE_HOME body injection

### Sub-agent prompt skeleton

```
You are dispatched as Phase A implementer for V4 Spike XIV.

Read first:
- docs/specs/2026-05-06-v4-spike-xiv-design.md § "Phase A — C1 fix"
- server/harness.py:178-225 (current wrap_with_preamble + _load_preamble)

Goal:
Modify wrap_with_preamble in server/harness.py so that when os.environ['ASSEMBLE_HOME']
is set, the dispatch body (NOT the preamble portion) gets a [ENV] hint line as the
first body line. preamble portion must stay byte-identical (canonical sha v3 preserved).

Implementation contract:
1. server/harness.py wrap_with_preamble — inject [ENV] line in body region only
2. tests/unit/test_wrap_with_preamble_assemble_home.py (NEW) — 4 tests per spec § Test 추가
3. ALL existing tests must remain green (813 baseline)

Verify before WROTE:
- pytest 2>&1 | tail -3 → 817 passed (813 + 4 new)
- canonical_preamble_sha256() unchanged
- record_dispatch + verify_dispatches green

Print WROTE: <abs path of test file> on stdout.
```

### Spec review checklist (post-implementation)

- [ ] preamble portion byte-identical (A.AC1, A.AC2)
- [ ] ASSEMBLE_HOME unset → no injection (A.AC3)
- [ ] ASSEMBLE_HOME set → body 첫 줄 [ENV] 주입 (A.AC4)
- [ ] dispatches.jsonl audit verify green (A.AC5)
- [ ] pytest 817 passed (A.AC6)

### Code quality review

- [ ] [ENV] line wording 한국어 + 영어 일관 (`이 dispatch는 ASSEMBLE_HOME=… 환경에서…`)
- [ ] os.environ.get default empty string 처리 (None 안 들어가게)
- [ ] f-string 한 줄로 가독성 유지
- [ ] 새 test 4개 모두 explicit assert + clear docstring

---

## T2 — Phase B: 7 ★ 번들 mode-gate + Quick mode flow

### Sub-agent prompt skeleton

```
You are dispatched as Phase B implementer for V4 Spike XIV (paradigm hybrid).

Read first:
- docs/specs/2026-05-06-v4-spike-xiv-design.md § "Phase B — C2/I2 fix"
- bundled/plan-pack/SKILL.md (전체)
- bundled/builder/SKILL.md (전체)
- bundled/debugger/SKILL.md (전체)
- bundled/reviewer/SKILL.md (전체)
- bundled/verifier/SKILL.md (전체)
- bundled/shipper/SKILL.md (전체)
- bundled/keeper/SKILL.md (전체)

Goal:
각 ★ 번들 SKILL.md 머리말에 § "Mode gate" block 추가 + 끝부분에 § "Quick mode flow"
추가 + dispatch contract description 에 한 줄 강화. spec § "Fix design" 에 명시된
wording 일관 적용.

Wording 일관 규칙 (7 SKILL.md 모두 동일):
- § "Mode gate" 머리말 wording: spec block 그대로 복붙. <X분> + <Y회>는 각 번들의
  정상 N-step 시간/dispatch 수로 치환.
- § "Quick mode flow": 각 번들의 N-step 을 1 dispatch 로 압축한 단축 분기.
  산출물 schema (sections) 는 full mode 와 동일 — Sub-agent 가 single dispatch 안에서
  모든 sections 채움. dispatches.jsonl 에 mode=quick 메타.

Implementation contract:
1. 7 SKILL.md 일관 편집 (atomic single commit)
2. tests/unit/test_mode_gate_consistency.py (NEW) — 5 tests per spec § Test 추가
3. ALL existing tests must remain green
4. 표준 번들 (idea-shaper / design-pack / guardian) SKILL.md 변경 X — gate 도입 X

Verify before WROTE:
- grep -c "Mode gate" bundled/*/SKILL.md → 7 ★ 번들에서 1 hit each, 표준 3 0 hits
- grep -c "Quick mode flow" bundled/*/SKILL.md → 7 hit
- grep -c "사용자 명시 동의 없이 단축 금지" bundled/*/SKILL.md → 7 hit
- pytest 2>&1 | tail -3 → 5 new tests added, all green

Print WROTE: <abs path of new test file>.
```

### Per-bundle quick mode contract (T2 의 hardest part)

각 ★ 번들 quick mode 는 *동일 산출물 schema* 유지. 단축 dispatch 한 회의 prompt
template 신규 생성:

| 번들 | quick prompt file (NEW) | 출력 산출물 (schema unchanged) |
|---|---|---|
| plan-pack | `bundled/plan-pack/prompts/subagent/quick_mode.md` | PRD.md (4 sections inline) |
| builder | `bundled/builder/prompts/subagent/quick_mode.md` | IMPL_REPORT.md |
| debugger | `bundled/debugger/prompts/subagent/quick_mode.md` | DEBUGGER_LOG.md |
| reviewer | `bundled/reviewer/prompts/subagent/quick_mode.md` | REVIEW_REPORT.md |
| verifier | `bundled/verifier/prompts/subagent/quick_mode.md` | VERIFY_REPORT.md |
| shipper | `bundled/shipper/prompts/subagent/quick_mode.md` | SHIPPER_LOG.md |
| keeper | `bundled/keeper/prompts/subagent/quick_mode.md` | KEEPER_REPORT.md |

새 7 prompt files 추가 시 enforcement chain:
- `ALLOWED_PROMPT_FILES` 7 entry 추가 → 42 → **49** entries
- `_PROMPT_TO_STAGE` 7 entry 추가 (각 stage 매핑)
- `test_allowed_prompt_files_matches_bundle_inventory` 자동 통과 (auto-derive from disk)
- `test_prompt_to_stage_values_are_known_stages` 자동 통과 (stage 들이 모두 STAGE_CATEGORY_PRIORITY 키)

각 quick prompt 본문 line 1 = `# <bundle> quick mode — single-dispatch fallback`
line 2 = `Print WROTE: <absolute path> on stdout. ...` (표준 contract)

### Spec review checklist

- [ ] 7 ★ SKILL.md mode-gate (B.AC1) ✅
- [ ] 7 ★ SKILL.md Quick mode flow (B.AC2) ✅
- [ ] dispatch contract 한 줄 강화 (B.AC3) ✅
- [ ] preamble v3 sha 보존 (B.AC4) — preamble 본문 미변경 ✅
- [ ] quick fallback 산출물 schema 일치 (B.AC5)
- [ ] 표준 3 SKILL.md 미변경 (B.AC7)
- [ ] ALLOWED_PROMPT_FILES 49 entries (42 + 7 new quick)
- [ ] _PROMPT_TO_STAGE 49 entries
- [ ] pytest 817 + 5 new = 822 passed (B.AC8)

### Code quality review

- [ ] 7 SKILL.md mode-gate wording 100% 일관 (diff 검증 가능)
- [ ] 7 quick prompt files 본문 표준 contract (line 2 WROTE)
- [ ] AskUserQuestion options 한국어 자연스러움 (preamble rule 5 외래어 표기)

### Risk

이 task 가 본 spike 에서 가장 큼 — 7 SKILL.md 일관 편집 + 7 새 prompt files + 5 새 tests.
fresh sub-agent 1회로 전부 끝내려면 prompt 가 매우 명확해야 함. spec § "Fix design"
이 그 역할.

---

## T3 — Phase B: KEEPER_REPORT.md.template Mode usage section

### Sub-agent prompt skeleton

```
Read:
- docs/specs/2026-05-06-v4-spike-xiv-design.md § "KEEPER_REPORT 단축 모드 카운트"
- bundled/keeper/templates/KEEPER_REPORT.md.template

Goal:
template 끝부분에 § "Mode usage" section 추가. spec block 그대로 복붙.

Verify:
- grep -c "Mode usage" bundled/keeper/templates/KEEPER_REPORT.md.template → 1
- 기존 keeper template 테스트 모두 green

Print WROTE: <abs path of template>.
```

가벼운 task — 1 file edit. T2 후속이지만 분리된 commit (template 편집 + audit 추가
는 별 단위가 자연스러움).

---

## T4 — Phase C: plan-pack Step 6 Recommendation policy

### Sub-agent prompt skeleton

```
Read:
- docs/specs/2026-05-06-v4-spike-xiv-design.md § "Phase C — I1 plan-pack Step 6"
- bundled/plan-pack/SKILL.md:294-320 (Step 6 prompt selector + table)

Goal:
§ "Step 6 prompt selector" 다음에 § "Recommendation policy" 신규 section 추가.
spec block 그대로. recommend_iter_continue 알고리즘 명시 + "dogfood 시간 한계"
사유 박지 말 것 wording.

Implementation contract:
1. bundled/plan-pack/SKILL.md edit
2. tests/unit/test_plan_pack_step6_recommendation.py (NEW) — 3 tests
3. ALL existing tests green

Verify:
- grep -c "Recommendation policy" bundled/plan-pack/SKILL.md → 1
- grep -c "dogfood 시간 한계" bundled/plan-pack/SKILL.md → 0
- pytest → 825 passed (822 + 3 new)

Print WROTE.
```

---

## T5 — Phase D: mark_orthogonal_stage API

### Sub-agent prompt skeleton

```
Read:
- docs/specs/2026-05-06-v4-spike-xiv-design.md § "Phase D — I3 orthogonal stage"
- server/progress.py (전체 — 118 줄)
- tests/unit/test_progress_*.py (있으면 패턴 참고)

Goal:
1. server/progress.py 에 mark_orthogonal_stage 함수 + _ensure_orthogonal_field
   helper + VALID_ORTHOGONAL_STAGES 상수 + mark_stage 의 auto-route 추가
2. progress.json schema 확장 (orthogonal_stages dict, lazy creation)
3. tests/unit/test_mark_orthogonal_stage.py (NEW) — 6 tests

Implementation contract:
- mark_stage('safety'/'meta') 받으면 mark_orthogonal_stage 로 forward
- 'back' status orthogonal 호출 시 ValueError ("'back' is sequence-only")
- 기존 progress.json (orthogonal_stages 필드 없음) load OK — _ensure_orthogonal_field

Verify:
- pytest → 831 passed (825 + 6 new)
- python3 -c "from server.progress import mark_orthogonal_stage; print('ok')" → ok

Print WROTE.
```

---

## T6 — Phase E: SKILL.md dispatch_prompt drift sweep

### Sub-agent prompt skeleton

```
Read:
- docs/specs/2026-05-06-v4-spike-xiv-design.md § "Phase E — I4 doc drift"
- bundled/design-pack/SKILL.md (특히 line 34)
- bundled/idea-shaper/SKILL.md (특히 line 33)

Goal:
2 SKILL.md 정정 — dispatch_prompt 1-arg basename 시그니처:
- design-pack:34 → dispatch_prompt('design_draft_step1.md')
- idea-shaper:33 → dispatch_prompt('idea_shape_step1.md')

Verify:
- grep "dispatch_prompt('design_draft_step1.md')" bundled/design-pack/SKILL.md → 1 hit
- grep "dispatch_prompt('design_draft_step1.md', run_id)" bundled/design-pack/SKILL.md → 0
- grep "dispatch_prompt('idea_shape_step1.md')" bundled/idea-shaper/SKILL.md → 1
- grep "bundled/idea-shaper/prompts/subagent/idea_shape_step1.md" bundled/idea-shaper/SKILL.md → 0

Print WROTE: <one of the SKILL.md paths>.
```

---

## T7 — Phase E: enforcement test

### Sub-agent prompt skeleton

```
Read:
- docs/specs/2026-05-06-v4-spike-xiv-design.md § "Phase E" Test 추가
- T6 commit (방금 한 정정 결과)

Goal:
tests/unit/test_skillmd_dispatch_prompt_signature.py (NEW) 추가:
- test_all_bundled_skillmd_dispatch_prompt_uses_1arg_basename
- test_no_skillmd_uses_full_path_in_dispatch_prompt

regex pattern (spec 명시):
PROMPT_CALL_RE = re.compile(r"dispatch_prompt\(\s*['\"]([^'\"]+)['\"]\s*([,)])")

Skip prose placeholders: <file>.md, {file}.md, <X>.md, <name>.md (markers `<` or `{`).

Verify:
- pytest tests/unit/test_skillmd_dispatch_prompt_signature.py → 2 passed
- pytest 전체 → 833 passed (831 + 2 new)

Print WROTE.
```

---

## T8 — Phase F: B-20a 자동 sanity probe (18 AC)

### Sub-agent prompt skeleton

```
Read:
- docs/specs/2026-05-06-v4-spike-xiv-design.md § "Phase F — B-20a"
- tests/dogfood/spike_xiii_b18.py (참고 — 12 AC base)

Goal:
tests/dogfood/spike_xiv_b20a.py (NEW) — Spike XIII B-18 12 AC 그대로 + Phase A~E
fix 검증 6 AC 추가 = 18 AC.

Setup:
1. tempfile.mkdtemp(prefix='spike-xiv-b20a-')
2. shutil.copytree(real assemble dir → tempdir)
3. ASSEMBLE_HOME=<tempdir> 환경 진입
4. inventory.scan(force=True) + 12 AC 검증
5. AC13: from server.harness import wrap_with_preamble; check ASSEMBLE_HOME injection (set + unset)
6. AC14: grep mode-gate / Quick mode flow in 7 ★ SKILL.md
7. AC15: grep "Recommendation policy" + iter≤3 algorithm in plan-pack SKILL.md
8. AC16: from server.progress import mark_orthogonal_stage; mark 'safety' + 'meta' OK
9. AC17: load enforcement test result (run T7 test as subprocess in tempdir)
10. AC18: canonical_preamble_sha256() == "8d22a29c…089a9"
11. cleanup tempdir in finally

Output:
- docs/dogfood/spike-xiv-b20a.md — 결과 리포트, 18 AC PASS/FAIL 표 + wall-time

Verify:
- python tests/dogfood/spike_xiv_b20a.py → exit 0
- AC 18/18 PASS

Print WROTE: <path of the report doc>.
```

### Risk

T8 가 본 spike F Phase 의 핵심. Spike XIII B-18 패턴 재사용으로 ~1h 가능.

---

## T9 — Phase F: B-20b lived dogfood capture guide

### Sub-agent prompt skeleton

```
Read:
- docs/specs/2026-05-06-v4-spike-xiv-design.md § "B-20b lived dogfood 가이드"
- docs/dogfood/spike-xiii-b19-capture-guide.md (참고)

Goal:
docs/dogfood/spike-xiv-b20b-capture-guide.md (NEW) — Spike XIII B-19 가이드 기반 +
Phase A~E fix 검증 6 신규 capture point (C20~C25, spec 명시).

Wording 한국어 자연스럽게 (preamble rule 5 외래어 표기).

Verify:
- ls docs/dogfood/spike-xiv-b20b-capture-guide.md → 존재
- grep -c "C20\|C21\|C22\|C23\|C24\|C25" docs/dogfood/spike-xiv-b20b-capture-guide.md → ≥6 hits

Print WROTE.
```

---

## T10 — Phase F: B-20b 분석 + verdict

사용자 운전 후 별도 세션 transcript 기반 분석. T8 (B-20a 자동) PASS 후, 사용자가
별도 세션에서 task 1개 (예: 작은 CLI tool, 30~45분 scope) 완주. transcript iCloud
저장. 본 세션에서 분석:

```
Read:
- docs/dogfood/spike-xiv-b20b-capture-guide.md (T9 결과)
- transcript file (iCloud / /tmp 변환)

Goal:
docs/dogfood/spike-xiv-b20b.md (NEW) — verdict (SHIP-READY / SHIP-WITH-MINOR-CARRYFORWARDS / NEEDS-FIX / NEEDS-MAJOR-REWORK).

Spec § "V4 release gate 통과 조건" 적용:
- B-20a 18 AC 모두 PASS (T8) + verdict ∈ {SHIP-READY, SHIP-WITH-MINOR} → V4 출시 가능
- 단, 본 spike fix 5 항목 (C1, C2/I2, I1, I3, I4) 재발 0 — 재발 시 fix design 자체 재검토

Carryforward 항목 정리 (V5 backlog 추가).
```

T10 commit message:
- SHIP-READY: `docs(v4-spike-xiv-F): B-20b lived dogfood verdict — SHIP-READY`
- NEEDS-FIX: `docs(v4-spike-xiv-F): B-20b lived dogfood verdict — NEEDS-FIX (defects N개)`

---

## T11 — Ship: CHANGELOG flip + push

### Pre-condition

T10 verdict ∈ {SHIP-READY, SHIP-WITH-MINOR-CARRYFORWARDS}.

### Steps

1. CHANGELOG.md `[Unreleased]` 섹션의 V4 Spike XIV 항목을 `[VX.Y.Z] - 2026-05-XX`
   섹션으로 flip
2. git tag `v<version>` (필요 시)
3. push to origin

### Commit message

```
chore(v4-spike-xiv): ship — Spike XIII NEEDS-FIX cleanup verdict <SHIP-READY>

5 fixes applied:
- C1: ASSEMBLE_HOME auto-prepend in dispatch body (preamble v3 sha preserved)
- C2/I2: paradigm hybrid (default=full + opt-in --quick mode + AskUserQuestion gate)
- I1: plan-pack Step 6 Recommendation policy (iter≤3 + counts → yes default)
- I3: mark_orthogonal_stage API + progress.json schema 확장
- I4: SKILL.md dispatch_prompt 1-arg basename drift sweep + enforcement test

V4 release gate B-20a 18/18 PASS. B-20b verdict <verdict>.
```

---

## Risk register (overall)

| ID | Risk | Mitigation |
|---|---|---|
| R1 | T2 가 너무 큼 (7 SKILL.md + 7 prompts + 5 tests) — fresh sub-agent context 한계 | spec § "Fix design" 자세히. 만약 sub-agent 가 incomplete return 시 T2 를 T2a/T2b 로 분할 (T2a = SKILL.md 머리말, T2b = quick prompt files) |
| R2 | preamble v3 sha 변경 위험 — Phase A 가 실수로 preamble 본문 건드리면 ALLOW_LIST 깨짐 | A.AC1, A.AC2 에서 시작 + 끝 양쪽 검증. T1 sub-agent prompt 에 "preamble 본문 절대 미수정" 명시 |
| R3 | quick mode 산출물 schema 가 full mode 와 어긋날 위험 | T2 에서 7 quick prompt file 본문이 full mode 산출물의 sections 모두 cover 한다고 explicit 검증 |
| R4 | T8 (B-20a) 18 AC 중 일부 FAIL 시 어디 fix 할지 불명확 | AC 13~18 별 mapping: AC13→T1, AC14→T2, AC15→T4, AC16→T5, AC17→T7, AC18→T1 |
| R5 | T10 (B-20b) verdict NEEDS-FIX 시 Spike XV 후속 spike 필요 | spec § "V4 release gate 통과 조건 (재시도)" 에서 fix 5 항목 재발 0 강제. 재발 0 시 cleanup spike (Spike XV) scope 새 정의 |
| R6 | ALLOWED_PROMPT_FILES 42 → 49 확장 — 다른 코드에서 hardcoded 42 가정 있을까 | 사전 검증: grep -rn "ALLOWED_PROMPT_FILES.*42\|len.*42" — pre-scan 에서 발견 시 T2 에 포함 |
| R7 | 본 spike scope 외 정정이 필요해질 위험 (Spike XIII 누적 carryforward) | M-XII4 / F-XII1~5 등 모두 V5. 본 spike 에서 다루지 않음. 발견되면 T10 carryforward 정리에만 추가. |

---

## Success criteria (final)

본 spike 가 SHIP 으로 마감되려면:

- [x] T0 spec + plan single starting commit ✅ (이 plan)
- [ ] T1~T7 모든 atomic commit + 회귀 0 (pytest 813 → 833+ all green)
- [ ] T8 B-20a 18/18 AC PASS
- [ ] T9 capture guide 신규 작성
- [ ] T10 B-20b verdict ∈ {SHIP-READY, SHIP-WITH-MINOR-CARRYFORWARDS}
- [ ] V4 정체성 invariants 모두 보존 (preamble v3 sha unchanged, ALLOW_LIST = {v1, v2, v3} 그대로)
- [ ] T11 ship commit + push

V4 출시 게이트 (Spike XIII NEEDS-FIX → 본 spike SHIP) 진행 가능.

---

## Memory updates (post-ship)

ship 완료 후 메모리에 반영:
1. `project_assemble_v4_spike_xiv.md` (NEW) — 본 spike 결과 + V4 출시 verdict
2. `project_assemble_v4_spike_xiii.md` 끝부분에 "→ Spike XIV 에서 fix → SHIP" 한 줄
3. `project_assemble_v4_spec.md` § "Phase G 빈손 컴 dogfood" 결과 업데이트 (V4 결정 #6 release gate 통과)
4. `MEMORY.md` index 에 새 entry 추가
