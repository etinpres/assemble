<!--
PLACEHOLDERS (substituted via str.replace by shipper_tag_step4.md, abort path):
  {{RUN_ID}}, {{VERDICT}}, {{TIMESTAMP}}, {{CLEAN_TREE}},
  {{BRANCH}}, {{HEAD_SHA}}, {{VERIFY_VERDICT}}, {{VERDICT_REASON}},
  {{HANDOFF_COMMANDS}}, {{ABORT_DETAIL}}, {{ABORT_PHASE_NOTE}}

ABORT VARIANT: rendered when ANY of Steps 1-3 fails or skips. Sections 3 (Version
bump), 4 (Build artifact), 5 (Tag) are omitted; the {{ABORT_DETAIL}} line summarises
which steps actually completed (post-Spike-IX CF-1 fix — was previously hardcoded
"No version bump applied" which is wrong on build-fail path where version IS bumped).
-->

# SHIP REPORT (ABORT) — {{RUN_ID}}

**Verdict**: {{VERDICT}}
**Generated**: {{TIMESTAMP}}

## Summary

Run `{{RUN_ID}}` aborted (verdict **{{VERDICT}}**) at {{TIMESTAMP}}.
{{ABORT_DETAIL}}
See §Verdict reasoning for the concrete signal that triggered abort, and §Hand-off
for the recommended fix-then-re-run path.

## Pre-flight

| Field | Value |
|---|---|
| Clean tree | `{{CLEAN_TREE}}` |
| Branch | `{{BRANCH}}` |
| Head SHA | `{{HEAD_SHA}}` |
| Verify verdict | `{{VERIFY_VERDICT}}` |

(captured from `preflight.json`)

> ⚠️ {{ABORT_PHASE_NOTE}}

## Verdict reasoning

{{VERDICT_REASON}}

## Hand-off

Pre-flight failed — fix the underlying issue, then re-run shipper. Recommended next steps:

{{HANDOFF_COMMANDS}}
