# SCOPE — Both build and tag prefix sections

## Allow list

- `server/scope_parser.py` — extend parser

## Completion criterion

```bash
pytest tests/unit/test_scope_parser.py -v
```

## Build

`python -m build`

## Tag prefix

`v.`
